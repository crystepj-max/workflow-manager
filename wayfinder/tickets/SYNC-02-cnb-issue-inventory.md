# CNB issue #36–#77 账实盘点与本地未入库产出梳理

> 梳理时间：2026-09-18 23:45（本地会话）
> 范围：CNB `chris.ai/workflow-manager` issue #36–#77；本地检出 `main` @ `53436f9`
> 过滤口径：工作流 UI 交互类（FEAT-84/85/86 及其后继）由 ZCODE 推进，本文只做**排除式引用**，不提出改动指令。

---

## 一、总账（#36–#77）

区间内 36 个 issue（#64 / #67 / #68 / #70 / #71 / #74 为 PR 编号，非 issue，不在此列）。

| 分类 | 数量 | 含义 |
|---|---|---|
| 已关闭 | 14 | #42–#45、#47–#51、#65、#66、#69、#72、#73 |
| **开放但已开发已合入** | **17** | 代码已在主干，只差关闭 issue |
| **开放、已开发但未合入主干** | **3** | #38、#39、#41 |
| **开放且未开发** | **2** | #40、#75 |

核心结论：**22 个开放 issue 里，真正的开发缺口只有 2 个**，其余全是「账没跟上」。

---

## 二、A 类：已开发已合入，只差关 issue（17 项）

| cnb# | 任务 | 状态证据 |
|---|---|---|
| #36 | CHORE-36 验收清单前置条件规则补齐 | 登记册「已合并」，`merge.commit=e2c3994` |
| #37 | CHORE-37 closeout 角色 --force 表述收敛 | 「已合并」，`merge.commit=2f2fc55` |
| #46 | WR-005 人工接受完成需真实决定与版本来源 | 「已合并」，`e96853a` |
| #52 | WR-007 按输入版本维护依赖与证明失效 | 「已合并」，`a29de3b` |
| #53 | WR-008 声明、核验并索引实际交付产物 | 「已合并」 |
| #54 | WR-010 探索覆盖度与定向补充规则 | 「已合并」 |
| #55 | WR-014 收口事实整理与授权交付动作分离 | 「已合并」 |
| #56 | WR-015 实施前检查复用确定性判定 | 「已合并」 |
| #57 | WR-016 协议版本与可执行 Schema 能力 | 「已合并」 |
| #58 | WR-017 Current/Target/Legacy 文档收敛 | 「已合并」 |
| #59 | WR-018 节点权限与独立证明边界 | 「已合并」 |
| #60 | WR-019 四模板消费方契约与端到端验收 | 「已合并」 |
| #61 | WR-020 按 Run/节点/尝试的质量成本记录 | 「已合并」 |
| #62 | WR-021 状态与恢复核心提炼 | 「已合并」 |
| #63 | WR-022 代表任务度量模板复杂度 | 「已合并」，`70e1845` |
| #76 | FIX-76 M5 调度器内建登记册对账 | 代码已落地，登记册状态滞后（见下） |
| #77 | FEAT-77 M5 夜间批次调度器 | 代码已落地，登记册状态滞后（见下） |

### #76 / #77 的专门核验（二者登记册仍写「定义中」，属滞后）

- **FEAT-77**：`scripts/ai-task-dispatcher.mjs`(主调度器)、`ai-task-candidate-collect.mjs`、两份 M5 测试、fixtures 均已存在于主干 → **实现完整**。
- **FIX-76**：目标是把「主干合并事实」引入 M5 依赖判定。已核：`ai-task-candidate-collect.mjs:25` 与 `ai-task-execution-plan.mjs:32` 均 `import { collectMergeFacts }`，`registry-reconcile.mjs` 已就位并有 `registry-reconcile.test.mjs` → **实现已到位**。
- 滞后原因：`registry-reconcile.mjs` 只回写**主干 merge commit** 携带的任务号；这两笔是经 CNB PR 合入、未在提交信息里带任务尾注，故未被回写。属机制盲区，不是开发缺口。

---

## 三、B 类：已开发但未合入主干（3 项）

### #38 CHORE-38 · 收口脚本 prune 兜底单测补齐（P2）

- 分支 `dev-chore-38-r1`，4 个独有提交，末提交写明「施工完成，置等待验收」。
- 产出齐：`scripts/local-task-merge.mjs` 改 5 行 + `local-task-merge.test.mjs` 加 30 行 + 规格四件套（`task-spec-V1.md` 197 行 / `requirements-analysis.md` / `definition-check.md` / `uat-card-V1.md`）。
- 登记册仍写「定义中」→ 滞后。
- **状态判定：开发完成，待合入主干**。

### #39 LOC-016 · 运行看板 Logical Run 决策卡与工作区 UI（P2）

- 分支 `dev-loc-016-r1`，17 个独有提交，`client.js` +1319 行、`host.js` +77 行、双语 locale 各 +109 行、`client.smoke.mjs` +535 行、新增 `logical-run.test.mjs` 221 行。
- 历史：V2 拆两片（片1 呈现层）→ 三轮收敛审查修复 → V3 根据验收反馈拆为「列表页 + 详情窗口」→ 停在人工验收，返工额度耗尽。
- 交互层（决策卡点选续跑、BLOCKED 改模型恢复）被未决决策票 `DT-01-ui-resume-attribution` 阻塞。
- **替代评估见第五节：建议被 FEAT-85 取代**。

### #41 LOC-022 · 开发侧 DSH 运行态残留清理与登记册一致性收敛（P2）

- 登记册「已完成」，已产出 `cleanup-report.md`。
- 性质：**环境一次性清理任务**，不是代码开发（目标是 `~/.dsh-workflow-dev` 下约 220 KB 残留，目的为消除「登记册指向已消失实体」的不一致，非释放空间）。
- 🟡 风险：2026-09-18 本机发生过一次整体磁盘清理，开发 Home 相关目录被波及。该任务清理对象多半已随清理消失 → **建议复核清理报告是否仍有意义后直接关单**，不要重做。

---

## 四、C 类：未开发（2 项）与是否仍有必要

### #40 LOC-018 · v0.1 产品模式发布验收

- 无分支、无代码规格；依赖 9 个前置任务（含 LOC-016）。
- **性质：这是发布前的验收闸门，不是开发任务**。它无法被「开发」，只能在前置满足条件后**执行**。
- 判定：**必须保留**，但不应排在开发队列里。其依赖链中 LOC-016 的位置应改由 FEAT-85 承接。

### #75 CHORE-75 · 开工与收口纪律对齐（工作区命名与收口归档强制护栏，集成 registry-reconcile 例行对账，P1）

- 无分支、无规格。
- 拆分看：
  - 「集成 registry-reconcile 例行对账」→ **已被 CHORE-73 + FIX-76 覆盖**（#76/#77 已落地验证）。
  - 「工作区命名 / 收口归档强制护栏」→ 尚未发现任何实现；FIX-72 已做编号迁移但未做强制护栏。
- 判定：**仍有开发的必要，但范围应缩减为「命名与归档护栏」这一半**。「不必要的内核」不是主观判断——另一半已经落地，重做等于返工。

---

## 五、LOC-016 是否可被 FEAT-84/85/86 取代（专项）

### 逐项对照

| LOC-016 范围项 | FEAT-85 对应条目 | 判定 |
|---|---|---|
| Logical Run 详情视图（时间线/分段/快照修订/业务结果/完成类型） | 详情工作区：生命周期、当前分段、快照修订、节点尝试、业务结果、完成类型、正式产物、活动记录 | **覆盖且更强** |
| 结构化人工决策卡（点选 + 理由 → 续跑） | WAITING_HUMAN 决策卡：选项、理由输入、提交前影响说明、提交后锁定、续跑中状态，支持 USER_ACCEPTED / ADD_BUDGET / STOP | **覆盖** |
| BLOCKED 恢复引导（改 Provider/Model → 新 Snapshot Revision） | BLOCKED 恢复卡：阻塞原因、Provider/Model 修改前后值、产生新 Snapshot Revision 且保留旧修订 | **覆盖** |
| 工作区面板（mode/repository/branch/HEAD/集成状态/活动锁/清理状态） | 同名字段逐项一致 | **覆盖** |
| Outcome / Completion 筛选 | 结果 / 完成类型筛选（并增加空间筛选、分页、返回位置保留） | **覆盖** |

### LOC-016 唯一的「非 UI」产出评估

`host.js` +77 行（`liveActiveLocks` / `refreshWorkspaceContext`）是为「工作区面板」供数的数据层。而 FEAT-85 的要求里同样包含该面板（含活动锁），故这 77 行**不构成独立价值**——它服务的是一个已被替代的表面。

### 结论与选项（不由本文代签）

| 选项 | 成本 | 收益 | 风险 |
|---|---|---|---|
| **A · 取代（推荐）** | 关闭 cnb#39、归档分支 `dev-loc-016-r1`，约 0.5 人时 | 消除两套同名逻辑争夺同一份 `client.js` 的长期隐患，界面出口归一 | FEAT-85 若有未覆盖的细项需另行拾起（对照上表五项，未发现） |
| B · 部分复用 | 从 `dev-loc-016-r1` 挑拣 `logical-run.test.mjs` 与 i18n key 移植，1–2 人时 | 保留部分测试资产 | 移植的测试面向旧裁剪的项目结构，大概率需大量改写，性价比低 |
| C · 并存 | 不可行 | — | 两套逻辑同时进主干，FEAT-85 卡已明文禁止 |

**推荐 A**。理由：五项目标逐条被覆盖，LOC-016 已无独立交付价值；且它当前的 1319 行 `client.js` 相较主干落后 58 个提交，合入成本远高于重做成本。

> 注：LOC-016 / cnb#39 的最终裁决权归属 FEAT-85 收口侧的 V-12 / UAT-07，且 UI 执行归 ZCODE。本文只提供账实证据。

---

## 六、交叉发现：登记册存在系统性滞后

以下条目「实际状态 ≠ 登记册状态」：

| 任务 | 登记册写 | 实际 |
|---|---|---|
| FIX-76 | 定义中 | 已实现（代码在骨干） |
| FEAT-77 | 定义中 | 已实现（dispatcher 在骨干） |
| CHORE-38 | 定义中 | 开发完成，待合入 |
| LOC-016 | 本地已定义 | 呈现层实现存在但未合并 |

根因：`registry-reconcile.mjs` 只认**主干 merge commit 尾注**这一个信号。分支上的自建工作、经 CNB PR 合入但未带尾注的提交，两类都看不见。若不改，这类「账实不符」会反复出现。这是 #75 缩减后范围里值得加的一条。

---

## 七、附：本地未入库产出清单（2026-09-18 清点）

**未推送 / 未合入主干的部分**

1. 本地 `main` 领先 `origin/main` 1 个提交 `53436f9`（FEAT-102 验收登记 + FEAT-103 立卡），未推 GitHub。本地 `cnb/main` 引用停在今早 10:49，不含该提交。
2. `dev-chore-38-r1`：CHORE-38 全部产出（代码 + 测试 + 规格 + UAT 卡）未合并主干。
3. `dev-loc-016-r1`：LOC-016 产出未合并（建议作废）。
4. **stash@{1}**：14 文件 124 行改动。经逐行比对，**78%（97 行）已进主干**，真正未落地仅 27 行（22%），且其中大部分是被主干重构取代的旧写法。详见 SYNC-03。
   > ⚠️ 本条曾误判为「含 assetScripts 35 行未进主干」，方向写反了：实测**主干有 `assetScripts`（3 处）、stash 无（0 处）**，是 stash 为旧写法、主干更先进。2026-09-19 已纠正。
5. stash@{0}：1 行登记册修正（可弃）。
6. stash@{2}：`feat/ai-task-deliver-m2` 上的 `temp`，内容待核。
7. `dev-loc-045-r1`：51 个 benchmark 结果 / 报告文件未提交（多为可重生成的跑测产物）。
8. `codex/9897` 旧工作树（detached 于 `909ae5f`）：`package.json` 改动 + 未跟踪 `handoffs/` 与 `prototypes/`（后两者主干已有同名内容，属旧泡影）。
9. 主检出：未跟踪 `wayfinder/tickets/SYNC-01-cnb-panel-deferred.md`（本次同步新建）。

**排除：ZCODE 的 UI 工作**

`dev-feat-103-r1` 工作区有 7 个未提交文件（`builtin-roles.json`、`locales/zh.json`、`locales/en.json`、`src/client.js`、`role-manifest.test.mjs`、`role-library.cjs`、`role-library.test.mjs`），属角色库/UI 链路，不在本清单处置范围。
