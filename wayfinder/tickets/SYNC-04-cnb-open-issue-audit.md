# SYNC-04 · CNB 未关闭 issue 盘点（2026-09-19）

> 触发：松哥 2026-09-19 指令「检查 CNB 剩下还未关闭的 issue，哪些已经实现可关闭，哪些尚未实现不能关闭，尚未实现的目前什么进展，是否可直接开工，还是有前置依赖」。
> 只读盘点 + 证据核验；除 CHORE-111 规格补一行声明外，**未对任何 issue 执行关闭动作**。

## 0. 基线

| 项 | 实测 |
|---|---|
| CNB 未关闭 | **7 个**：#40 / #41 / #75 / #97 / #109 / #110 / #111 |
| CNB 已关闭 | 51 个 |
| GitHub issue 总数 | 114 个（无本批 4 个任务的对应 issue，其 `github_sync` 仍 `pending`） |
| 三向同步 | `origin/main` = `cnb/main` = 本地 `main`（本次推送后归零） |

## 1. 分类结论

| CNB | 任务 | 登记状态 | 实现情况 | 可否关闭 | 开工前置 |
|---|---|---|---|---|---|
| **#41** | `LOC-022` 开发侧 DSH 运行态残留清理 | `已完成` | ✅ **已实现** | ✅ **可关闭** | — |
| **#97** | `FIX-97` VWF 运行宿主三项修复 | `等待验收` | ✅ 已实现，**未合并** | ❌ 不可关 | 合并 + 人工验收 |
| **#40** | `LOC-018` v0.1 产品模式发布验收 | `定义中`（卡面写「本地已定义」，不一致） | ❌ 未实现 | ❌ 不可关 | 定义补齐 + 卡面字段规范化 + 依赖收敛；**必须人在环** |
| **#75** | `CHORE-75` 开工与收口纪律护栏 | `定义中` | ❌ 未实现 | ❌ 不可关 | 定义收尾（补 definition-check + 规格声明未决 0 + 状态推进） |
| **#109** | `FIX-109` 收口脚本两处口径缺口 | `定义中` | ❌ 未实现 | ❌ 不可关 | **仅待基线 V1 确认** → 确认即可开工 |
| **#110** | `CHORE-110` 轻量路线验收包可登记性 | `定义中` | ❌ 未实现 | ❌ 不可关 | **DT-01（分层 A/B/C）未裁定 → 阻塞** |
| **#111** | `CHORE-111` 发号源 CNB→GitHub 迁移 | `待确认` | ❌ 未实现 | ❌ 不可关 | **仅待基线 V1 确认** → 确认即可开工 |

## 2. 逐项证据

### #41 / LOC-022 —— 可关闭

- 登记册 `status=已完成`；任务卡变更记录：「2026-09-16 松哥确认全清（含 2 条 WAITING_HUMAN）+ 不停实例」。
- 8 项验收标准**全部打勾**；清理报告已落盘 `docs/tasks/specs/LOC-022-dsh-residue-cleanup/cleanup-report.md`（含 definition-check 与规格 V1）。
- **只读复核（本次独立执行，2026-09-19）**：

  | 核查项 | 期望 | 实测 |
  |---|---|---|
  | `~/.dsh-workflow-dev/tasks/loc-014-r1/` | 已清 | 目录不存在 ✅ |
  | 开发侧 `workspaces/records/` | 12 → 0 | **0 条** ✅ |
  | 开发侧 `state.json` `workspaces` | 11 → 0 | **0 条** ✅ |
  | 同 `state.json` `archived` | 0 | **0** ✅ |
  | 产品侧 `~/.dsh/workspaces/records/` | 零改动 | 未触碰（计数已随产品运行自然变化）✅ |

- 归属：运行操作类任务，无 merge 概念（`merge=null` 属正常）。

### #97 / FIX-97 —— 已实现未验收，不可关

- 代码已落地：`packages/dsh-visual-workflow/src/host.js` 三处补丁 + `tests/evaluation-baseline-runtime.test.mjs` EB6/EB7/EB8 用例（+238/-17，9 文件）。
- 定义链完整：规格 V1 + definition-check（全过、未决 0）+ requirements-analysis + `DT-01`（采纳 A）。
- **未合并**：`fix/FIX-97-vwf-run-host-fix`（`5e819bf`，2026-09-17 23:05）领先 main 1 个提交；另有重做版 `fix/FIX-97-vwf-run-host-fix-clean`（`605ba93`，2026-09-18 09:39，挂在 `/private/tmp/fix97clean`），相对 main 差异 230 行，与前者近乎等价（host.js 21 vs 25 行）。
- 卡上明确「验收：**待合并后产品验收**」→ 阻塞点在合并与人工验收，不在开发。

### #40 / LOC-018 —— 未实现，需先整改后方可开工

定义门实测失败 16 条，归为四类：

1. **卡面字段不合规**：`优先级 = P1（发布闸门）`（须纯 `P1`）；`GitHub 同步 = 待补 issue`（非法值，须 `pending`/`synced#N`/`not-applicable`）；`无人值守许可 = 不允许`（门禁要求 `允许`）。
2. **产物缺失**：无 `definition-check.md`；规格未声明 `未决产品事项：0`。
3. **依赖状态不合规**：`LOC-010` 为 `已取消`、`LOC-016` 为 `本地已定义` —— 门禁要求依赖须 `已合并`。
4. **卡面依赖字段格式**致解析器取到 `已在外部验收合并）` 等碎片 → 需改为规范清单。

**实质依赖其实已满足**：`LOC-008/011/012/013/014/015/017` 均已合并；`LOC-010` 已取消（应从依赖中移除）；`LOC-016` 已作废、其范围由 `FEAT-85`（已合并）承接 → 依赖应收敛为 `FEAT-85`。

**开工属性**：发布闸门含人工 E2E，无人值守许可为「不允许」→ **必须人在环**，无法无人值守执行。

### #75 / CHORE-75 —— 未实现，差定义收尾

- 卡 + 规格 V1 已入库（本会话为它补卡，原为无卡裸票，`deps=[]` 无外部前置）。
- 定义门失败 3 条：状态仍 `定义中`；缺 `definition-check.md`；规格未声明 `未决产品事项：0`。
- 其范围（开工/收口护栏 + `registry-reconcile` 识别盲区）**已由本次盘点再次验证为真实存在**（见 §3）。

### #109 / FIX-109、#111 / CHORE-111 —— 可直接开工（待基线确认）

- 两票卡 + 规格 + definition-check 齐全，定义门**均只剩一条**「当前状态必须为『已定义』或『本地已定义』」。
- `#111` 本次补上规格 §9 漏写的显式行 `> 未决产品事项：0`（检查器按 `/未决产品事项[^\n]*[：:]\s*0/` 从规格取值，只写在 definition-check 里不算），失败数 2 → 1。
- 阻塞点相同：**待松哥确认需求基线 V1** → 随后把状态推进为 `本地已定义` 即可开工。
- ⚠️ `FIX-109` 与 `CHORE-111` **同改 `local-task-merge.mjs`**（不同行段：L237 收尾回写 / L410 提示串）→ 需排先后，不宜真并行。

## 3. 反向不一致（额外发现，非本次问题范围）

`cnb#76 / FIX-76`、`cnb#77 / FEAT-77` 的 issue **已关闭**，但登记册仍为「定义中」且 `merge=null` —— 实际二者均已实现并合入 main：

- `FIX-76`：PR #88（`40e70da`）、PR #96（`488ee34`）→ 合入 PR #200/#201。
- `FEAT-77`：PR #87（`67abc23`）→ 合入 PR #192；其规格已标注「已由 FEAT-77 实现」。

这正是 `CHORE-75` 规格所写「`registry-reconcile` 只认主干 merge commit 尾注，分支自建工作与无尾注的 CNB 合并均不可见」的实例。**无需重开 issue**，只需补登记册状态（属 CHORE-75 范围）。

## 4. 待松哥裁决

1. **cnb#41 / LOC-022 是否关闭**（唯一「已实现可关闭」项；关闭为对外动作，未擅自执行）。
2. **FIX-109 / CHORE-111 基线 V1 是否确认**（确认即可开工；两者同文件需排先后）。
3. **CHORE-110 的 DT-01（A 分层 / B 新记录类型 / C 不放宽）选哪个**（不裁定则永久阻塞）。
4. `LOC-018` 与 `CHORE-75` 是否安排「定义补齐」——两者都不是「能直接开工」，都差最后一小段定义工作。
