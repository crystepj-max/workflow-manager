# 样板项目说明文档

本文档用于 LOC-045 O01 优化任务。下方十条事实必须逐字保留；外围说明可在优化后精简，但事实原句及编号不得改动。

## 外围说明（可压缩区域）

本项目样板用于验证工作流在文档优化场景下的行为。优化目标是在不改变任何事实陈述的前提下，减少文档整体字符长度。评审人员应逐条核对事实是否原句保留，并测量完整文本的 Unicode code point 长度是否减少至少百分之二十。请勿删除编号、请勿改写事实、请勿用摘要替代原句。

## 固定事实（不可改动）

1. 项目名称为样板。
2. 输入格式为JSON。
3. 输出编码为UTF-8。
4. 日志时间采用UTC。
5. 每页默认20条。
6. 最大重试次数为2。
7. 空列表显示无数据。
8. 文件名区分大小写。
9. 错误输出写入stderr。
10. 成功退出码为0。

## 附加外围（可压缩区域）

如需进一步说明，可在此补充背景，但不得插入会改变事实编号或事实原句的内容。优化后应仍能按编号定位十条事实。
