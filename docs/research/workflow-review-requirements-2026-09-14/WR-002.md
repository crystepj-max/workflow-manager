# WR-002｜阻止互相矛盾的裁决进入下一质量关口

| 元数据 | 内容 |
|---|---|
| 需求版本 | 建议 V1，2026-09-14 |
| 状态 | 需求拆分建议；未登记为已定义施工任务，未启动实施 |
| 建议优先级 | P0 |
| 来源 | F2：裁决与路由一致性 |
| 评审源码基线 | `8e38d74955c67f0f196c93eb880cfef7cce71cbd`；开发前核对当前 HEAD 与相关模块变化 |

## 1. 需求背景与参考资料

建设 review 的 route/verdict、test 的 route/result 各自合法但可互相矛盾。contradictory-verdict-and-head 复现中，REQUEST_CHANGES 配 APPROVE、FAILED 配 PASS 仍进入 UAT。结构正确不能代表业务判定正确。

来源为[系统评审存档](/Users/chris/.codex/worktrees/77ea/workflow-manager/docs/research/workflow-review-requirements-2026-09-14/evidence/workflow-design-review-2026-09-14.md)。评审证据为源码查阅、195 项相关自动检查与 7 个替身环境复现；不代表真实模型故障频率或产品人工验收。新增规则与数值是本条建议基线，不是已有批准记录。

- [templates/wf-construction-full-feature.json](/Users/chris/.codex/worktrees/77ea/workflow-manager/templates/wf-construction-full-feature.json:95)
- [templates/wf-construction-full-feature.json](/Users/chris/.codex/worktrees/77ea/workflow-manager/templates/wf-construction-full-feature.json:150)
- [scripts/generate.mjs](/Users/chris/.codex/worktrees/77ea/workflow-manager/scripts/generate.mjs:505)

## 2. 需求目标与验收标准

**目标：**每份专业结论只有一种可解释的路由，失败或矛盾结果不能被当成通过。

**业务规则与边界：**

- V1 保留现有字段以降低迁移风险，增加明确映射：review 的 APPROVE/APPROVE、RETURN_DEV/REQUEST_CHANGES、BLOCKED/COMMENT_ONLY；test 的 PASS/PASSED、RETURN_DEV/FAILED、BLOCKED/BLOCKED。其他组合均为契约错误，不作专业通过判断。COMMENT_ONLY 在此表示无法形成可放行裁决，普通非阻断建议仍随 APPROVE 报告。
- 在路由和 UAT 组装之前执行同一确定性检查；错误携带字段及允许组合。未来去掉重复字段是另行兼容迁移，本条不要求删除。
- 诊断与优化维持现有单裁决字段；探索的完成类型一致性由 WR-010 负责。

**可检查的验收条件：**

- [ ] AC-01：列出的 6 个合法组合均走预期边；其余 route/verdict 或 route/result 组合全部被拒绝。
- [ ] AC-02：对复现中的两种矛盾分别测试，UAT、人工等待与收口调用数均为 0。
- [ ] AC-03：拒绝原因能区分“契约矛盾”与专业判断“需要修改”；不把矛盾结果存成有效通过证明。
- [ ] AC-04：角色、模板 Schema 和运行时校验对这 6 个组合表述一致；旧字段仍可读取。

**代表验收路径：**向真实生成脚本返回“审查要求修改但 route=APPROVE”，确认用户得到契约错误；改为 RETURN_DEV 后应正常回开发。测试结论做同样检查。

## 3. 开发依赖与接口约定

**硬前置：**无。可在当前实现上独立开始；关联改动不等于前置条件。

**关联模块 / 接入位置：**

- [templates/wf-construction-full-feature.json](/Users/chris/.codex/worktrees/77ea/workflow-manager/templates/wf-construction-full-feature.json)
- [scripts/generate.mjs](/Users/chris/.codex/worktrees/77ea/workflow-manager/scripts/generate.mjs)
- [scripts/validate-core.cjs](/Users/chris/.codex/worktrees/77ea/workflow-manager/scripts/validate-core.cjs)
- [dsh/roles/review.md](/Users/chris/.codex/worktrees/77ea/workflow-manager/dsh/roles/review.md)
- [dsh/roles/test.md](/Users/chris/.codex/worktrees/77ea/workflow-manager/dsh/roles/test.md)

**输入、输出与接口归属：**本条拥有建设 review/test 的裁决组合表及路由前语义校验；提供结构化 CONTRACT_INCONSISTENT 错误供 WR-011 分类。

**开工材料与环境：**在仓库根使用 Node.js 24 与已安装依赖；先检查工作区和基线差异。四模板真源位于 `templates/`，角色真源位于 `dsh/roles/`，`.generated/` 只能重新生成。已有项目审批、人工验收及远端政策继续适用，本建议不授权自动发布、合并或无人值守执行。

## 4. 影响范围与建议优先级

**影响对象：**建设审核、测试、UAT 的放行行为，以及角色输出与错误提示。

**功能边界 / 非目标：**只解决裁决一致性及所需角色文案；成果版本核对归 WR-003，通用 Schema 能力声明归 WR-016。

**优先级理由：**影响错误放行、虚假完成或失控消耗，建议先于扩展使用范围处理。

**风险与兼容：**COMMENT_ONLY 兼容含义须在模板内写明，避免把非阻断建议升级为失败。P0 表示应先于继续宣称自动质量门可靠交付。

## 5. 交付与验证要求

- 交付本条范围的实现或明确指定的研究/文档成果、必要回归检查、直接受影响文档及逐条 AC 结果；不混入兄弟条目的产品范围。
- 优先使用现有相邻 `scripts/test/`、插件 `tests/` 与宿主替身；功能修复先建立能在旧行为下失败的检查。文档需求验证事实、链接和生成一致性，不机械新增实现镜像测试。
- 变更蓝图/生成规则时执行 `npm run generate` 与 `npm run validate`；运行与实际风险对应的测试。需要发布时另按项目既有 `npm run release:verify` 和真实产品模式验收流程执行。
- 交接列出已实现、自动检查、真实环境验证、待人工验收及已知限制；未执行项明确记录，不以自动 PASS 代替人工决定。
- 本条可单独评审和验收。修改相同文件只构成并行合并风险，不能擅自新增业务依赖或扩大交付范围。
