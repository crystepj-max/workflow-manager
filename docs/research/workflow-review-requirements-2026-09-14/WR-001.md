# WR-001｜显式交接节点输入与返工反馈

| 元数据 | 内容 |
|---|---|
| 需求版本 | 建议 V1，2026-09-14 |
| 状态 | 需求拆分建议；未登记为已定义施工任务，未启动实施 |
| 建议优先级 | P0 |
| 来源 | F1；最小通用协议：系统交接信息 |
| 评审源码基线 | `8e38d74955c67f0f196c93eb880cfef7cce71cbd`；开发前核对当前 HEAD 与相关模块变化 |

## 1. 需求背景与参考资料

普通节点只得到原始任务、路径和角色上下文；业务边流转没有保证将上一步评估意见交给下一次执行。复现 feedback-handoff 中执行节点运行了两次，第二次提示仍没有评估差距或明确报告引用。用户看到“正在优化”，实际执行者可能不知道要改什么。

来源为[系统评审存档](/Users/chris/.codex/worktrees/77ea/workflow-manager/docs/research/workflow-review-requirements-2026-09-14/evidence/workflow-design-review-2026-09-14.md)。评审证据为源码查阅、195 项相关自动检查与 7 个替身环境复现；不代表真实模型故障频率或产品人工验收。新增规则与数值是本条建议基线，不是已有批准记录。

- [scripts/generate.mjs](/Users/chris/.codex/worktrees/77ea/workflow-manager/scripts/generate.mjs:260)
- [scripts/generate.mjs](/Users/chris/.codex/worktrees/77ea/workflow-manager/scripts/generate.mjs:707)
- [templates/wf-optimize.json](/Users/chris/.codex/worktrees/77ea/workflow-manager/templates/wf-optimize.json:50)

## 2. 需求目标与验收标准

**目标：**节点能够声明并实际收到本次执行必需的目标、基线、成果和反馈，返工围绕最新差距进行。

**业务规则与边界：**

- 提供节点级输入绑定，支持任务原始输入、指定上游节点的结构化字段、指定成果引用；required/optional、首次运行默认值必须显式声明。普通首次执行允许尚无评价反馈；由返工边进入时，触发返工的裁决和差距必须存在。
- 解析发生在调用代理之前，输出不可变输入清单；记录生产节点、执行标识及可用的成果版本。仅注入声明内容，正文可通过明确的文件引用交付；不把整个 Run 的历史无限拼进提示。
- 缺必需输入、引用错误类型或当前轮没有成功产出时，在调用消费节点前给出可定位错误；不得静默退用另一轮旧结果。旧蓝图维持原行为并显式标注旧输入模式。

**可检查的验收条件：**

- [ ] AC-01：优化首轮执行正常；评估返回带唯一标记的 OPTIMIZE 后，第二次执行收到该标记、评价契约引用和当前成果引用。
- [ ] AC-02：诊断修复收到对应 diagnosis 的根因和证据；建设返工收到触发本次返工的 review/test 反馈，而非另一轮的旧报告。
- [ ] AC-03：缺必需引用时消费节点调用数为 0，错误包含 node、binding 和原因；可选输入未产生时按声明处理。
- [ ] AC-04：两轮反馈同时存在时仅选择本次流转对应版本；蓝图编辑、投影往返、生成脚本均保留输入声明。

**代表验收路径：**运行优化夹具：先写问题 A 的评价，再产生问题 B 的评价并再次返工；检查两次执行各收到自己的差距，删除 B 的必需引用后应停止在执行前。

## 3. 开发依赖与接口约定

**硬前置：**无。可在当前实现上独立开始；关联改动不等于前置条件。

**关联模块 / 接入位置：**

- [scripts/generate.mjs](/Users/chris/.codex/worktrees/77ea/workflow-manager/scripts/generate.mjs)
- [scripts/validate-core.cjs](/Users/chris/.codex/worktrees/77ea/workflow-manager/scripts/validate-core.cjs)
- [scripts/projection-core.cjs](/Users/chris/.codex/worktrees/77ea/workflow-manager/scripts/projection-core.cjs)
- [templates/wf-optimize.json](/Users/chris/.codex/worktrees/77ea/workflow-manager/templates/wf-optimize.json)
- [templates/wf-diagnose.json](/Users/chris/.codex/worktrees/77ea/workflow-manager/templates/wf-diagnose.json)
- [templates/wf-construction-full-feature.json](/Users/chris/.codex/worktrees/77ea/workflow-manager/templates/wf-construction-full-feature.json)

**输入、输出与接口归属：**本条拥有“节点输入声明 → resolved_inputs”的接口。每项至少包含 binding、producer、version_ref、value 或 artifact_ref；已有 Record 时引用其 Revision，否则明确标记临时执行引用，不伪装为正式证据。WR-007 消费此清单。

**开工材料与环境：**在仓库根使用 Node.js 24 与已安装依赖；先检查工作区和基线差异。四模板真源位于 `templates/`，角色真源位于 `dsh/roles/`，`.generated/` 只能重新生成。已有项目审批、人工验收及远端政策继续适用，本建议不授权自动发布、合并或无人值守执行。

## 4. 影响范围与建议优先级

**影响对象：**四模板的节点调用上下文、返工路径与蓝图编辑/生成；现有未声明输入的旧模板保持可识别兼容。

**功能边界 / 非目标：**覆盖普通节点及输入绑定的编辑/生成/运行链；不承担正式记录持久化、文件内容校验或业务评价质量。

**优先级理由：**影响错误放行、虚假完成或失控消耗，建议先于扩展使用范围处理。

**风险与兼容：**共享上下文可能含大量内容；按声明解析和裁剪，保留引用，避免提示膨胀。涉及生成器与投影的并行修改须合并后检查往返一致性。

## 5. 交付与验证要求

- 交付本条范围的实现或明确指定的研究/文档成果、必要回归检查、直接受影响文档及逐条 AC 结果；不混入兄弟条目的产品范围。
- 优先使用现有相邻 `scripts/test/`、插件 `tests/` 与宿主替身；功能修复先建立能在旧行为下失败的检查。文档需求验证事实、链接和生成一致性，不机械新增实现镜像测试。
- 变更蓝图/生成规则时执行 `npm run generate` 与 `npm run validate`；运行与实际风险对应的测试。需要发布时另按项目既有 `npm run release:verify` 和真实产品模式验收流程执行。
- 交接列出已实现、自动检查、真实环境验证、待人工验收及已知限制；未执行项明确记录，不以自动 PASS 代替人工决定。
- 本条可单独评审和验收。修改相同文件只构成并行合并风险，不能擅自新增业务依赖或扩大交付范围。
