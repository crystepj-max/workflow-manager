# 评审证据存档

基线：`8e38d74955c67f0f196c93eb880cfef7cce71cbd`。原评审日期：2026-09-14。

- `workflow-design-review-2026-09-14.md`：完整评审；仅把附件链接从临时目录改为本存档目录。
- `workflow-design-review-probes.json`：7 项语义缺口原始结果。
- 两份 `.mjs`：原始诊断脚本原样保存，包含评审工作区绝对路径和 `/tmp` 输出路径；迁移现场运行前需只调整 root/import/output 路径。它们是历史证据，不是当前产品回归测试入口。
- 两份 `.tap`：原 135 项核心/模板检查及 60 项宿主检查日志；不是本需求包新改动的测试结果。

原脚本使用编译器和宿主代码，代理/传输等使用既有测试替身，不启动真实模型或外部服务。复跑会在 `/tmp` 写诊断结果；正式修复应将对应最小反例纳入仓库现有测试目录，并检查旧行为会失败。

原结果中的 `record_dir` 是当时临时目录，仅作来源标识，不保证长期存在。结论和必要数值已保存到 JSON 与评审，不依赖该临时目录才能阅读需求。
