# Definition Check · CHORE-262 CI 可信度 M5 回归修复

## 检查元数据

| 字段 | 值 |
|---|---|
| 任务标识 | `CHORE-262` |
| 远端 issue | github#262 |
| 检查对象 | 收敛 `local-task-registry.test.mjs` 降级告警用例的断言口径，使该文件在有/无宿主指纹下均全绿 |
| 检查时间 | 2026-09-21 |
| 结论位置 | 本文末「结论」节 |

## 目标与范围

- [x] 目标限定为：该文件在**有指纹**与**无指纹**两种环境下均 16/16 通过
- [x] 范围限定为**单个用例的断言口径**，不改产品代码
- [x] 明确不含：修改 `allocate` 的告警行为、文案或触发条件
- [x] 明确不含：该测试文件的其他用例与任何周边代码
- [x] 明确不含：父票其余子票范围（M3 / M2 / M4 / M6 / M7）

## 现状证据

- [x] 失败位置取自 CI 真实输出：`main@f0d95202` 的 `validate` 作业，③ 段失败项之一，位置 `scripts/test/local-task-registry.test.mjs:179:1`，原文 `AssertionError: Expected values to be strictly equal`
- [x] 根因由父票 CHORE-260 实证：含 FIX-245 代码 + 移除宿主指纹 → 1 项红；FIX-245 之前提交 `7a8ae6a5` + 同环境 → 16 项全绿
- [x] 本票本地复现（修复前）：无指纹 `15/16`（1 项红），有指纹 `16/16`
- [x] 根因指向行：`scripts/local-task-registry.mjs:443`「未识别到 agent 身份」告警，与降级告警同路径
- [x] 修复后双环境复测：无指纹 `16/16`、有指纹 `16/16`、`agent-identity.test.mjs` `9/9`

## 验收标准

- [x] AC-1 / AC-2 / AC-3 / AC-4 均可机器验证，判据为具体命令与通过数
- [x] AC-4 以「产品源码零 diff」约束，杜绝通过放宽产品行为「修绿」
- [x] 标准不含模糊表述

## 依赖与前置

- [x] 无跨票代码依赖：本票与父票其余子票彼此独立，可分别施工
- [x] 施工隔离：本票在独立 worktree / 分支施工，不动主检出内并行会话的在制内容
- [x] 发号完成：`gh` 已登录 `crystepj-max`，发号得到 `github#262`
- [x] 定义落档：任务卡、本规格、本核对清单三者齐备且互相引用一致

## 风险与残留

- [x] 断言改为过滤后计数后，「告警总数」不再被约束；该取舍为父票规格 §M5「修法 ①」明确选定，并已在代码注释中标注理由
- [x] 若将来身份告警文案变更，本用例不受影响（已按语义过滤，不依赖文案总数）
- [x] 无产品行为改动，不引入新回归风险面

## 结论

**通过。** 根因已由父票实证、本票已独立复现；修法为单用例断言口径收敛，改动面极小且判据可机器验证；任务卡 / 规格 / 核对清单三件套齐备，具备开工条件。
