# FEAT-208 需求分析摘要

## 来源判定（向用户复述）

- 输入：GitHub issue #208（FEAT：决策卡提交路径闭环），GitHub 已认证可用 → **GitHub 轨道**，需求源与发布去向均为 GitHub issue #208。
- 本任务为 **LOC-016 DT-01-ui-resume-attribution 裁决（2026-09-19 松哥：UI 续跑接在原运行上继续）的实施落地**，非全新需求从零分诊；LOC-016 片2「交互续跑」的提交路径由此闭环。
- 任务标识 FEAT-208 已按迁移后发号源（GitHub）取得（issue #208 即发号 issue）；本地任务卡与登记册双轨登记（仓库既有惯例），`GitHub 同步 = synced#208`。

## 分诊

- 分类：`enhancement`（补全已交付界面的提交闭环，非缺陷——受阻为当时裁决的如实呈现）。
- 冗余检查：运行详情决策卡/恢复卡呈现层由 FEAT-85 交付（已验收）；LOC-016 片2 的续跑实现仍开放；本任务不重复 LOC-016 片1 呈现层，仅接通提交路径。无历史拒绝记录（.out-of-scope 不存在）。
- 状态：定义材料齐备后 `已定义` + `ready-for-agent`。

## 关键决策（均已裁定，无未决）

| 决策 | 裁定 | 依据 |
|---|---|---|
| UI 续跑执行归属 | 接在原运行上继续（同 Logical Run 追加段/attempt） | 松哥 2026-09-19：需求目标未变更，变的只是工具与实现方式；快照版本记录承载变更（LOC-016 DT-01 关闭落档） |
| 提交通道 | 复用/扩展既有 `vwf.run.control`（新增续跑/决策动作与 initiator 归属字段），不另起新 RPC 体系 | 只影响内部施工方式，不改变用户行为与验收边界（skill §2.2）；立卡时「不改 RPC」的保守约束随裁决解除 |
| 无人值守 | 允许 | 产品语义已由 DT-01 裁定，无待人工代选项 |

## size 判定

- 路径清晰（归属已裁决、控制通道已存在、界面已交付）；影响面单一（运行详情决策卡/恢复卡 + 宿主控制动作扩展）；单执行会话可完成 → **sized-m**（触达运行控制语义，谨慎取 M）。

## 三要素

- **目标**：决策卡（WAITING_HUMAN）点选选项+理由提交后，原 Logical Run 追加段续跑；恢复卡（BLOCKED 改模型）同口径恢复；详情页与链路实时反映；provenance 如实记录 UI 触发。
- **范围**：运行详情决策卡/恢复卡的提交路径 + 宿主 `vwf.run.control` 动作扩展（增量、可加法）；**不做**：呈现层重构、执行引擎语义变更、LOC-016 片2 其余增强。
- **验收**：见 task-spec-V1 §15 V-1~V-6（真实含 WAITING_HUMAN/BLOCKED 的运行实测提交闭环 + 零回退 + 载荷不上调）。

## 缺口

无——产品语义已由 DT-01 裁定闭合，技术路径存在于既有控制通道。
