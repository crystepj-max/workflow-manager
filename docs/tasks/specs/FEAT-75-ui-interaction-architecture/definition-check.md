# Definition Check（FEAT-75 · 工作流插件 UI / 交互架构定稿）

> 模板：`docs/design/ai-task-define-delivery/definition-check.md`。
> 结论：**未通过**（9.3 失败，未决产品事项 = 4，含 1 项会话间冲突）→ 保持「定义中（待决策，含冲突）」，不进「待确认」。

## 检查元数据

| 项 | 值 |
|---|---|
| 任务名称 | 工作流插件 UI / 交互架构定稿（模板库、编辑器、运行入口与 Run 看板） |
| 任务标识 | `FEAT-75` |
| 拟确认基线版本 | V1（草案） |
| 检查人（Agent） | requirements-analysis 独立开工会话 |
| 检查时间 | 2026-09-19T12:37:16Z |
| 未决产品事项数 | **4**（要求为 0 → 本项不通过） |

## 9.1 目标与范围

- [x] 用户问题明确：用户如何发现/理解/配置/调用/干预运行/处理决策与阻塞/查看成果；权威材料此前只在 GitHub Issue 里
- [x] 需求目标明确：给出唯一、可施工、已裁定的 UI 架构定稿 + 逐条归属 + 未决事项成票
- [x] 非目标明确：零实现、不改源码、不重复已落地能力、不改契约与规格正文
- [x] 修改范围明确：仅新增 1 份设计文档 + 1 张任务卡 + 1 个规格目录（含 3 张决策票）
- [x] 不修改范围明确：`requirements-analysis.md` §「涉及范围-不做」逐条列明
- [x] 修改前状态明确：P1–P10 只在 #75 评论；`docs/design/` 查无 UI 权威；#54–#57 无裁定记录
- [x] 修改后状态明确：架构文档入库且可逐条检索；缺口 R-1…R-9 全部有归属；三票待裁

## 9.2 规则与边界

- [x] 主要业务规则明确：P1–P10 + 分层铁律（Outcome/Lifecycle/Completion 不混）已入架构文档 §2
- [x] 主要用户路径明确：发现（模板库）→ 配置（三层渐进）→ 调用（Skill/Chat，插件入口待定）→ 干预（PAUSED/Interrupt/Guidance）→ 决策（决策卡）→ 成果（三分组 + 快照 + 待建 Timeline）
- [x] 关键边界场景明确：内置只读不可绕过（含 JSON 页签）、额度耗尽保留原 Outcome、preset 与实际 schema 不符时的可信度边界、旧记录无事件字段时的退化呈现
- [x] 关键异常场景明确：BLOCKED 不静默换模型（P5）；无 producer 的枚举值不得做成空档筛选（C5）
- [x] 对已有功能的影响明确：本卡零实现，影响面 = 文档；对 LOC-016 / FEAT-208 的抢改风险已在 C3 显式避让
- [x] 已知风险明确：载荷余量仅 1.6KB（抬高所有 UI 切片成本）；浏览器侧能否唤起 Skill 会话未取证；卡号与 `CHORE-75` 数字重叠

## 9.3 决策完整性

- [ ] 所有会影响产品结果的人工决策均已完成 —— **未完成 4 项**
- [ ] 不存在"施工时再决定"的产品问题 —— S-1 / S-2 的选择器深度 / S-5 正是此类
- [ ] 未决产品事项数量为 0 —— **实际 4**

| 未决项 | 票 | 为何不能由 Agent 代裁 |
|---|---|---|
| **#75 保留推进 vs 降为需求源并关票**（并行会话 CHORE-113 与本轮前提互斥） | `decision-tickets/DT-00-issue-75-fate.md` | 决定本卡与本文档是否存续、#75 派工面边界、以及五方共用 `client.js` 改动面时谁避让；两方都自称产品决定，只能由产品经理重裁 |
| 并行会话记录的三条"产品（本会话）"裁定归属确认（OBSERVING 非目标 / Completion Mapping 不算门槛 / 先补能力再重排） | 同 DT-00 §4 | 若为真，DT-03 直接关闭、S-2 需排到 FEAT-114 之后；若不成立，CHORE-113 的 §12 要回退 |
| 插件是否新增运行入口（A/B/C） | `DT-01-plugin-run-entry.md` | 改变用户入口与产品定位，且关系到"禁止第二套 Runtime"的红线取舍 |
| Outcome Preset 呈现深度 | `DT-02-outcome-preset-surfacing.md` | 决定是否消耗贴线的载荷预算换配置便利，属投入/收益判断 |
| OBSERVING 呈现口径 | `DT-03-observing-presentation.md` | 决定 v0.1 是否兑现规格中的概念，或显式移出目标版本范围（**可能已由并行会话裁定**） |

## 9.4 任务组织

- [x] 已完成必要的功能切片拆分：S-0…S-5（S-3 本轮撤销，归并行会话的 FEAT-114）+ 三条"归他人"（FEAT-115 面板重排 / Timeline / 提交续跑），见卡 §功能切片
- [x] 每个子任务均可独立 UAT：S-2 纠偏（数据级）、S-4（看板只读字段）互不依赖且不依赖决策；S-1 / S-5 挂起至对应 DT 关闭
- [x] 前置依赖已明确填写"无"或具体任务：本卡 → DT-00（先决）；架构文档本身 → 无；S-1 → DT-01；S-2 选择器 → DT-02；S-5 → DT-03；Timeline → LOC-016 片1；提交续跑 → FEAT-208；面板四段 → FEAT-115
- [x] 优先级已确定：S-0 P1、S-2 纠偏 P1、S-4 P2、S-2 选择器 P2（S-3 已撤销归 FEAT-114）

## 9.5 验收

- [x] 每个主要功能有明确验收条件（卡 V-1…V-10）
- [x] 已能形成主要 UAT 场景：V-1/2/3/4/5/6/7/10 读文档即可判；V-8 原型现物走查；V-9 拆卡后各片 UAT
- [x] 验收标准可以通过真实操作判断（含 `git ls-files` / `grep -c` / 打开原型三类可执行判定）
- [x] **每条验收项均已声明执行时机**：裁决前可观测 = V-1…V-7、V-10；收口后观测 = V-8、V-9
- [x] **「收口后观测」项已写明执行者与回填位置**：V-8 由产品经理执行、结果回填 UAT 卡「收口后复核」区；V-9 由基线确认后的拆票会话执行、回填各施工卡与收口摘要；失败处置 = 如实记录并另立任务，不静默跳过

## 9.6 无人值守

- [x] 已明确「无人值守许可」= **不允许**
- [x] 若允许无人值守，不存在必须在施工中由人选择的产品问题 —— 前置条件不成立（已按"不允许"填写，与 9.3 一致，无互斥）

## 结论

- [ ] 全部通过 → 可改「待确认」
- [x] **未通过** → 保持「定义中（待决策，含冲突）」，列出缺口并继续决策/补全

### 未通过项

1. **9.3 决策完整性**：DT-00（#75 处置冲突，先决）、DT-01（运行入口 A/B/C）、DT-02（Preset 呈现深度）、DT-03（OBSERVING 口径，可能已由并行会话裁定）未裁。

### 已具备的条件（决策落地即可推进）

- 目标、范围、非目标、边界、风险、切片、验收七类要素均已闭合到"可施工"粒度；
- 12 条缺口逐条带 `文件:行号` 证据，并区分"已实现 / 部分实现 / 未实现"；
- #75 正文 14 项完成标准已给出全部归属，无悬空项（与并行会话判定不一致的项显式标"分歧待裁"）；
- 权威落库（S-0）已交付，"权威真空"这一条不依赖任何决策，现已消除；
- 即使 DT-01/02/03 全不裁，只要 DT-00 定了，**S-2 的 preset 数据纠偏与 S-4 的看板字段补齐就能立即进入基线确认**——这两片无产品取舍、不占决策位。

### 检查依据与限制说明

- 取证方式：只读核查（`grep` / `sed` / `git` / `gh`）。未运行 `npm test` / `npm run validate`，本卡零代码改动不构成验证前提；仓库 `validate` 自 2026-09-08 起存在与本卡无关的既有红项。
- 未取证项（如实标注）：① 插件浏览器侧能否唤起 Skill 会话（DT-01 选 B/C 的前置探路项）；② #54–#57 的**真机视觉**达成度（架构文档 §7 依据是票面事实 + 代码落点，非逐条目视复验）；③ CNB 期第 1–4 轮审查记录未取；④ `CHORE-113` §12 那三条"产品（本会话）"裁定是否出自产品经理本人。
- 登记册与看板：本卡已追加进 `registry.json`（revision 115→116，只追加一条，并行会话在途新增的 CHORE-113 / FEAT-114 / FEAT-115 / TMP-* 条目均原样保留），`BOARD.md` 由脚本重生成。**未在 GitHub 上回填评论或改动任何票的状态**。
