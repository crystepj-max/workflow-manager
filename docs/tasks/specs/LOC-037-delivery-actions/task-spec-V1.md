# LOC-037｜将收口事实整理与授权交付动作分离：任务规格 V1

> 完整候选基线，尚未获人工确认；当前不得启动开发。P0 的确认不覆盖本任务。

| 元数据 | 值 |
|---|---|
| 任务标识 | LOC-037 |
| 需求来源 | 本地文档 |
| 来源定位 | docs/research/workflow-review-requirements-2026-09-14/WR-014.md |
| 任务名称 | 将收口事实整理与授权交付动作分离 |
| 任务类型 | 完整功能开发 |
| 分类 | enhancement |
| 体量 | M |
| 优先级 | P1 |
| 当前状态 | 定义中 |
| 需求基线版本 | V1 |
| 前置依赖 | LOC-032 |
| 施工环境组 | LOC-037 |
| 施工环境角色 | 独立 |
| 无人值守许可 | 不允许 |
| 任务规格位置 | docs/tasks/specs/LOC-037-delivery-actions/task-spec-V1.md |
| 定义时间 | 待人工确认；材料编制于 2026-09-15T02:47:39.403Z |
| GitHub 同步 | pending |

## 1. 需求背景

来源：复杂度表：收口职责过宽。closeout 角色同时承担事实整理、GitHub 操作和清理，指令仍出现固定 origin/gh；动作必要性和完成事实混在一起。

原评审基线为 `8e38d74955c67f0f196c93eb880cfef7cce71cbd`；本轮实际检查 `775ac84e9c37e2278c2ee32a0f2bcb75cbbb8172`，main 另有 LOC-023 状态文档提交。评审建议、当前实现和本 V1 目标分别记录，不混用。

相关参考资料：

- [WR-014 原始需求](../../../research/workflow-review-requirements-2026-09-14/WR-014.md)
- [系统评审](../../../research/workflow-review-requirements-2026-09-14/evidence/workflow-design-review-2026-09-14.md)
- [本轮现状核验与边界](../../p1-definition-evidence-2026-09-15.md)
- [dsh/roles/closeout.md](../../../../dsh/roles/closeout.md)
- [templates/wf-optimize.json](../../../../templates/wf-optimize.json)
- [templates/wf-diagnose.json](../../../../templates/wf-diagnose.json)
- [templates/wf-construction-full-feature.json](../../../../templates/wf-construction-full-feature.json)
- [packages/dsh-visual-workflow/src/host.js](../../../../packages/dsh-visual-workflow/src/host.js)
- [scripts/cwf-env-recycle.mjs](../../../../scripts/cwf-env-recycle.mjs)

## 2. 用户问题

非 Git 任务也被要求合并，收口可能擅自改成果或将失败发布说成完成，用户无法判断到底交付了什么。

## 3. 目标

用户先得到完整交付事实，再按已授权的必要动作完成交付；外部动作失败和可选清理失败被准确区分。

可观察完成标准为 §15 的四项 AC 和 §16 四组操作场景，逐项提供真实结果；实现、机器验证与人工验收分别记录。

## 4. 非目标

本期不增加 CNB 运行时发布适配，不改人工验收关口，不自动决定发布目标，不强删分支或工作区。

## 5. 修改前

closeout 角色同时承担事实整理、GitHub 操作和清理，指令仍出现固定 origin/gh；动作必要性和完成事实混在一起。

## 6. 修改后

用户先得到完整交付事实，再按已授权的必要动作完成交付；外部动作失败和可选清理失败被准确区分。 交付动作选择、收口报告、必要外部动作状态与环境清理结果。

## 7. 功能范围

事实报告、动作计划、授权引用、local/GitHub 适配、必要动作完成判定和安全资源回收。

关联模块：

- [dsh/roles/closeout.md](../../../../dsh/roles/closeout.md)
- [templates/wf-optimize.json](../../../../templates/wf-optimize.json)
- [templates/wf-diagnose.json](../../../../templates/wf-diagnose.json)
- [templates/wf-construction-full-feature.json](../../../../templates/wf-construction-full-feature.json)
- [packages/dsh-visual-workflow/src/host.js](../../../../packages/dsh-visual-workflow/src/host.js)
- [scripts/cwf-env-recycle.mjs](../../../../scripts/cwf-env-recycle.mjs)

影响对象：交付动作选择、收口报告、必要外部动作状态与环境清理结果。 优先级 P1：在错误放行类 P0 后补齐可靠协作、维护与运行保障；无硬前置者不必等待全部 P0。

## 8. 不修改范围

本期不增加 CNB 运行时发布适配，不改人工验收关口，不自动决定发布目标，不强删分支或工作区。 不修改其他任务的基线和验收结论，不迁移用户凭据，不安装全局角色副本。

## 9. 业务规则

1. 事实整理是只读候选阶段，报告列候选版本、证明、人工决定和未完成项。发现必须改候选时退回修改及重新证明，不在 closeout 偷改。非 Git 默认 required_actions=[]，可完成纯本地交付。
2. 动作计划字段 required_actions、optional_actions、authorization_ref、target_adapter、target_ref、candidate_ref；由已批准交付范围及仓库配置确定。每个动作执行前验证授权对象、范围、版本与目标匹配，有效授权复用。缺必要授权保留报告并等待，不重复整理或索要已有授权。
3. V1 支持 local/no-op 和已有 GitHub 动作路径；选择默认推送目标必须先读 remote.pushDefault/分支上游及显式目标，不固定 origin。目标 CNB 或未知适配器时 capability_unavailable，不回退发布到 GitHub。CNB 扩展单独定义。
4. 执行调用 LOC-032 的 operation ledger execute-or-reconcile。必要动作失败/结果不确定时不能 DELIVERED；不确定先回读对账，不盲目重试。成功必须有远端对象和候选版本回读证据。
5. 可选清理失败只记录 cleanup_pending，交付事实不倒退。待保全脏改动、其他活跃 Run、同组未完成成员禁止回收；共享开发 DSH 仅回收本 Run namespace/resource，不停止整个共享宿主。
6. GitHub PR 仍遵守项目 Controller Review Gate、人工验收和已有发布授权政策，本任务不通过新动作接口绕过这些规则。

输入输出和归属：消费 LOC-032 的 operation_key、授权范围、pending/succeeded/failed/unknown 与外部结果引用；输出 delivery_report、逐动作状态、cleanup_pending。新动作规划归本条，重复执行保障归 LOC-032。

## 10. 用户操作路径

1. 执行者读取任务卡与本规格，确认 V1 已获批准；记录当前分支/HEAD 并核对相对本轮检查基线的变化。
2. 核验 §14 的每项前置已交付并合入当前基线，未满足则保持执行受阻，不拿草稿接口代替。
3. 按 §7–9 实现本条完整能力，以 §16 的固定场景验证成功和失败路径。
4. 呈递逐项 AC、实际证据、候选版本与限制，到人工验收关口等待；不代签或自行进入发布。

## 11. 异常和边界场景

- **非 Git 本地交付**：准备非 Git 文档目录，required_actions 为空且事实报告齐全。 处理结果必须为：可交付，Git/PR 调用为零，报告包含正确候选和限制。
- **授权及远端回读**：在隔离的 GitHub 测试仓库或受控 API 替身，准备有效/缺失授权两组动作。 处理结果必须为：有效授权不重问且有回读；缺失组已有报告但未执行必要动作。真实远端未授权时该层证据标 UNVERIFIED。
- **失败分类与目标**：准备必要 merge 失败、可选清理失败、目标 CNB 三种场景。 处理结果必须为：必要失败不是 DELIVERED；仅可选失败可交付并列残留；CNB 报能力缺失且不调用 GitHub。
- **成果和资源保全**：记录候选摘要；准备含未提交改动的工作区及同组活跃成员。 处理结果必须为：候选未改，脏改动/活跃成员未删除，其他 Run 和共享 DSH 保持可用。

共同恢复规则：证据不足写 BLOCKED/UNVERIFIED 并保留失败输入、恢复位置；同一原因重试不改变基线，不因无人值守降低验收。旧 Run 使用冻结快照；范围外变化另行记录，不静默迁移历史。

## 12. 已确认的关键决策及原因

已确认事实仅为用户本轮要求“参照 P0 流程完成 P1 分析”；以下产品规则与施工许可是待确认提案，不能填写用户已批准。

| 决策主题 | 拟选方案 | 原因 | 确认人 | 确认时间 |
|---|---|---|---|---|
| D-LOC-037：V1 产品规则及范围 | 本期适配 local 和已有 GitHub；CNB 目标明确阻断、不另投 GitHub；发布仍需已有有效授权及质量门。 | 非 Git 任务也被要求合并，收口可能擅自改成果或将失败发布说成完成，用户无法判断到底交付了什么。 | 待用户确认 | 未确认 |
| D-BATCH：环境与施工许可 | 独立环境组 LOC-037，按真实依赖分阶段；批准后允许至人工验收关口，本轮不启动 | 可隔离施工，保留真实人工关口 | 待用户确认 | 未确认 |

未决产品事项：2（本条 V1 选择及共享环境/许可；本批共享项只需确认一次）。没有留给施工中临时决定的产品分支。完整确认对象见 [本批确认单](../../p1-definition-confirmation-2026-09-15.md)。

## 13. 功能切片关系

- 本切片：用户先得到完整交付事实，再按已授权的必要动作完成交付；外部动作失败和可选清理失败被准确区分。
- 体量 M；规则、实现和验证围绕同一用户能力，§16 可独立 UAT，不按内部接口层拆成无法单独验收的任务。
- 兄弟条目负责其他领域能力；共改 host/generate/templates 只构成合并冲突风险，不据此新增业务依赖。

## 14. 前置依赖说明

```text
前置依赖：LOC-032
```

LOC-032 的外部动作幂等与恢复对账必须完成后才能正确接入必要动作；只做本地报告不足以满足本需求的完整验收。

前置规格：[LOC-032 V1](../operation-idempotency/task-spec-V1.md)。

环境安排（待确认）：独立工作区和环境组 `LOC-037`，开发分支默认 `codex/loc-037-delivery-actions`。不加入 P0 已批准的环境组，也不复用其待验收候选。对于跨组依赖，由执行者先从登记册核对真实合入 commit，再验证 `git merge-base --is-ancestor <前置合入commit> HEAD`；缺证据一律受阻。当前单任务独立环境规划器不自动核验这类跨组依赖，不能仅凭其返回 create 就放行。

启动前读取当前仓库 AGENTS.md、单任务 runbook 和环境登记；Node.js 24，首次缺依赖时 npm install。建设 Run 的 DSH 资源按当前环境分配器实际记录使用；本批拟采用当前 runbook 的共享宿主/独立 Run 命名空间；与本轮 AGENTS 的独占 Home 要求存在冲突，须经 D-BATCH 明确确认后生效，不能自行覆盖。此前不操作 DSH；不复制旧任务 Home、不重启其他 Run 的 DSH。每任务 namespace/cache 与资源记录独立；开发 DSH 固定端口共享且同时只有一个活跃插件。执行前检查槽位；其他任务活跃时等待或受阻，不调用会抢占重启的 start。候选默认并发 1，凭据只引用已配置 Profile，不自动复制。

当前批量工具只接纳无依赖任务；本任务必须在前置合入后经单任务入口分阶段启动，即使依赖已完成也不应直接声称现有批量工具能调度。 本轮不创建 Run 或开发环境。

## 15. 验收条件

- [ ] AC-01：非 Git 优化正常收口，无 Git/PR 调用；已授权 GitHub 交付通过动作接口执行且回读确认。
- [ ] AC-02：缺必要授权时先完成可交付材料并准确等待该动作授权；既有有效授权不重复询问。
- [ ] AC-03：必须合并却合并失败时不得 DELIVERED；可选清理失败保留成果状态并列出待清理资源。
- [ ] AC-04：收口前后候选内容一致；含未保全改动的工作区不被强删；同组环境遵守全部成员完成后回收。

对源条目歧义的收敛以 §9 明文规则为 V1 提案，须经 §12 确认。交付必须逐条填写 AC 实际结果与证据，不得用一个总 PASS 替代。

## 16. UAT 场景

### UAT-01 非 Git 本地交付

- 对应：AC-01。
- 验收目的：检查非 Git 本地交付的用户可见行为。
- 前置条件：准备非 Git 文档目录，required_actions 为空且事实报告齐全。
- 操作步骤：运行收口并记录所有工具调用。
- 预期结果：可交付，Git/PR 调用为零，报告包含正确候选和限制。
- 凭证：保存本次输入、命令或操作、实际输出与关联 Run/候选标识；将预期和实际逐项比较，不只截取最终状态。
- 人工关注：结果可读、原因准确、未执行层没有被标为通过。

### UAT-02 授权及远端回读

- 对应：AC-02。
- 验收目的：检查授权及远端回读的用户可见行为。
- 前置条件：在隔离的 GitHub 测试仓库或受控 API 替身，准备有效/缺失授权两组动作。
- 操作步骤：分别执行；有效组在返回后回读远端对象与 HEAD。
- 预期结果：有效授权不重问且有回读；缺失组已有报告但未执行必要动作。真实远端未授权时该层证据标 UNVERIFIED。
- 凭证：保存本次输入、命令或操作、实际输出与关联 Run/候选标识；将预期和实际逐项比较，不只截取最终状态。
- 人工关注：结果可读、原因准确、未执行层没有被标为通过。

### UAT-03 失败分类与目标

- 对应：AC-03。
- 验收目的：检查失败分类与目标的用户可见行为。
- 前置条件：准备必要 merge 失败、可选清理失败、目标 CNB 三种场景。
- 操作步骤：逐一收口并检查状态。
- 预期结果：必要失败不是 DELIVERED；仅可选失败可交付并列残留；CNB 报能力缺失且不调用 GitHub。
- 凭证：保存本次输入、命令或操作、实际输出与关联 Run/候选标识；将预期和实际逐项比较，不只截取最终状态。
- 人工关注：结果可读、原因准确、未执行层没有被标为通过。

### UAT-04 成果和资源保全

- 对应：AC-04。
- 验收目的：检查成果和资源保全的用户可见行为。
- 前置条件：记录候选摘要；准备含未提交改动的工作区及同组活跃成员。
- 操作步骤：执行事实整理和回收后比对候选、文件和其他 Run。
- 预期结果：候选未改，脏改动/活跃成员未删除，其他 Run 和共享 DSH 保持可用。
- 凭证：保存本次输入、命令或操作、实际输出与关联 Run/候选标识；将预期和实际逐项比较，不只截取最终状态。
- 人工关注：结果可读、原因准确、未执行层没有被标为通过。

验证安排：先建立能捕捉实际故障的相关检查，再修改范围内实现。蓝图/生成规则变化运行 `npm run generate` 和 `npm run validate`；其他改动运行相邻模块必要测试。仅文档变化检查事实、链接与生成一致性。发布所需 `npm run release:verify`、产品 DSH 重启与真实 E2E 按原项目关口执行，机器通过不等于发布或人工验收。

## 17. 风险

当前仓库默认使用 CNB，因此本条 V1 不保证本项目可自动远端收口；本地交付可用，CNB 适配属于后续范围，必须由用户接受这个边界。

并行变更触及共享文件时合并后重跑直接相关检查；如改变已确认用户行为，先提出差异版，保留其他既有批准，不自行扩写基线。

## 18. 已知限制

本期不增加 CNB 运行时发布适配，不改人工验收关口，不自动决定发布目标，不强删分支或工作区。 本轮仅定义材料，未实现本条、未运行本条目标 UAT。GitHub 当前访问 403，正式载具为本地卡片与登记册；CNB 只可能承载后续 Git 同步，本轮没有上传。文件均落在可纳入 Git 的 docs 路径，但未提交前不能声称主分支/远端已可获取。

## 19. 版本历史

| 版本 | 日期 | 变更摘要 | 确认人 |
|---|---|---|---|
| V1 候选 | 2026-09-15 | 从 WR-014 收敛范围、边界、依赖、四项 AC 与四组 UAT，等待批准 | 未确认 |
