# Definition Check · 片 1 handoff schema 深度加固

> 进入「待确认」前逐项勾选；全部通过后才可请求人工确认基线。人工确认后才落档为「已定义」。任一未勾选或未决产品事项 > 0 → 禁止标为「已定义」。

## 检查元数据

| 项 | 值 |
|---|---|
| 任务名称 | handoff 交接包 schema 深度加固：版本号格式与内容文本下限 |
| 拟确认基线版本 | V1 |
| 检查人（Agent） | 独立开工会话（#123 余项） |
| 检查时间 | 2026-09-19 |
| 未决产品事项数 | **0** |

## 9.1 目标与范围

- [x] 用户问题明确——空壳证据与非法版本号可顺利落盘
- [x] 需求目标明确——落盘/校验阶段即拒，且有测试固定
- [x] 非目标明确——不动 assembled 必填结构、不做时间序、不动引擎判定
- [x] 修改范围明确——`definitions` + `baseline_revision` + 内容字段 + `record_version` + 7 示例 + 契约 + 3 测试文件夹具
- [x] 不修改范围明确——`nonEmptyText`/`isoDateTime`/枚举字段/记录类型/runbook

## 9.2 现状证据（本会话实测，非转录）

- [x] `baseline_revision` 裸 string 已定位到行（schema:201）
- [x] `minLength` 计数 0、`format` 计数 0、`pattern` 计数 2 —— 命令实测
- [x] 校验器支持 `minLength`/`pattern`、不支持 `format` —— 读 `cwf-validate.mjs:63-76` 确认，故约束会真生效
- [x] 误伤面量化：`.agent-runs/` 259 条真实内容字段最短 35 字符（零误伤）；`baseline_revision` 实际取值集合 `{V1,V2,V3}`；示例为 `v1`
- [x] 夹具面量化：3 个测试文件存在 `summary:'s'`/`rationale:'r'`/`element:'e'` 等单字符占位
- [x] 测试基线：`cwf-record`/`cwf-evidence-verify`/`cwf-validate`/`schema-protocol` 四文件 60 用例当前全绿（2026-09-19 实测）

## 9.3 验收标准

- [x] 每条验收可自动或人工复现（V-1~V-9 均对应命令或断言）
- [x] 无「正常/合理」等模糊表述
- [x] 每条新约束要求「修复前先红」的负例先行

## 9.4 依赖与前置

- [x] 前置依赖：无（与片 2 代码面不重叠）
- [x] 既有通道确认存在：`cwf-record` 写入即调 `validateRecord`，无需新接线
- [x] 契约 §8.4 ajv-cli 通道确认存在且可跑

## 9.5 风险与可逆性

- [x] 风险与处置已列（同文件并发、下限过窄、record_version 前移波及旧记录校验入口、夹具改动量）
- [x] 可逆性：schema/契约/夹具均为纯文本改动，可整片回退；无数据迁移、无对外动作
- [x] 已显式标注唯一需要留意的连带项：`acceptance-construction-epic.md` 的字面校验路径

## 9.6 无人值守许可

- [x] 允许。加固强度、版本号形态、时间序归属、与 CHORE-110 先后，四项产品口径已由松哥 2026-09-19 裁定（规格 §12），施工中无需再向人求决。

## 9.7 与既有契约一致性

- [x] §8.3 语义不冲突：本票只把「非空」收紧为「成形态」，不改任何判定条目
- [x] §8.4 版本约定遵守：patch 级前移 `record_version`、不前移示例链 HEAD 绑定
- [x] §7.3 proof 绑定不受影响：`verified_head`/`verified_branch` 走 `nonEmptyText`，本票不动其定义（有断言固定）
- [x] 引擎⑧全等比较已纳入（E-3：两处下限必须一致）

## 9.8 未决事项

无未决产品事项。

> 非产品口径的待办（不阻塞定义门）：正式号已于 2026-09-21 经松哥授权在 GitHub 补建为 #219 并回填登记册；规格 §18 保留了草稿号沿革。

## 结论

- [x] **全部通过** → 状态可改为「待确认」，呈递人工确认基线
