# Definition Check · FEAT-237 批量任务源接入 GitHub

> 检查对象：`docs/tasks/specs/FEAT-237-github-task-source-claim/task-spec-V1.md`（V1）
> 检查日期：2026-09-20 ｜ 执行人：施工会话（克里斯）

## 1. 目标与范围

- [x] 任务目标可用一句话说清，且与远端 issue #237 标题一致（批量任务源接入 GitHub：ready-for-agent 筛选 + 施工中认领互斥）
- [x] 「做」的清单落到文件级（§5.5 五处脚本 + 文档 + 三个单测文件），无「优化一下」这类不可验收项
- [x] 「不做」的清单明确排除发号链路、收口动作路径、体量标签自动判定、自动接管认领
- [x] 交付物全部可落库（`docs/tasks/` 与 `scripts/`），无依赖 `.scratch/` 的交付物

## 2. 规则边界

- [x] 两条标签语义各自唯一，无重叠：`ready-for-agent` = 可施工信号；`施工中` = 认领信号
- [x] 谁写、何时摘、谁读三问均已在 §5.1 表格回答
- [x] 标签取值不做自然语言扩展，标签名与语义一一对应
- [x] 不修改机器本地 git 配置、不引入新的认证方式

## 3. 决策完整性

- [x] 六个关键决策（D-01～D-06）已逐条给出选择与原因
- [x] 每个决策都给了被否决方案及否决理由（交集 vs 并集、block vs local-only、门禁前 vs 门禁后补标、默认放行 vs 严格、身份来源、是否自动接管）
- [x] 未决产品事项为 0，无留给施工阶段拍的开放问题

## 4. 任务组织

- [x] 单任务体量：五处脚本改动集中在同一链路（任务源 → 准入 → 认领），可单会话完成
- [x] 施工环境角色为「独立」，无同组串行依赖
- [x] 前置依赖为「无」：CHORE-111 已合并（PR #229 在 `origin/main`），`gh` 通道与锚点解析可直接复用
- [x] 与并行任务的文件重叠已确认：`local-task-merge.mjs` 仅改一行 import + re-export，与 FIX-109 的改动区域不同

## 5. 验收可判定性

- [x] 七条验收标准每条都有可执行判据（单测名、退出码、报告字段、真机链路）
- [x] 「无新增失败」给了可比对的判定方法（干净基线 vs 改动树逐条对比失败清单），不是「跑一遍看着绿」
- [x] 远端不可信、无锚点、并发认领三类边界场景各有明确预期（§10）

## 6. 无人值守

- [x] `无人值守许可 = 允许`：改动全部落在仓库脚本、机器本地配置与单测内
- [x] 对外动作（打标签、评论、指派、摘标签）均发生在已授权的 GitHub 主源仓库，且全部可撤销
- [x] 无「必须施工中由人取舍」的决策点；僵尸认领这类运维动作不属施工路径

## 7. 遗留与风险

- [x] 五条风险各有处置，且处置都可机械执行（补标失败即排除、阻断即报错、并发即让位）
- [x] 两条已知限制如实标注（`施工中` 不能表达认领人存活；M2 若绕过 `cwf-run-init` 则不认领）

## 结论

Definition Check 通过：未决产品事项 = 0，七条验收标准均可判定，六条关键决策已裁定。

| 项 | 结果 |
|---|---|
| 未决产品事项 | 0 |
| 需要人工裁定的开放问题 | 无 |
| 落档状态 | 登记册 `本地已定义`；GitHub issue #237 打 `ready-for-agent` |
