# Definition Check · CHORE-111 任务编号发号源从 CNB 迁移到 GitHub

> 进入「待确认」前逐项勾选；全部通过后才可请求人工确认基线。人工确认后才可落档为「已定义」。任一未勾选或未决产品事项 > 0 → 禁止标为「已定义」。

---

## 检查元数据

| 项 | 值 |
|---|---|
| 任务名称 | 任务编号发号源从 CNB 迁移到 GitHub（编号连续性 + 历史号段冲突处置） |
| 拟确认基线版本 | V1（复核修订版） |
| 检查人（Agent） | Qoder（复核轮，2026-09-19 14:00Z） |
| 首轮检查 | WorkBuddy（克里斯）2026-09-19 |
| 未决产品事项数 | **0**（D-01 于首轮前裁定；复核轮新发现 4 项已于 2026-09-19 由松哥裁定为 D-02～D-05） |

---

## 复核轮结论先行

首轮检查（2026-09-19 上午）记「全部通过、未决 0」，但复核轮独立查证后发现 **4 处实质缺口**，均属范围或验收定义不足，已提交松哥裁定并写回规格 §3 / §8 / §10：

| 复核新发现 | 首轮为何漏 | 处置 |
|---|---|---|
| 验收标准 4 要求门禁全绿，实测当前已红（3 项并行会话 `spec-untracked`） | 首轮只核「标准是否可复现」，未实跑门禁 | 改为「相对基线不新增失败」+ 记录基线清单（D-02） |
| 迁移引入功能断裂：`local-task-merge` 严格正则匹配不上 8 条不规范 `remote` → 收口静默不提示关 issue；平台名反查 git 远端（`github` vs 实际 `origin`）取不到 | 首轮把 §3 第 6 项当作纯文案改动 | 解析层纳入本票（D-03） |
| `delivery-closeout-host.mjs` 是一整套 CNB 适配器，`pushDefault=cnb` 使收口目标解析为 **CNB**（向灾备镜像执行对外动作）；首轮只列 L25/L26 白名单 | 首轮未读 L215–228 目标解析与动作实现 | 实现最小 GitHub 适配器（D-04） |
| 指导执行者操作的文档仍写 CNB 发号：`docs/tasks/README.md` L16/L27/L47、`dsh/skills/requirements-analysis/SKILL.md` L262、`dsh/roles/closeout.md` L18；另漏 `ai-task-dispatcher.mjs` L32 | 首轮范围只覆盖 `scripts/` | 操作真源 3 处纳入，历史设计与 wayfinder 记录留后续（D-05） |

同时更正两处**错误事实**：

- 任务卡「关键证据」记 GitHub 分支保护**已撤销**（依据 `branches/main/protection` 返回 404）→ 实为**仍受保护**：repository ruleset `main protect`（id `21234444`，`enforcement=active`）承载，protection API 404 不代表撤销。**这条若不更正，执行者可能直推主干，绕过 PR 闸门。**
- 任务卡记「71 个任务，`remote` 全为 `cnb#N`」→ 实测 77 条，其中 8 条为双锚点或空格式。

## 9.1 目标与范围

- [x] 用户问题明确——主源已是 GitHub，发号源与收口目标却仍指向 CNB，且工具层不报错
- [x] 需求目标明确——发号源迁到 GitHub 且保留「远端发号、禁止本机自增」的防撞号属性
- [x] 非目标明确——§5 列 8 项，含「不改 `git config`」「不规范化 8 条历史 `remote` 值」「不改写历史设计文档」
- [x] 修改范围明确——§3.1～§3.4 共 15 处，逐条给出文件、函数、行号与迁移后形态（行号 2026-09-19 逐条读取源码核对）
- [x] 不修改范围明确——§5 全部；文档只纳入操作真源 3 处（D-05）

## 9.2 现状证据

- [x] 修改前现状已实测并记录（`gh auth status` scope、ruleset 实查、`git remote -v` 三远端、`remote.pushDefault=cnb`、登记册 77 条分布、8 条不规范取值逐条列出）
- [x] 存在可复现的冲突现象（GitHub #108/#109/#110 复用结果已 `gh issue view` 逐条复核标题/状态/标签；#106 确认未动）
- [x] 根因已定位到代码级（`resolveRemoteSlug` 只读 cnb 远端、`remoteAllocate` 调 cnb CLI、`resolveAdapter` 目标解析优先级、`local-task-merge` 严格正则）

## 9.3 验收标准

- [x] §8 共 8 条，每条可自动或人工复现（实跑 `allocate`、`gh issue view` 核对、`git diff` 断言零改动、只读调用目标解析、`--dry-run` 读 `pendingManualClose`、文档 grep）
- [x] 门禁类标准以「相对基线不新增失败」表述，不含不可达条件（D-02）
- [x] 不含模糊表述（「正常」「合理」等）

## 9.4 依赖与前置

- [x] 前置依赖：无（`gh` 已登录且具备 `repo` scope，实测通过）
- [x] 同文件冲突已处置：与 `FIX-109` 同改 `local-task-registry.mjs` / `local-task-merge.mjs`，两者均未开工 → 不升级为硬依赖，§9 写明冲突基线约定；与 `CHORE-110` 无文件重叠
- [x] 依赖的既有通道已确认存在（`gh` CLI、`scripts/test/local-task-registry.test.mjs` 既有发号测试夹具、镜像 workflow 已上线）

## 9.5 风险与可逆性

- [x] 已列 6 项风险与处置（收口指向灾备镜像、号段跳变、误改历史字段、双源并存撞号、静默降级、GitHub 适配器缺真实验证）
- [x] 改动可逆：脚本与文档改动回退即恢复；D-01 已执行的 GitHub issue 改名/重开亦可回退；本票不改 git config、不动历史数据

## 9.6 无人值守许可

- [x] 依据明确（D-06）——改动落在仓库脚本、文档与单测；对外动作限于已授权的 GitHub 主源仓库且可回退
- [x] 例外已标注：GitHub 收口适配器为新建路径，**首次真实收口须人工在场**，不列入无人值守范围（§13）

## 9.7 与既有契约一致性

- [x] 不改变编号契约语义：`<TYPE>-<号>` 仍由远端服务端分配，仅换平台
- [x] 与 CHORE-106 已落地的 cnb 适配器并存，不覆盖其行为（新增 GitHub 路径，禁止回落 CNB）
- [x] 与 `dsh/roles/closeout.md` 的「不回落发布到 CNB」约定一致（§3.4 第 15 项同步更新该文档）
- [x] 状态词汇已说清：登记册机器取值 `本地已定义` 与 issue 对外口径「已定义」指同一状态（§14），不新造第二套定义入口
- [x] 不覆盖长期原则与版本目标规格

## 9.8 未决事项

- [x] **0 项**。D-01（历史号段处置，A · 复用占位号对齐，已执行并复核）+ 复核轮 D-02～D-05（门禁口径 / 解析点归属 / 适配器深度 / 文档范围）均已由松哥 2026-09-19 裁定并写回规格 §10。

---

## 结论

定义门各项**全部通过**，未决产品事项数为 **0**。

基线 V1（复核修订版）已于 **2026-09-19T14:08:57Z 由松哥确认并授权开工实施**。本票落档为「已定义」：登记册写 `本地已定义`（机器词汇），issue #215 对外口径写「已定义」并打 `ready-for-agent` + `sized-m`。

> 当前状态：`本地已定义`（定义阶段结束，进入施工）。
