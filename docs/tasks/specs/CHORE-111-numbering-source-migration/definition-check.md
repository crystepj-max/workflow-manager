# Definition Check · CHORE-111 任务编号发号源从 CNB 迁移到 GitHub

> 进入「待确认」前逐项勾选；全部通过后才可请求人工确认基线。人工确认后才可落档为「已定义」。任一未勾选或未决产品事项 > 0 → 禁止标为「已定义」。

---

## 检查元数据

| 项 | 值 |
|---|---|
| 任务名称 | 任务编号发号源从 CNB 迁移到 GitHub（编号连续性 + 历史号段冲突处置） |
| 拟确认基线版本 | V1 |
| 检查人（Agent） | WorkBuddy（克里斯） |
| 检查时间 | 2026-09-19 |
| 未决产品事项数 | **1**（历史号段冲突处置口径，见 `decision-tickets/DT-01-numbering-collision.md`） |

---

## 9.1 目标与范围

- [x] 用户问题明确——主源已是 GitHub，发号源却仍绑 CNB，出现口径分离
- [x] 需求目标明确——发号源迁到 GitHub 且保留「远端发号、禁止自增」的防撞号属性
- [x] 非目标明确——不改历史任务号、不改历史 `remote` 字段、不迁移既有 issue 内容、不动 UI 与镜像 workflow
- [x] 修改范围明确——§3 表中 9 处改造点已逐条给出文件与行号
- [x] 不修改范围明确——已列明 6 类不动项

## 9.2 现状证据

- [x] 修改前现状已实测并记录（`gh auth status`、分支保护 404、镜像 workflow 运行 success、registry 71 任务分布）
- [x] 存在可复现的冲突现象（GitHub #109/#110 与本地 `FIX-109`/`CHORE-110` 撞号，已逐条 `gh issue view` 核实）
- [x] 根因已定位到代码级（`resolveRemoteSlug` 只读 cnb 远端、`remoteAllocate` 调 cnb CLI）

## 9.3 验收标准

- [x] 每条验收标准可被自动或人工复现（`allocate` 后查 `task_id`/`remote`、`gh issue view` 核对、`git diff` 断言既有字段零改动、门禁跑通）
- [x] 验收标准不含模糊表述（「正常」「合理」等）

## 9.4 依赖与前置

- [x] 前置依赖为空（`gh` 已登录且具备 `repo` scope，实测通过）
- [x] 依赖的既有通道已确认存在（`gh` CLI、`local-task-registry.mjs`、既有测试文件、镜像 workflow 已上线）

## 9.5 风险与可逆性

- [x] 已列风险与处置（号段跳变、误改历史字段、双源并存期撞号、`gh` 未登录静默降级）
- [x] 改动可逆（脚本内改动，回退即恢复；选 A 时对 GitHub 的两条 `edit`/`reopen` 亦可回退）

## 9.6 无人值守许可

- [x] 依据明确——迁移点全部落在仓库脚本内部，无外部不可逆动作；不需人取舍的部分（发号链路、文案、适配器）已写明口径

## 9.7 与既有契约一致性

- [x] 不改变编号契约语义：`<TYPE>-<号>` 仍由远端服务端分配，仅换平台
- [x] 与 CHORE-106 已落地的 cnb 适配器并存，不覆盖其行为（`delivery-closeout-host.mjs` 增 GitHub 适配器而非替换）
- [x] 不覆盖长期原则与版本目标规格

## 9.8 未决事项

- [ ] **未决 1 项**：GitHub 历史号段 #106/#108/#109/#110 的处置口径（A 复用占位号对齐 / B 不复用保留 CNB 号 / C 一次性重编），详见 `decision-tickets/DT-01-numbering-collision.md`。

> GitHub issue 编号**不可回收**是硬约束，故「删除占位号腾位」不成立；该项属产品口径取舍，不由实施方单方决定。

---

## 结论

定义门各项除「9.8 未决事项」外全部通过。未决产品事项数 = 1 > 0 → **本票暂不得标为「本地已定义」**，状态维持 `定义中`。

待 `DT-01` 裁定后：本票关闭决策票、按裁定结果落定规格 §4.3，定义门可过，本票转入「待确认」等待人工确认基线 V1。

> 当前状态：`定义中`（本次仅落档卡与规格，尚未请求基线确认；`DT-01` 阻塞开工）。
