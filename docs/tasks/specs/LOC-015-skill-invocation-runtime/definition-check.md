# Definition Check · LOC-015

| 项 | 值 |
|---|---|
| 任务名称 | Skill 调用入口接入 Logical Run Runtime |
| 任务标识 | `LOC-015` |
| 拟确认基线版本 | **V2** |
| 检查人（Agent） | ZCode（requirements-analysis） |
| 检查时间 | 2026-09-11 |
| 未决产品事项数 | **0** |
| 结论 | **全部通过** → 状态改为「待确认」，呈递人工确认基线 V2 |

> 规格：`docs/tasks/specs/LOC-015-skill-invocation-runtime/task-spec-V3.md`（V3 仅改写验收第 1 条验证归属，定义结论不变）
> V1 检查结论（未通过：9.3 / 9.6）见本文件「附录 · V1 未通过记录」。

---

## 9.1 目标与范围

- [x] 用户问题明确 —— DSH 内 Skill 触发的运行在看板上记录残缺（无任务归属、单段、完成类型为空、不可续跑）
- [x] 需求目标明确 —— Skill / Chat 入口与插件入口共享同一 Logical Run Runtime（规格 §2.3）
- [x] 非目标明确 —— 不建第二套 Runtime、不改 `wf_run` 语义、不新增工具面、非 DSH 执行器接入不在本期（规格 §4）
- [x] 修改范围明确 —— 两份 runbook 口径 + Current 侧文档口径 + 真实验证（规格 §7）
- [x] 不修改范围明确 —— 运行语义/数据契约/RPC/客户端 UI/产品规格正文（规格 §8）
- [x] 修改前状态明确 —— runbook 指引走内置 `workflow` 工具，插件旁观得退化摘要
- [x] 修改后状态明确 —— runbook 首选 `wf_run`，插件为发起方，记录完整；内置工具为回退且如实提示

## 9.2 规则与边界

- [x] 主要业务规则明确 —— 正式入口唯一 / 回退不静默 / 续跑参数单一来源 / 生成物不可手改（规格 §9）
- [x] 主要用户路径明确 —— 触发词 → 装配 args → `wf_run` → 门禁裁决 → 续跑同一逻辑运行 → `DONE`（规格 §10）
- [x] 关键边界场景明确 —— 同 `taskId` 互斥；`wf_run` 不可用（规格 §11）
- [x] 关键异常场景明确 —— 回退分支须如实提示；非 DSH 环境触发需停止说明（规格 §11）
- [x] 对已有功能的影响明确 —— `wf_run` 与 Runtime 不改；内置工具路径降为回退
- [x] 已知风险明确 —— 模型不遵循 runbook、`wf_run` 不可用、口径改动扩散、文档漂移（规格 §17）

## 9.3 决策完整性

- [x] 所有会影响产品结果的人工决策均已完成 —— 发起权归插件（A）、B 不采纳、内置工具降为回退、范围收缩、验收基准、无人值守许可（规格 §12，用户 2026-09-11 确认）
- [x] 不存在「施工时再决定」的产品问题 —— V1 的「方案取舍留待实施期定」已消除
- [x] 未决产品事项数量为 0

## 9.4 任务组织

- [x] 如需求复杂，已完成必要的功能切片拆分 —— 本卡为单张可独立 UAT 的完整切片；「回退路径补全记录」明确不采纳，如需再单独立卡（规格 §13）
- [x] 每个子任务均可独立 UAT
- [x] 前置依赖已明确填写 —— 「无」（规格 §14）
- [x] 优先级已确定 —— P1

## 9.5 验收

- [x] 每个主要功能有明确验收条件 —— 7 条（规格 §15），含真实验证与回退不静默
- [x] 已能形成主要 UAT 场景 —— UAT-01（完整运行）、UAT-02（回退提示）（规格 §16）
- [x] 验收标准可以通过真实操作判断 —— 判定基准：运行记录 `task_id` 非空 + `trigger=start`；看板分段数 ≥ 2；`DONE` 时 `completion` 非 null

## 9.6 无人值守

- [x] 已明确「无人值守许可」—— 允许
- [x] 若允许无人值守，不存在必须在施工过程中由人选择的产品问题 —— 唯一前置为技术验证（`wf_run` 在 DSH 内是否可用），不通过时**停下呈报**，不构成产品选择

---

## 结论

- [x] **全部通过** → 状态改为「待确认」，呈递人工确认基线 V2
- [ ] 未通过 → 保持「定义中」

确认话术：请确认需求基线 V2，可按该版本进入交付。

**人工确认记录**：用户于 2026-09-11 会话确认基线 V2 → 落档「本地已定义」（本地轨道；`GitHub 同步 = pending`）。复用的批准对象 = V2 规格（范围收缩为「口径统一 + 真实验证」）。

---

## 附录 · V1 未通过记录（2026-09-11，保留追溯）

1. **9.3 决策完整性未通过**：接入机制（runbook 走 `wf_run` / 保留内置工具 + 新增回灌 / 组合）未定，且卡上明示「方案取舍在实施 Definition Check 时……定」。
2. **9.6 无人值守未通过**：该未决项与「无人值守许可 = 允许」互斥。
3. **9.1 / 9.2 派生未勾选项**：修改范围、`wf_run` 不可用时的异常行为随第 1 项未定。

解除路径与结果：用户于 2026-09-11 确认「DSH 的 skill 入口后面会调用」→ 发起权归插件（方案 A）→ 范围收缩为「口径统一 + 真实验证」→ V2 全部通过。
