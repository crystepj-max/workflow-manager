# Definition Check（FIX-226 · 自定义角色正文的运行期冻结）

| 项 | 值 |
|---|---|
| 任务名称 | 自定义角色正文的运行期冻结 |
| 拟确认基线版本 | V1 |
| 检查人（Agent） | Qoder 独立开工会话 |
| 检查时间 | 2026-09-19T13:16:16Z（首检 12:39:13Z 未通过，决策落定后复检） |
| 未决产品事项数 | **0** |

## 9.1 目标与范围

- [x] 用户问题明确（等待期间改角色文件，恢复后节点中途换了规矩，而看板仍显示「定义已冻结」）
- [x] 需求目标明确（同一次运行全程读到同一份角色正文；快照能回答用的哪一版）
- [x] 非目标明确（不碰内置角色内联、不做运行中切角色、不改角色库编辑、不动修订链）
- [x] 修改范围明确（生成器装配范围 + 快照角色条目 + 读取顺序 + 降级留痕）
- [x] 不修改范围明确（`roleDir`/产物旁角色包机制、内置角色裁剪规则、定义快照本身）
- [x] 修改前状态明确（内置已冻结 / 自定义工作区优先读；受影响角色 = `dispatcher`，被 `dev-workflow-2-0`、`default-workflow` 使用；`.generated` 旁无角色包时只剩一个可变路径）
- [x] 修改后状态明确（§6 已按决策 3=A 定稿）

## 9.2 规则与边界

- [x] 主要业务规则明确（§9 五条：冻结单位是内容、摘要可核对、只内联被引用、降级必须显式、生成物不得手改）
- [x] 主要用户路径明确（§10 五步）
- [x] 关键边界场景明确（§11 六项）
- [x] 关键异常场景明确（读不到文件、超尺寸闸门、同名内置/自定义、旧运行续跑）
- [x] 对已有功能的影响明确（撤销「编辑工作区角色种子即对 bundled run 生效」这条既有取舍——已在 §12 记为人工确认接受）
- [x] 已知风险明确（§17，含与 CHORE-104 载荷瘦身、FIX-65 捕获上限的历史关系）

## 9.3 决策完整性

- [x] 所有会影响产品结果的人工决策均已完成 → 决策 3 = A（内容内联），并确认接受「改角色对已存在的运行不再生效」这一行为变更
- [x] 不存在「施工时再决定」的产品问题（尺寸闸门阈值、摘要算法属施工内部选择，受 §9.3 规则约束并由 AC-05 验证降级可见）
- [x] 未决产品事项数量为 0

## 9.4 任务组织

- [x] 已完成功能切片拆分（三片：证据兜底／抢位／角色冻结；本片为角色冻结）
- [x] 本切片可独立 UAT（改文件 → 恢复 → 看是否换规矩）
- [x] 前置依赖已明确填写「无」
- [x] 优先级已确定为 P1（随 V1 基线一并确认）

## 9.5 验收

- [x] 每个主要功能有明确验收条件（AC-01..06）
- [x] 已形成主要 UAT 场景（UAT-01，含「新运行仍读到新内容」的反向关注点）
- [x] 验收标准可以通过真实操作判断

## 9.6 无人值守

- [x] 已明确「无人值守许可」= 允许（随 V1 基线一并确认；决策 3 落定后施工路径唯一、可单测）
- [x] 允许无人值守时无须施工期间另做产品决策

## 结论

- [x] **全部通过 → 状态可改为「待确认」，呈递人工确认基线 V1**
- [ ] 未通过 → 保持「定义中」

未通过项：无。**落档结果**：2026-09-20T02:19:49Z 三片均转「已定义」——远端 issue 正文与标签、登记册 `status`、任务卡与规格状态四处一致，看板已重写。剩余非门禁事项：本卡与规格文件尚未提交入库，干净检出前需先合并。

发号与登记已完成（2026-09-19 经用户授权走 GitHub 通路），基线 V1 已于 2026-09-20T02:19:49Z 按正式号确认。当前唯一残留：本卡与规格文件尚未提交入库，合并前干净检出取不到规格（`npm run validate:task-context` 会报 spec-untracked，与仓库内其他在途定义卡同状态）。

**呈递结果**：V1 基线已于 2026-09-19T13:32:21Z 获用户确认（三片一并确认，含本片「改角色文件对已存在的运行不再生效」这一行为变更）。批准对象与影响范围记在 [task-spec-V1.md](task-spec-V1.md) §12 基线确认记录。发号与登记已完成（GitHub Issue + 登记册 + 看板），已于 2026-09-20T02:19:49Z 转「已定义」。
