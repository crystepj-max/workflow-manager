# Definition Check · LOC-016（运行看板 Logical Run 决策卡与工作区 UI）

| 项 | 值 |
|---|---|
| 任务名称 | 运行看板 Logical Run 决策卡与工作区 UI |
| 任务标识 | `LOC-016` |
| 拟确认基线版本 | **V2** |
| 检查人（Agent） | Agent（requirements-analysis） |
| 检查时间 | 2026-09-15 |
| 未决产品事项数 | **0** |
| 结论 | **全部通过** → 状态改为「待确认」，呈递人工确认基线 V2 |

> 规格：`.scratch/LOC-016-logical-run-ui/task-spec-V2.md`
> 分析摘要：`.scratch/LOC-016-logical-run-ui/requirements-analysis.md`
> 决策票：`.scratch/LOC-016-logical-run-ui/decision-tickets/DT-01-ui-resume-attribution.md`（阻塞片2，不阻塞本基线）

---

## 9.1 目标与范围

- [x] 用户问题明确 —— 看板看不懂一次运行（跨段/业务结果/完成类型全不可见），人工决策与受阻恢复只能复制命令回会话（`client.js:2544-2546`）
- [x] 需求目标明确 —— 看板内完整读懂一次 Logical Run；决策材料结构化呈现并可复制等价参数；工作区现场含实时活动锁可见；按 Lifecycle/Completion 筛选（规格 §3）
- [x] 非目标明确 —— 不在本基线实现看板内续跑执行；不改 Runtime/持久化契约；不新增可改变运行状态的宿主能力；不下发 capability；不照抄无 producer 枚举（规格 §4）
- [x] 修改范围明确 —— 插件客户端看板 + 两处只读投影（`vwf.runs.list` 新增 `completion`；`vwf.logicalRuns.get` 新增可选只读入参/字段）+ i18n + 客户端冒烟（规格 §7）
- [x] 不修改范围明确 —— 引擎/编译语义、logical-runs 记录结构、工作区内核与 capability 口径、编辑器、Chat/Skill runbook、`vwf.run.control` 既有语义（规格 §8）
- [x] 修改前状态明确 —— 五项零实现，仅 PAUSED 卡与「第 N 段」徽标与命令文本（规格 §5，含证据行号）
- [x] 修改后状态明确 —— 详情视图六区块 + 决策材料卡 + 工作区面板（含实时锁）+ 筛选（规格 §6）

## 9.2 规则与边界

- [x] 主要业务规则明确 —— R1 一次运行一个 Run / R2 只读优先 / R3 分层不混用 / R4 枚举真源 / R5 材料如实 / R6 参数真实 / R7 capability 不下发 / R8 缺字段不猜 / R9 新旧兼容（规格 §9）
- [x] 主要用户路径明确 —— 筛选 → 选 Run → 六区块阅读 → 决策材料与复制参数 → 工作区面板（规格 §10）
- [x] 关键边界场景明确 —— `found:false`、无 `decision_package`、材料缺项、`blocked_edge` 缺失、`completion=null`、旧记录无控制面字段、检查点降级、无 workspace（规格 §11）
- [x] 关键异常场景明确 —— 实时锁查询失败降级、筛选空态、分页与筛选叠加、i18n 缺 en 回落但不掩盖漏译（规格 §11）
- [x] 对已有功能的影响明确 —— 既有 PAUSED 卡与列表徽标保留；门禁卡的占位命令文本替换为真实参数（R6）；`runs.list` 摘要只增不改
- [x] 已知风险明确 —— 摘要口径漂移、活动锁解析、客户端冒烟桩、包体上限、LOC-015 两坑、登记册落后 main、片2 探路无解（规格 §17）

## 9.3 决策完整性

- [x] 所有会影响产品结果的人工决策均已完成 —— 续跑落地方式（先探路）、切片（2 片）、筛选口径与数据来源、工作区面板口径（含实时锁）、目标不降级，均于 2026-09-15 会话由用户确认（规格 §12）
- [x] 不存在「施工时再决定」的产品问题 —— 原「②④ 点选续跑」已明确移出本基线（片2 + DT-01），基线内无待决项
- [x] 未决产品事项数量为 0

## 9.4 任务组织

- [x] 如需求复杂，已完成必要的功能切片拆分 —— 原 5 项含未决设计未知（执行归属）→ 拆为片1（呈现层，本基线）与片2（交互续跑，待 DT-01）
- [x] 每个子任务均可独立 UAT —— 片1 四类 UAT（UAT-01~04）；片2 待结论另立
- [x] 前置依赖已明确填写「无」或具体任务 —— 前置依赖：无（LOC-011/012/013 已合并，#79/#80/#93 已落地；DT-01 仅阻塞片2）
- [x] 优先级已确定 —— P2（唯一裸值，符合 `scripts/ai-task-preflight-check.mjs:69` 解析）

## 9.5 验收

- [x] 每个主要功能有明确验收条件 —— 详情视图/完成类型/决策材料/等价参数/工作区与实时锁/筛选/旧记录兼容/只读性回归/文案与门禁（规格 §15，共 10 条）
- [x] 已能形成主要 UAT 场景 —— UAT-01~04 均含前置条件、步骤与预期结果（规格 §16）
- [x] 验收标准可以通过真实操作判断 —— 真机可复现（多段 Run、`WAITING_HUMAN` Run、隔离 workspace、多类型完成），且区分「机器可验证」（测试/门禁）与「人工 UAT」（真机点选与参数复制）

## 9.6 无人值守

- [x] 已明确「无人值守许可」—— 允许
- [x] 若允许无人值守，不存在必须在施工过程中由人选择的产品问题 —— 基线内所有产品口径已定；DT-01 属片2 前置，不影响片1 施工

---

## 结论

- [x] **全部通过** → 状态可改为「待确认」，呈递人工确认基线 V2
- [ ] 未通过

未通过项：无。
