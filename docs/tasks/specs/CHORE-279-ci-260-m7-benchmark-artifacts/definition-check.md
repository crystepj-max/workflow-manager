# Definition Check · CHORE-279 M7 基准产物治理

## 检查元数据

| 字段 | 值 |
|---|---|
| 任务标识 | `CHORE-279` |
| 远端 issue | github#279 |
| 检查对象 | 测试不再改写仓库 benchmark 产物；入库冻结基线保持不可变 |
| 检查时间 | 2026-09-21 |
| 结论位置 | 本文末「结论」节 |

## 9.1 目标与范围

- [x] 目标限定为：跑测试不再改写仓库中任何 benchmark 产物（父票 AC-06）
- [x] 范围仅覆盖写入链路（2 个库函数可指定项 + 2 个测试用例 + README 说明）
- [x] 明确不含：改写、删除、重新冻结任何入库产物
- [x] 明确不含：改动命令行工具的默认输出位置与调用方式
- [x] 明确不含：新增闸门断言（不扩大范围）

## 9.2 现状证据

- [x] 现象已量化并复现：跑前 `git status` 0 项 → 跑后 **51 项**（冻结清单 1 + 报告 2 + 占位记录 48）
- [x] 产物类与输入类已分离：`scripts/benchmark` 共 101 个被跟踪文件，其中产物 51、输入 50
- [x] 写入源已定位到用例：测试文件 8 个用例中的第 4、5 个
- [x] 无条件写入点已定位：`writeFreezeManifest(benchRoot, …)` 不随 `resultsDir` 隔离而改变
- [x] 忽略参数已定位：`writeResearchReport` 恒写 `benchRoot/reports`，不读传入的 `resultsDir`
- [x] 证据语义已定性：冻结合同记 `frozen_at` + `repo_head`，被改写即证据失真
- [x] 非确定性已确认：占位记录 `record_id` 每次随机、`created_at` 为当前时间，故「只重冻一次」不能根治
- [x] 闸门影响面已查明：三个校验脚本均不引用 benchmark，本票不影响闸门结果

## 9.3 验收标准

- [x] AC-01 目标测试全绿（8/8），可机器验证
- [x] AC-02 跑完整测试后 `scripts/benchmark` 下无产物类改动，可机器验证
- [x] AC-03 封条 `frozen_at` 与模板指纹保持原值，可机器验证
- [x] AC-04 占位记录 `created_at` / `record_id` 保持原值，可机器验证
- [x] AC-05 命令行工具无参调用行为不变，可由代码路径核对
- [x] AC-06 失败清单与干净主干逐条一致（对照实验），可机器验证
- [x] 标准不含模糊表述；「修绿」不得通过删除产物或忽略文件达成

## 9.4 依赖与前置

- [x] 无跨票代码依赖；M7 与 M1–M6 相互独立
- [x] 产品决策已取得：做法一（隔离测试 + 封条不动）由项目负责人于 2026-09-21 选定
- [x] 工具依赖已确认：`gh` 已登录，发号得到 `github#279`
- [x] 施工隔离：本票在独立 worktree / 分支施工，不动主检出与并行会话的在制内容

## 9.5 风险与残留

- [x] 入库封条与当前模板确实不一致：已在 README 写明「不随模板演进回填」，并把「显式重新冻结」列为新一轮实验的前置
- [x] 不新增产物洁净度自动校验：如实记录为人工/CI 可复核项，不伪装成已自动化
- [x] 既存死代码 `benchTmp` 不处理：与本次改动无关，仅在任务卡与本清单记录
- [x] 本票改动面限于测试与库的可指定项，未改任何断言、未放宽检查

## 结论

**通过。** 根因已定位到具体的写入点与无条件写入语义；做法一为项目负责人显式选定；验收标准均为可机器验证项。具备开工条件。
