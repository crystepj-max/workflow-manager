# Definition Check · CHORE-110 轻量路线验收包可登记性

> 进入「待确认」前逐项勾选；全部通过后才可请求人工确认基线。人工确认后才可落档为「已定义」。任一未勾选或未决产品事项 > 0 → 禁止标为「已定义」。

---

## 检查元数据

| 项 | 值 |
|---|---|
| 任务名称 | 轻量路线验收包可登记性：acceptance_package schema 放开与签署人留痕 |
| 拟确认基线版本 | V1 |
| 检查人（Agent） | 独立开工会话（Qoder，CHORE-110） |
| 检查时间 | 2026-09-19（同轮复核 + DT-01 裁定后终态） |
| 未决产品事项数 | **0**（DT-01 两项开放项已于 2026-09-19 由松哥裁定：A·b3 存在性分层；摘要修复并入本票） |

---

## 9.1 目标与范围

- [x] 用户问题明确——轻量路线的验收签署无结构化落点，`decided_by` / `decided_at` 恒为 null，「谁验收的」永久丢失
- [x] 需求目标明确——契约给出唯一口径；轻量路线签署可追溯；禁止伪造独立评审
- [x] 非目标明确——不动收口脚本口径（FIX-109）、不动引擎路线与适配层、不动 UI、不追溯历史
- [x] 修改范围明确——schema + 契约 + `cwf-validate` 校验分支 + 证据摘要取值
- [x] 不修改范围明确——已列明 5 类不动项

## 9.2 现状证据

- [x] 修改前现状已实测并记录（schema 必填项、契约 §8.3 条款、记录类型合法性、本批 Run 记录构成）
- [x] 存在可复现的历史现象（CHORE-106 / CHORE-37 归档摘要三字段全为 null）
- [x] 根因已定位到代码级（必填结构强制五类引用，与轻量路线不产生 review/test 记录的现实冲突）
- [x] **2026-09-19 全量复核修正**：51 个已归档任务的三字段 null 分成五类成因，schema 阻塞仅 6 例；另有 3 例签署已发生却在永久层丢失（CHORE-36 快照过期、LOC-032/033 未关联 Run）。见规格 §3.1
- [x] **因果修正**：6 例阻塞任务缺失项各异（LOC-037/045 仅缺 design、LOC-038 仅缺 review），「轻量＝只有 dev_handoff」的画像不成立 → 影响分层判据设计
- [x] **兼容性证据**：`record_version` 为 `const:"v0.1.8"`，盘上 453 个记录文件 + 7 个 examples 全为该值；`formal-records.mjs:50-56` 另列五类但实测为「存在即链接」非闸门（规格 §7.3）

## 9.3 验收标准

- [x] 每条验收标准可被自动或人工复现（契约文本可查、归档摘要字段可断言、`cwf-validate` 行为可测）
- [x] 验收标准不含模糊表述（「正常」「合理」等）

## 9.4 依赖与前置

- [x] 前置依赖为空
- [x] 依赖的既有通道已确认存在（`cwf-record` / `cwf-validate` / `workspace-evidence-summary` 均已支持 `acceptance_package` 记录类型）

## 9.5 风险与可逆性

- [x] 已列风险与处置（schema 放宽被误用、两套语义分叉、审计可追溯性取舍）
- [x] 可逆性说明——契约与 schema 改动可回退；**唯一不可逆项是「若选 C 则放弃签署留痕」**，已显式标注

## 9.6 无人值守许可

- [x] **允许**——DT-01 已裁定（A·b3 存在性分层，摘要修复并入），方案与范围均已定，施工阶段无需要人取舍的产品问题；验收与收口仍是人工关口。

## 9.7 与既有契约一致性

- [x] **已按裁定逐条比对**：方案 A·b3 只把 `assembled` 的必填集从六项收窄为两项并新增可选 `evidence_gaps`，属**纯放宽**——`awaiting_decision↔decided` 状态机、`decided` 必填 `verified_branch/verified_head`、五类齐全时的九项校验全部不动；§5「AI 不得代签」由「`acknowledged_by` 不得等于记录 `produced_by`」的机检形式承接（规格 §7.2）。
- [x] **已提前查明的硬性一致性约束**（写入规格 §7.1/§7.3/§7.6 与 §8 标准 6）：
  - `record_version` 保持 `const:"v0.1.8"`，改动不得使盘上 453 个记录文件与 7 个 examples 失效；
  - 五类引用规则在 `handoff.schema.json` 与 `scripts/formal-records.mjs:50-63` 各存一份，放开必须同处改，否则 schema 与机器校验分叉；
  - `route` 一名在本仓已表示「节点下一跳」，新字段不得复用该名；
  - 契约 §5 L201 与 §8.3 明文禁止 AI 代签，新增签署字段须有可核对来源；
  - schema 与契约的真源在本仓，`scripts/sync-ai-task-skill-set.mjs:86` 生成的 skill 侧副本须同步（规格 §7.5）。

## 9.8 未决事项

- [x] **开放项 1 已裁定**：轻量路线验收签署的登记形态＝**A · 验收包分层，取 b3 存在性分层**（判据为本 Run 盘上实际存在的记录，不由会话自报路线）；随附 b/c/d/e/f 由裁定一并确定。记录：`decision-tickets/DT-01-lightweight-acceptance-record.md`（**closed**）。
- [x] **开放项 2 已裁定**：**并入本票**——摘要在签收后刷新、按 task_id 自动关联 Run、无验收包时显式标记而非静默 null，并修复 CHORE-36 / LOC-032 / LOC-033 三例存量摘要。已写入规格 §7.4 与 §8 标准 8。
- [x] 与 GitHub #123 **定义不重叠**（#123 余项为 `baseline_revision` 格式与自由文本下限），但同改 `handoff.schema.json` → 施工须串行、合并前复核对方是否已动同一文件。
- [x] 与 `FIX-109` **不重复 claim**（其两处缺口为 `merge_commit` 回写时机与「涉及范围」正则，并明文不改验收包/摘要 schema）；同文件协调点已在 §7.4 标注。

未决产品事项数：**0**

---

## 结论

定义门**全部通过**（2026-09-19）：§9.1–§9.8 全绿，未决产品事项 **0**。

本轮完成的收敛不止于补齐检查表——原定义「三字段恒为 null 因轻量路线登记不了」的单一因果链经 51 个归档任务全量核对被修正为五类成因（schema 阻塞仅 6 例，另有 3 例签署已发生却被永久层丢掉），DT-01 三个选项的真实成本与随附子问题（路线标识来源、命名冲突、签署凭据、版本兼容、规则副本）已算清并写入规格 §7。

**基线确认**：松哥于 2026-09-19 会话中就 DT-01 两项开放项作出决定（A·按实际证据分层 / 并入摘要修复），构成本票 V1 基线的人工确认。

> 遗留人工关口：本票**未**在此确认「最终 §7 施工口径文本」需另行批准；实施完成后仍须走**验收**与**收口/合并**两个独立人工关口。
