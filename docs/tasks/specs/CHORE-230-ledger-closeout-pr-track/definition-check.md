# Definition Check · CHORE-230 任务账本与立票/收口路线适配 PR-only 主干

> 进入「待确认」前逐项勾选；全部通过后才可请求人工确认基线。人工确认后才可落档为「已定义」。任一未勾选或未决产品事项 > 0 → 禁止标为「已定义」。

---

## 检查元数据

| 项 | 值 |
|---|---|
| 任务名称 | 任务账本与立票/收口路线适配 PR-only 主干（消除每任务额外 PR） |
| 拟确认基线版本 | V1 |
| 检查人（Agent） | Qoder |
| 检查时间 | 2026-09-20 |
| 未决产品事项数 | **0**（D-01～D-06 已由松哥 2026-09-20 裁定） |
| 立票过程偏差 | 发号一度误落 CNB 灾备镜像（`cnb#116`），已换号为 `CHORE-230`/`github#230` 并留 `legacy_id`；成因与防护写进规格 §2.4、§4.4 第 12 项 |

---

## 9.1 目标与范围

- [x] 用户问题明确——每收口一个任务多开一个 PR（实测 #223 + #229）、新任务材料进不了主干（实测 FIX-224/225/226 未跟踪）、门禁两难、误发号暴露面变大
- [x] 需求目标明确——立票/施工/收口在 PR-only 主干上闭环，每任务 1 个 PR，且不降级可追溯性与既有校验
- [x] 非目标明确——§5 列 7 项，含"不开 ruleset bypass""不搬出 Git""不改历史值""不修 CI born-red"
- [x] 修改范围明确——§4.1～§4.5 共 15 处，逐条给文件、函数、行号与目标形态（行号取自 2026-09-20 `origin/main` `a8174f2`，逐条读源码核对）
- [x] 不修改范围明确——§5

## 9.2 现状证据

- [x] 修改前现状已实测并记录：额外 PR 实例、未跟踪材料清单（`git status` 的 `??`）、`merge.commit` 的两个真实消费方（preflight 祖先校验 L69–77、`validate-workspace` D-8 逐字一致 L314–315）、收口脚本的主干推进点与账本写入点、存量字符串形状 `merge`
- [x] 关键可行性已验证：合并 sha 可由 `Merge pull request #<n>` 在本地 `origin/main` 离线解析，不需联网；本票自己的 `mergeCommit` 已通过 `gh pr view` 与主干合并提交对得上
- [x] 根因已定位到结构层——账本要过代码评审闸门，而账本记录的合并事实在闸门之后才产生

## 9.3 验收标准

- [x] §7 共 8 条，每条可复现（跑一次真实收口、`git log` / `gh pr view` 对照、三态解析用例、门禁双用例、`git diff` 断言历史值不变、stderr 观察）
- [x] 不含模糊表述（无"正常""合理"类判据）
- [x] 与"不新增失败"同口径处理 CI：既有 born-red 不在本票验收内（§12 已记）

## 9.4 依赖与前置

- [x] 前置依赖：无。"未合并则无法开始"不成立
- [x] 同文件冲突已处置：与 `FIX-109` 在 `local-task-merge.mjs` 强重叠 → 开工前 rebase 到含 FIX-109 的主干，并把其两个缺口列入回归（规格 §8）
- [x] 跨票依赖已如实标为限制而非静默假设：GitHub 轨道证据来源依赖 CHORE-110 的验收包 schema，未落地前 `--via-pr` 对无验收包的任务不生成 `evidence-summary.json`（§12）

## 9.5 风险与可逆性

- [x] 已列 6 项风险与处置（推导误判、合并信息格式漂移、门禁放宽被误用、同文件冲突、共用登记册 diff 噪音、自动合并波及整批任务）
- [x] 改动可逆：均为脚本与文档；账本新字段 `merge.pr` 对旧消费方是增量而非替换，`merge.commit` 语义不变
- [x] 不产生不可逆对外动作：解析只读、推导不删历史值、收口路线不自动合并（D-03）

## 9.6 无人值守许可

- [x] 依据明确（D-03）：实施与自动检查免问；**合并需人工批准**——本票改的是所有后续任务共用的开工门禁与收口路线，且当前自动 Review 闸门处于空档
- [x] 无"施工中需人取舍"的产品问题残留：面 A/面 B 的顺序、字段形状、门禁分级阈值均已由规格定死

## 9.7 与既有契约一致性

- [x] 不新增第二套定义/收口入口：`--via-pr` 是 `local-task-merge.mjs` 的模式分支，立票材料走同一条 PR 路线
- [x] 不推翻现行硬规则：任务卡/登记册/看板仍入库、仍支持离线开工（推导在离线时回落显式值）
- [x] 与 CHORE-111 的裁定不冲突：历史值零改动（D-05 沿用其 D-03）、禁止 CNB 发号（本票第 12 项加防护）、收口适配器动作形态不改（§5）
- [x] 与 FIX-109 的目标同向：本票 D-02 正是其"merge_commit 回写时机"缺口的根治路径
- [x] 状态词汇不新增枚举：`STATUSES` 保持封闭，推导只影响读取与看板呈现（避免第二套状态词）

## 9.8 未决事项

- [x] **0 项**。范围（D-01）、合并事实记法（D-02）、无人值守边界（D-03）、误建票处置（D-04）、存量形状（D-05）、被否决的替代方案（D-06）全部落定。

---

## 结论

定义门各项**全部通过**，未决产品事项数为 **0**。

本票处于「**待确认**」，等待人工确认需求基线 V1 后落档为「已定义」（登记册机器取值 `本地已定义`，issue #230 打 `ready-for-agent` + `sized-m`）。

> 当前状态：`待确认`（立票材料已在任务分支入库，见 PR；决策 6 项已落定）。
