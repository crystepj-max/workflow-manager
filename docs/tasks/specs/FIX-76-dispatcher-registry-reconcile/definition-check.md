# Definition Check · FIX-76（M5 调度器内建登记册对账）

| 项 | 值 |
|---|---|
| 任务名称 | M5 调度器内建登记册对账 |
| 任务标识 | `FIX-76` |
| 拟确认基线版本 | **V1** |
| 检查人（Agent） | Agent（requirements-analysis） |
| 检查时间 | 2026-09-17 |
| 未决产品事项数 | **0** |
| 结论 | **全部通过** → 状态可改为「本地已定义」，呈递人工确认基线 V1 |

> 规格：`./task-spec-V1.md`
> 分析摘要：`./requirements-analysis.md`
> 决策票：`./decision-tickets/DT-01-fix-layer.md`（已裁定：在 candidate-collect 层修）

---

## 1. 目标与范围

- [x] 用户问题明确 —— M5 调度器候选采集按登记册 `status` 判依赖，依赖已合并但登记滞后时任务整晚被误排除（LOC-028 复现风险）
- [x] 需求目标明确 —— 候选采集依赖判定基于「登记册已合并 或 主干有合并痕迹」，与 CHORE-73 第 3 条一致
- [x] 非目标明确 —— 不改 execution-plan 的 assessCandidate（CHORE-73 已实现）、不改对账脚本/账本锁、不改验收三态/返工上限
- [x] 修改范围明确 —— 仅 `collectLocalCandidates` 的依赖排除段（第 99–106 行）+ 一个单测用例
- [x] 不修改范围明确 —— 账本写入路径、`registry-reconcile.mjs`、execution-plan 既有逻辑、调度器/watchdog/报告
- [x] 修改前状态明确 —— 依赖排除只看 `dep.status !== STATUS_MERGED`，绕过了 CHORE-73 的「看事实」判定（规格 §1，含代码行号）
- [x] 修改后状态明确 —— 依赖满足 = 登记已合并 或 git 事实有合并痕迹；其余闸门不变（规格 §3）

## 2. 规则与边界

- [x] 主要业务规则明确 —— R1 依赖满足双源判定（登记 OR 事实）；R2 仍不满足才排除且标注原因；R3 不改动其他早排除项
- [x] 主要用户路径明确 —— 候选采集 → 依赖判定（含 git 事实）→ 其余闸门 → 产出候选/排除列表
- [x] 关键边界场景明确 —— 依赖任务不存在（视为未满足）；依赖任务有合并痕迹但登记未回写（满足）；依赖任务既无痕迹也非已合并（排除）
- [x] 关键异常场景明确 —— `collectMergeFacts` 扫描失败（git 不可用）时退化为仅看登记册，不阻断采集
- [x] 对已有功能的影响明确 —— execution-plan 的 assessCandidate「看事实」判定保留，二者互补而非冲突；既有采集闸门测试不受影响

## 3. 验收与可操作性

- [x] 验收项可操作 —— 4 项验收均给出具体判定方式（单测构造合并痕迹、全绿回归）
- [x] 未决产品事项为 0 —— 所有歧义已在决策票 DT-01 裁定
