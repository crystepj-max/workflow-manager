# Definition Check · CHORE-268 CI 可信度 M2 端口核验

## 检查元数据

| 字段 | 值 |
|---|---|
| 任务标识 | `CHORE-268` |
| 远端 issue | github#268 |
| 检查对象 | 解除 `dev-plugin.test.mjs` 对宿主机 `lsof` 的依赖，使用例在 runner 等价条件下自足 |
| 检查时间 | 2026-09-21 |
| 结论位置 | 本文末「结论」节 |

## 目标与范围

- [x] 目标限定为：探针不可用条件下 `dev-plugin.test.mjs` 16/16
- [x] 范围限定为**测试环境构造**（`baseEnv` 默认注入 + 默认替身生命周期）
- [x] 明确不含：修改 `scripts/dev-plugin.mjs` 的任何行为，尤其**不放宽 fail-closed**
- [x] 明确不含：在 CI 侧安装 `lsof`（会把测试通过继续绑在环境上）
- [x] 明确不含：该测试文件的其余用例与断言
- [x] 明确不含：父票其余子票范围（M5 / M3 已由 `CHORE-262` / `CHORE-264` 处理；M4 / M6 / M7 待办）

## 现状证据

- [x] 失败位置取自 CI 真实输出：`main@f0d95202` 的 `validate` 作业，③ 段 2 项，位置 `scripts/test/dev-plugin.test.mjs:61` / `:132`，原文 `无法核验端口 19527 占用情况`
- [x] 根因指向行：`scripts/dev-plugin.mjs:44`（`lsofBin`）、`:150`（`inspect` 失败返回 `null`）、`:163`（`listeningProcesses`）、`:330`（`resolvePortState` fail-closed）、`:536`（模块顶层执行）
- [x] **缺口精确化**：`fakeLsof()` 替身已存在，其余用例均已注入 `VWF_DEV_LSOF_BIN`，仅这 2 个用例漏注入 —— 已用全文件检索逐条核对
- [x] 本票本地复现（修复前）：强制 `VWF_DEV_LSOF_BIN=/nonexistent/lsof` → 16 项 / 14 通过 / 2 失败，与 CI 清单逐项一致
- [x] 修复后复测：强制不可用 16/16；默认环境 16/16
- [x] 同类遗漏排查：全仓仅 `dev-plugin.test.mjs` 引用 `dev-plugin.mjs`，无其他受影响文件

## 验收标准

- [x] AC-1 ~ AC-5 均可机器验证，判据为具体命令与通过数
- [x] AC-4 以「产品源码零 diff」约束，杜绝通过放宽 fail-closed「修绿」
- [x] AC-3 显式保护既有探针场景（端口占用报错 / 实例发现 / `stop --all`）不被削弱
- [x] AC-5 覆盖「是否有同类遗漏」这一复发面
- [x] 标准不含模糊表述

## 依赖与前置

- [x] 无跨票代码依赖：本票与父票其余子票彼此独立
- [x] 施工隔离：本票在独立 worktree / 分支施工，不动主检出内并行会话的在制内容
- [x] 发号完成：`gh` 已登录 `crystepj-max`，发号得到 `github#268`
- [x] 定义落档：任务卡、本规格、本核对清单三者齐备且互相引用一致

## 风险与残留

- [x] 默认替身以惰性方式创建并复用单份，`process.on('exit')` 清理其临时目录，不产生工作区残留
- [x] 默认注入置于 `...extra` 之前，保证既有用例的显式覆盖**优先级不被改变**
- [x] 无产品行为改动，不引入新回归风险面

## 结论

**通过。** 根因已定位到行并精确化为「个别用例漏注入替身」；修法把「不依赖宿主机工具」变为默认行为，同时保留所有显式探针场景的语义；判据可机器验证且覆盖复发面。三件套齐备，具备开工条件。
