# UAT 验收卡 · CHORE-37（closeout 角色的强删表述收敛）

> 依 CHORE-36 落地的硬要求编写：验收项必须声明「执行时机」，人工裁决只依据「裁决前可观测」项。

## 验收卡总览

| 字段 | 内容 |
|---|---|
| 任务名称 | 交付链口径残留收敛：closeout 角色的 `--force` 表述 |
| Issue | cnb#37（本地轨道；GitHub 同步 pending，CNB 仅作编号锚点） |
| 需求基线版本 | V1 |
| 本轮目标 | 让收口说明书中「删不掉怎么办」与已通过决策一致：拒绝删除、登记遗留、不阻塞合并、不使用强删手段 |
| 修改前 | `dsh/roles/closeout.md:24` 写「残留本任务未提交/未跟踪文件时，先确认归属再用 `--force`」 |
| 修改后 | 同处改为「**不带 `--force`**；……**拒绝删除**——先核实这批文件的归属，登记为遗留项交由后续处理，合并继续进行；哪怕 git 提示需加强删参数也不许升级手段（口径出处：决策 0001 §6、约定 §1.7.1、LOC-023 规格 §9 R-4）」 |
| 自动审查结果 | **未覆盖**——本票体量 S、单文件文档改动，未启动独立审查会话；以自动测试与逐项自检替代（如实标注，不作为隐藏缺口） |
| 自动测试结果 | **通过**——`npm test` 639 / 639，退出码 0（含多次复跑，见 UAT-04 备注） |
| 已知风险 | ① dist 是生成产物，若被当作真源手改会被下次构建覆盖（已按规格禁止手改并以差异比对验证）；② 本票未加机器闸门，同类漂移无自动拦截；③ `dev-plugin.test.mjs` 存在时序敏感抖动 |
| 未覆盖项 | ① 未执行独立自动审查会话；② 两处范围外残留未处理（见任务卡「范围外发现」）；③ 本机安装态技能副本未同步；④ `npm run validate:workspace` D-7 为既有红灯（FIX-66 未收口工作区），非本票引入 |
| Run 标识 | chore-37-r1 |

人工正式结果只允许三选一：**验收通过** / **验收退回** / **有条件通过**。

---

## UAT 清单（裁决依据）

### 【UAT-01 收口说明书不再允许强删】

**执行时机**
裁决前可观测

**验收目的**
确认收口说明书关于「删不掉怎么办」的表达既符合已定规则，又没有丢掉有用的操作提示。

**前置条件**
1. 位于任务工作区 `../workflow-manager-worktrees/dev-chore-37-r1`，分支 `dev-chore-37-r1`
2. 施工改动已提交（真源 `dsh/roles/closeout.md`）

**操作步骤**
1. 打开 `dsh/roles/closeout.md`，阅读第 6 步「原子清理工作区」阶段一删除条款（第 24 行）
2. 打开 `dsh/skills/construction-bootstrap/runbook.md` §7 收口第 5 步，比对同一件事的说法
3. 打开 `docs/design/ai-task-define-delivery/local-track-offline-mode.md` §7.4，做第三次比对
4. 核对第 25、26 行（归档三件套已入库、删除后追加 prune 兜底）是否原样保留

**预期结果**
1. 三处均表达同一口径：拒绝删除 + 登记遗留 + 不阻塞合并 + 不使用强删
2. 没有任何一处暗示或明示可以使用强删手段
3. 「核实归属」的操作提示被保留（未因禁用强删而误删整句）
4. 第 25、26 行未被改动

**自动验证结果**
通过（逐行比对完成；`diff dsh/roles/closeout.md packages/dsh-visual-workflow/dist/roles/closeout.md` 无差异）

**建议人工关注**
删掉「先确认归属」原句可能丢掉有用提示。现有文案改为「先核实这批文件的归属，登记为遗留项交由后续处理」，既守住了禁用强删，也保住了提示。若你希望更硬或更软的措辞，这是唯一的改写点。

---

### 【UAT-02 交付链再无允许类强删表述】

**执行时机**
裁决前可观测

**验收目的**
确认不是只改了看得见的一处——全仓扫描后，收口与清理口径相关的文档里不再有允许或建议使用强删的表述。

**前置条件**
1. 已执行 dist 重建（`node packages/dsh-visual-workflow/scripts/build-bundle.mjs --force`）

**操作步骤**
1. 在仓库根检索 `--force` 关键字（排除依赖目录），逐条查看命中位置上下文
2. 比对 `dsh/roles/closeout.md` 与 `packages/dsh-visual-workflow/dist/roles/closeout.md`

**预期结果**
1. 收口/清理口径相关的文档中，允许类表述计数为 0；剩余命中均属「禁止 / 不使用 / 原则句 / 历史实录 / 其他命令的参数名」
2. 两份文件差异为空

**自动验证结果**
通过（允许类 0 条；两处文件差异为空）

**建议人工关注**
本次扫描同时识别出两处**范围外**残留，已登记在任务卡「范围外发现」并等待你的处置决定：`docs/runbooks/acceptance-construction-epic.md:21`（演练手册里的强删命令示例，同段还在用已废弃的仓库内布局）与 `scripts/workspace-isolation.mjs:935`（脚本实际执行强删）。本票未动它们。

---

### 【UAT-03 四处口径载体语义一致】

**执行时机**
裁决前可观测

**验收目的**
确认「一处为准、四处同义」，不存在第二套说法。

**前置条件**
1. 施工改动已提交

**操作步骤**
1. 逐一核对：收口角色说明书 §阶段一、`runbook.md` §7 收口第 5 步、`local-track-offline-mode.md` §7.4、约定 §1.7.1 两阶段动作 + §2.5 门控第 4 条原则句
2. 与 `scripts/local-task-merge.mjs` 第 7 步的实际删除行为做一次语义对照

**预期结果**
1. 四处表述语义一致，且都与脚本实际行为吻合
2. 无互相矛盾的授意

**自动验证结果**
通过

**建议人工关注**
规格 V-3 曾把约定的原则句误引为「§3.4」（实为 §2.5 门控第 4 条；§3.4 是「归档缺口与补救」），已在施工中修正。若你手上有引用旧版发送的文档，可能仍在传播这个错引用。

---

## 收口后复核（合并后由执行体填写；**不是**人工裁决的依据）

| UAT 项 | 执行者 | 执行时间 | 命令 | 实际结果 | 结论 |
|---|---|---|---|---|---|
| UAT-04 主检出侧全仓测试全绿 | 执行体（AI） | 待填 | `npm test` | 待填（注：工作区侧已跑，见备注） | 待填 |
| UAT-05 主检出侧工作区治理闸门结论不变 | 执行体（AI） | 待填 | `npm run validate:workspace` | 待填（已知 D-7 由 FIX-66 未收口工作区导致，与本票无关） | 待填 |

> 备注：UAT-04 在工作区侧已跑三次，结果分别为 3 失败 / 1 失败 / **0 失败（639/639）**，失败项集中在 `dev-plugin.test.mjs` 的进程等待用例；单独复跑该文件三轮均 16/16 全绿，判定为并发时序抖动，与本票改动（仅一个角色说明文件）无引用关系。

> 备注：另一项重点排查——`scripts/test/role-compiled-prompt.test.mjs` 会断言注入的角色定义与 `dsh/roles/*.md` 逐字一致，是本次唯一与被改文件有引用关系的测试：单独跑 8/8 通过；`generate.test.mjs` 31/31 通过。二者均已单独确认。

---

## 人工裁决区（签收时填写）

| 结果 | 说明 |
|---|---|
| **验收通过** | 松哥于 2026-09-17 裁决：基线已正确实现，无影响后续工作的新增优化 |

优化意见：无（裁决为「验收通过」）。

两处范围外残留（`docs/runbooks/acceptance-construction-epic.md:21`、`scripts/workspace-isolation.mjs:935`）维持任务卡中「范围外发现」的登记状态，**未纳入本轮**，待另行决定。
