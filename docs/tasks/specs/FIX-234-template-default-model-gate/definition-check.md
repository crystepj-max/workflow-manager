# Definition Check（定义完成检查）· FIX-234

## 检查元数据

| 项 | 值 |
|---|---|
| 任务名称 | 内置模板默认模型绑定更新（6 节点指向已删除配置）与默认绑定机器闸门 |
| 拟确认基线版本 | V1 |
| 检查人（Agent） | ZCode（requirements-analysis） |
| 检查时间 | 2026-09-20T14:54:35Z |
| 未决产品事项数 | 0 |

## 9.1 目标与范围

- [x] 用户问题明确（配置漂移致默认绑定静默过期，真机才暴露，手工覆盖兜底）
- [x] 需求目标明确（开箱可跑 + 发布前机器闸门给出逐节点核对清单）
- [x] 非目标明确（仓外配置 / 探针语义 / #233 / 编辑器 UI / 两层设计重议 / 历史模板均不动）
- [x] 修改范围明确（四套蓝图 6 节点 + 映射表测试 + release:verify 新检查段）
- [x] 不修改范围明确（.generated 手改、异源三态、历史模板等）
- [x] 修改前状态明确（6 节点过期、无闸门）
- [x] 修改后状态明确（24 节点全有效 + 双层闸门）

## 9.2 规则与边界

- [x] 主要业务规则明确（蓝图重生成、验证/执行异模型两层设计、弱异源语义、闸门两条硬规则）
- [x] 主要用户路径明确（release:verify → 核对清单）
- [x] 关键边界场景明确（§11：CI 无 DSH / 可达全绿 / 可达有过期 / 未来漂移 / v4.1-flash 前提不成立 / 与运行期探针关系）
- [x] 关键异常场景明确（不可达显式跳过、负路径失败清单）
- [x] 对已有功能的影响明确（映射表测试语义更新为唯一存量测试影响，弱异源断言保持）
- [x] 已知风险明确（v4.1-flash 前提、通道施工成本、覆盖面错觉）

## 9.3 决策完整性

- [x] 所有会影响产品结果的人工决策均已完成（闸门方案、验证角色新模型均由用户 2026-09-20 拍板）
- [x] 不存在「施工时再决定」的产品问题（通道传输方式属内部施工选择，验收按行为写）
- [x] 未决产品事项数量为 0

## 9.4 任务组织

- [x] 已拆两切片（默认值修复 / 闸门段），各自可独立 UAT
- [x] 每个子任务均可独立 UAT（② 正路径 UAT 以 ① 为前置条件，UAT 层面）
- [x] 前置依赖已填写「无」
- [x] 优先级已确定（P1）

## 9.5 验收

- [x] 验收条件明确（AC-01 ~ AC-07）
- [x] 已能形成主要 UAT 场景（UAT-01 真机核对清单 + 可选负路径复演）
- [x] 验收标准可以通过真实操作判断

## 9.6 无人值守

- [x] 已明确「无人值守许可」（允许：仓库侧实现 + 可 stub 单测）
- [x] 若允许无人值守，不存在必须在施工过程中由人选择的产品问题（产品 DSH 实测属发布场景，UAT-01 显式划出）

---

## 结论

- [x] **全部通过** → 状态改为「待确认」，呈递人工确认基线
- [ ] 未通过 → 保持「定义中」

## 关键证据索引

1. LOC-019 原始意图：`f410900` 提交信息「验证角色 → deepseek-v4-flash」+ `scripts/test/builtin-template-model-defaults.test.mjs` 映射表（`VERIFY_MODEL`）——「漏网」定性不成立，真实根因为配置漂移无闸门。
2. 过期面实测：`templates/*.json` 四套蓝图 `bindings.models` 枚举——6 节点 `deepseek-v4-flash`（construction review/test、diagnose review/regression、explore evaluate、optimize evaluate），其余 11 节点 `deepseek-flash`（真机有效）。
3. 事实源约束：`deepseek-official` 模型目录无磁盘配置文件，权威源在产品 DSH 运行时（`llm.listProviders/listModels`；vwf 宿主 `src/host.js:1159-1199` 目录成员判定同口径）。
4. 闸门挂载点：`scripts/release-verify.mjs` 分段流水线（新增检查段自然融入）；`npm run validate` 为纯仓库侧、CI 可跑。
5. 用户决策：闸门方案 A（双层）、验证角色 `deepseek-v4.1-flash`（crystepj-max，2026-09-20）。
