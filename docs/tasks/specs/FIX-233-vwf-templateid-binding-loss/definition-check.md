# Definition Check（定义完成检查）· FIX-233

## 检查元数据

| 项 | 值 |
|---|---|
| 任务名称 | wf_run 按 templateId 启动误报「节点未绑定 Agent」（覆盖层合成混合形态被误判为蓝图落盘格式） |
| 拟确认基线版本 | V1 |
| 检查人（Agent） | ZCode（requirements-analysis） |
| 检查时间 | 2026-09-20T13:48:30Z |
| 未决产品事项数 | 0 |

## 9.1 目标与范围

- [x] 用户问题明确（带覆盖层的内置模板 templateId 起跑误报未绑定，仅显式传图可绕过）
- [x] 需求目标明确（templateId 与显式传图行为等价，误报消失）
- [x] 非目标明确（#234 默认绑定过期 / 机器闸门段 / 覆盖层语义 / 校验强度 / 探针语义均不动）
- [x] 修改范围明确（`ingestToDsl` 判据 + 单测 + dist 重建）
- [x] 不修改范围明确（投影内核语义、模板蓝图、保存闭环落盘形态）
- [x] 修改前状态明确（误报三节点未绑定）
- [x] 修改后状态明确（校验通过，绑定 = 默认 + 覆盖）

## 9.2 规则与边界

- [x] 主要业务规则明确（LOC-014 覆盖语义、requireModels 契约、显式传图支持）
- [x] 主要用户路径明确（`wf_run --templateId` 主路径 + 显式传图对照路径）
- [x] 关键边界场景明确（§11：无覆盖层 / 全量绑定 / 部分绑定 / $default / 坏覆盖文件 / 真实缺绑定 / 绑定指向未配置模型）
- [x] 关键异常场景明确（探针 BLOCKED 与本 bug 的区分）
- [x] 对已有功能的影响明确（其余模板、显式路径、编辑器保存闭环均不回归）
- [x] 已知风险明确（判据反向回归、dist 漂移、与 #234 耦合）

## 9.3 决策完整性

- [x] 所有会影响产品结果的人工决策均已完成（无遗留：修复取向属施工内部选择，用户可见行为唯一确定）
- [x] 不存在「施工时再决定」的产品问题
- [x] 未决产品事项数量为 0

## 9.4 任务组织

- [x] 无需切片（体量 S，单切片）
- [x] 单任务可独立 UAT
- [x] 前置依赖已填写「无」
- [x] 优先级已确定（P1）

## 9.5 验收

- [x] 验收条件明确（AC-01 ~ AC-07，均可机器判定或真实操作判定）
- [x] 已能形成主要 UAT 场景（UAT-01 产品模式真机）
- [x] 验收标准可以通过真实操作判断

## 9.6 无人值守

- [x] 已明确「无人值守许可」（允许：实现 + 机器闸门）
- [x] 若允许无人值守，不存在必须在施工过程中由人选择的产品问题（真机 E2E 属 #213 发布验收链，已显式划出）

---

## 结论

- [x] **全部通过** → 状态改为「待确认」，呈递人工确认基线
- [ ] 未通过 → 保持「定义中」

## 关键证据索引（复现与根因）

1. 报错源头：`packages/dsh-visual-workflow/dist/validate-core.cjs:809-817`（requireModels 只读蓝图 `bindings.models`）。
2. 误判分支：`packages/dsh-visual-workflow/src/host.js:469-481`（`ingestToDsl` 以「顶层非空 `bindings.models`」判蓝图形态）。
3. 混合形态来源：`src/host.js:417-433`（`composeModelBindings` 双写内联 `model` 与顶层 `bindings.models`）。
4. 复现：2026-09-20 以产品同款 dist（2026-09-19 17:52 构建）+ 仓库 `.generated/wf-explore/vwf-dsl.json` + 本机真实覆盖层（仅 `evaluate`）完整模拟 findWorkflow→ingestToDsl→projectToVwf→validateBlueprint，输出与 #233 报错逐字一致（探索统筹/专家研究/综合分析 未绑定 Agent+模型，`evaluate` 通过）。
5. 对照：去掉覆盖层 → `hasBindings=false` → 校验通过（解释其余三套模板不受影响）；显式全量 `bindings.models` 传图 → 通过（解释「显式传图可绕过」）。
