# UAT 验收卡 · CHORE-38（收口脚本清理兜底补自动校验）

> 依 CHORE-36 落地的硬要求编写：验收项必须声明「执行时机」，人工裁决只依据「裁决前可观测」项。

## 验收卡总览

| 字段 | 内容 |
|---|---|
| 任务名称 | 收口脚本 prune 兜底单测补齐 |
| Issue | cnb#38（本地轨道；GitHub 同步 pending，CNB 仅作编号锚点） |
| 需求基线版本 | V1 |
| 本轮目标 | ① 给「清理失效登记」兜底动作补上能咬得住的自动校验；② 让收尾提示按真实结果说话，不再无条件宣称已清理 |
| 修改前 | 对清理结果的断言 **0 处**；端到端用例只验「父工作区已删」；收尾提示无条件写「失效登记已 prune」 |
| 修改后 | 新增两条断言类用例（正常路径已清理、幽灵子登记被真正注销）；收尾提示按结果分叉，失败时写明「未能 prune」并给人工补救命令 |
| 自动审查结果 | **未覆盖**——本票体量 S，未启动独立审查会话；以变红验证与逐项自检替代（如实标注，不作为隐藏缺口） |
| 自动测试结果 | **通过**——`npm test` 680 / 680，退出码 0（补齐环境依赖后，见备注） |
| 已知风险 | ① 幽灵登记场景靠「移走子工作区目录」构造，已用变红验证确认有效；② 新工作区首次跑测试需先装子包依赖并生成产物，否则会出现与改动无关的失败 |
| 未覆盖项 | ① 未执行独立自动审查会话；② 清理**失败**分支的提示文案未做运行时验证（无法在不注入故障的前提下触发，已按规格不做故障注入用例，仅代码层核对）；③ 不处理与本票无关的既有红灯 |
| Run 标识 | chore-38-r1 |

人工正式结果只允许三选一：**验收通过** / **验收退回** / **有条件通过**。

---

## UAT 清单（裁决依据）

### 【UAT-01 新增断言真的能抓住退化】

**执行时机**
裁决前可观测

**验收目的**
确认补的不是「跑过就算」的装饰性断言，而是兜底动作一旦失效就会亮红灯。

**前置条件**
1. 位于任务工作区 `../workflow-manager-worktrees/dev-chore-38-r1`，分支 `dev-chore-38-r1`
2. 施工改动已提交

**操作步骤**
1. 跑 `node --test scripts/test/local-task-merge.test.mjs` → 记录结果
2. 临时把清理动作改成不执行，再跑同一命令 → 记录结果
3. 恢复，再跑一次 → 记录结果

**预期结果**
1. 第 1 步全绿（11 / 11）
2. 第 2 步**两条断言同时变红**：端到端用例（清理结果断言）+ 幽灵登记用例
3. 第 3 步恢复全绿

**自动验证结果**
**通过**——实测：第 1 步 11/11；第 2 步 `not ok 6`（端到端用例）与 `not ok 10`（幽灵登记用例）同时变红，9 pass / 2 fail；第 3 步恢复后 11/11 全绿。

**建议人工关注**
这是本票唯一的实质风险点。实测两条都咬住了，说明断言不是空转；如果只红了一条，另一条就必须重写。

---

### 【UAT-02 收尾提示不再谎报】

**执行时机**
裁决前可观测

**验收目的**
确认清理成功与失败两种情形下提示说法不同，失败时给出可执行的补救命令。

**前置条件**
1. 收尾提示已按结果分叉

**操作步骤**
1. 查看脚本中收尾提示的构造处，核对两个分支的说法
2. 跑一次正常收口路径，查看输出提示

**预期结果**
1. 存在两个分支：成功 → 「已 prune」；失败 → 「未能 prune … 需人工执行 git worktree prune」
2. 正常路径输出含「失效登记已 prune」
3. 不存在无条件声称成功的表述

**自动验证结果**
**通过（成功分支）/ 代码层核对（失败分支）**——成功分支在 11/11 全绿运行中已实际产生；失败分支无法在不注入故障的前提下触发，按规格不做故障注入用例，已直接核对三元分叉的代码逻辑：**无条件声称已 prune 的写法已不存在**。

**建议人工关注**
如果你希望失败分支也能被自动验证，需要允许注入故障的用例——本票按「不为不可能场景写测试、且保持最小改动」的取舍没做。要补的话请明确说，我可以另立一小票。

---

### 【UAT-03 改动范围收敛】

**执行时机**
裁决前可观测

**验收目的**
确认只动了必须动的地方，没有顺手改别的。

**操作步骤**
1. 查看本次改动的差异统计
2. 逐项确认改动可追溯到本票验收条件

**预期结果**
1. 改动仅两个文件：收口脚本（一处提示文案）+ 测试文件（新增用例与断言）
2. 清理动作本身、异常保护、既有 10 个用例的断言均未改动

**自动验证结果**
**通过**——`git diff --stat`：收口脚本 +4/-1，测试文件 +30/-0，共 34 行新增、1 行替换。

**建议人工关注**
无。

---

## 收口后复核（合并后由执行体填写；**不是**人工裁决的依据）

| UAT 项 | 执行者 | 执行时间 | 命令 | 实际结果 | 结论 |
|---|---|---|---|---|---|
| UAT-04 主检出侧全仓测试全绿 | 执行体（AI） | 待填 | `npm test` | 待填（工作区侧 680/680 全绿） | 待填 |
| UAT-05 主检出侧治理闸门结论不变 | 执行体（AI） | 待填 | `npm run validate:workspace` | 待填（已知 LOC 系列既有红灯，与本票无关） | 待填 |

> 备注：工作区侧首次跑全仓测试曾出现 15 项失败，逐个查明均为**环境缺失**而非改动引入——① 子包依赖未装（`ERR_MODULE_NOT_FOUND`，补 `npm install` 后消失）；② 缺生成产物目录（补 `npm run generate` 后消失）。补齐后 680/680 全绿。**新工作区首次跑测试前这两步是必需的**。

---

## 人工裁决区（签收时填写）

| 结果 | 说明 |
|---|---|
| **验收通过** | 松哥于 2026-09-19 裁决：基线已正确实现，无影响后续工作的新增优化 |

优化意见：无（裁决为「验收通过」）。

未纳入本轮的已知取舍，维持 UAT 卡中的登记状态：清理**失败**分支的提示文案仅做代码层核对，未做运行时验证（需注入故障，按规格不做故障注入用例）。如后续要求补自动化验证，另立小票。
