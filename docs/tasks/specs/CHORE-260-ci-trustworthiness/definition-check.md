# Definition Check · CHORE-260 CI 可信度专项

## 检查元数据

| 字段 | 值 |
|---|---|
| 任务标识 | `CHORE-260` |
| 远端 issue | github#260 |
| 检查对象 | 对 CI `validate` 闸门的全部红灯机制做一次完整盘点，给出统一判定基准与收敛顺序 |
| 检查时间 | 2026-09-21 |
| 结论位置 | 本文末「结论」节 |

## 9.1 目标与范围

- [x] 目标限定为：使 `validate` 在 CI 上稳定全绿，具备被设为必需状态检查的资格
- [x] 范围覆盖**全部**已知红灯机制，含 1 类非红灯但属可信度缺口的项（M7 基准产物改写）
- [x] 明确不含：修改产品行为语义、改蓝图与生成结果
- [x] 明确不含：对历史红灯做事后粉饰；只按机制修因
- [x] 明确不含：把 M1（FIX-254）的既有成果重复计功——M1 仅作对照基线列出

## 9.2 现状证据

- [x] CI 基线取自真实运行：PR #256 HEAD `e3909d8b` 的 `validate` 作业日志，③ 段 8 项失败、③′ 段两包全绿
- [x] 8 项失败均已取得**用例名 + 文件:行 + 错误原文**三者齐备（`scripts/validate.mjs` 已具备该输出能力，PR #248 落地）
- [x] M1 证据：CI 同 runner 双臂对照，失败数 15（无身份）→ 8（有身份）
- [x] M2 根因定位到行：`scripts/dev-plugin.mjs` 的 `lsofBin` / `listeningProcesses()` / `inspect()` 返回 `null` 链路；`resolvePortState()` 为 fail-closed 设计（产品行为正确，问题在测试环境依赖）
- [x] **M3 根因已实证**：`validate-workspace.mjs` 的 `EXEMPT_ABS_PREFIXES = ['/private/tmp/', '/tmp/']` 与 `os.tmpdir()` 的平台差异；在 macOS 上设 `TMPDIR=/tmp` 后该文件**恰好 4 项失败**，与 CI 输出逐项吻合；本机默认 `os.tmpdir()` = `/var/folders/…` 不在豁免前缀内，故本地绿
- [x] **M5 根因已实证**：含 FIX-245 的主干代码 + 移除宿主指纹 → `local-task-registry.test.mjs` 16 项中 1 项红；FIX-245 之前的提交 `7a8ae6a5` + 同样环境 → 16 项全绿。故为 FIX-245 引入的回归
- [x] **M4 已排除多种假设**：本地默认、`TMPDIR=/tmp`、`HOME` 替换、`GH_CONFIG_DIR` 指向空目录（模拟未登录）、无 `gh` —— 均未复现，标记为待取证
- [x] **M6 代码证据齐备**：`elapsed < 20_000`（m5 测试 `:137`）、`spawnSync timeout: 60_000`（`:67`）、`watchdogMinutes: 0.03`（`:130`）、EB 的 `until(…, ms = 8000)`（`evaluation-baseline-runtime.test.mjs:48`）
- [x] **M6 本地实测**：造 24 个 CPU 满载进程（load average 升至 20）后 M5 文件仍 6/6 通过（4.7s）→ 本地未复现假超时，如实记录而不夸大
- [x] **M7 已量化**：跑一次引擎层测试即改写 `scripts/benchmark/loc-045/` 下 51 个被跟踪文件（345 增 / 345 删），且改动含 `template_digests` / `script_digest`，说明冻结夹具已与当前模板脱钩
- [x] 前次陈述的错误已被发现并更正：PR #256 中「剩余 8 项均非本票引入」对 M5 一项不成立

## 9.3 验收标准

- [x] AC-01 可机器验证：CI `validate` 的 ③ 段输出 0 项失败
- [x] AC-02 可机器验证：③′ 段两包均绿
- [x] AC-03 环境无关性可复现：M2「无 `lsof`」、M3「`TMPDIR=/tmp`」、M5「无宿主指纹」三种条件均可本地构造
- [x] AC-04 无偶发红：连续 ≥3 次运行（含一次满载时段）结论一致
- [x] AC-05 本地与 CI 结论一致：同提交下失败集合相同
- [x] AC-06 产物治理生效：跑完整测试后 `git status scripts/benchmark` 为空
- [x] AC-07 判定基准可用：本表可被后续会话直接引用做机制定位
- [x] 标准不含模糊表述；「修绿」明确禁止通过删豁免前缀等削弱防护的手段达成

## 9.4 依赖与前置

- [x] 无跨票代码依赖：7 类机制彼此独立，可分别施工
- [x] 依赖顺序已声明：M5 → M3 → M2 → M4（先取证）→ M6 → M7
- [x] 工具依赖已确认：`gh` 已登录 `crystepj-max`，发号得到 `github#260`
- [x] 施工隔离：本票在独立 worktree / 分支施工，不动主检出内并行会话的在制内容
- [x] 本票为**盘点与判定**性质，不含实现改动；实现拆入 6 张子票（见规格 §5）

## 9.5 风险与残留

- [x] M4 根因未定：明确标为「待定位」，并写明下一步取证方式（CI 上单跑该脚本并保留完整 stdout/stderr），**禁止凭猜测改断言**
- [x] M6 未被本地复现：如实记录「风险属实但阈值在 10 核机器上未触达」，不夸大为已发生故障
- [x] M7 涉及产品决策（产物入库 / 忽略 / 只读校验三选一），本票只给方向，不代签
- [x] 子票拆分建议已给出，但不预先承诺体量与排期
- [x] 本票自身不改任何代码，不存在引入新回归的风险面

## 9.6 定义后追加（2026-09-21，施工期）

> 本节为**追加记录**，不改动 9.1–9.5 的历史内容（定义阶段快照保持原样）。

- [x] **AC-08 已追加**：收口归档全绿（M8）——CI `validate-workspace` 的 D-8 / D-9 / D-10 三项全绿（与规格 §4 一致）。
- [x] **机制表扩为 8 类**：新增 **M8 · 收口归档完整性**（P1）。发现方式为施工期实证——`validate` 链是 `validate.mjs && validate-task-spec-sync.mjs && validate-workspace.mjs`，③ 段先失败即**短路**，`validate-workspace` 的 D-8/D-9/D-10 **从未被执行**；M1–M7 收敛、引擎层首次全绿后才暴露。
- [x] **M8 历史欠账已量化**（2026-09-21 CI 实测）：D-8 约 30 条 / D-9 约 48 条 / D-10 约 60 条，主体为 `LOC-028`~`LOC-045`、`FEAT-84`~`FEAT-103`、`FIX-97`~`FIX-108`、`CHORE-104`、`CHORE-111`、`FEAT-237` 等早期任务。
- [x] **子票拆分扩为 7 张**：新增「子票 7 · M8 收口归档完整性」P1（见规格 §5）。
- [x] **本专项自身引入的 D-8/D-10 条目已顺手补齐**（`CHORE-262` / `-264` / `-268` / `-273` / `-276` / `-279`），未计入 M8 的历史欠账。
- [x] **新增发现 · D-10 的环境敏感性**：该检查核验**本地** `refs/heads/<branch>`，故同一提交在本地与 CI 结论可能相反（`branch_retained=false` 的任务在本地因残留工作分支违规、在 CI 通过）。此前「已清除」的判断仅对 CI 成立，已如实更正并写入规格 M8 节，交子票 7 一并裁定口径。
- [x] **基线口径更正已记录**：任务卡 §「当前 CI 实测基线」的 **8 项**取自 `e3909d8b`，属历史值；`main@f0d95202` 实测为 7 项（M2×2 + M5 + M3×4）。施工完成后 ③ 段为 **✅ 全绿**（M5→`CHORE-262` / M3→`CHORE-264` / M2→`CHORE-268` / M4→`CHORE-273` / M6→`CHORE-276`）。

## 结论

**通过。** 已完成对 CI `validate` 全部红灯机制的盘点：7 类机制中 1 类已修（M1）、4 类根因定位到行或已实证（M2/M3/M5/M7）、1 类待取证（M4）、1 类为已量化的脆弱性（M6）；同时纠正了前次关于「8 项均非本票引入」的错误陈述，并给出统一判定基准、收敛顺序与子票拆分建议。具备开工条件。

**（2026-09-21 追加）** 施工期实证后机制表扩为 **8 类**：M1–M6 已全部收敛，M4 的「待取证」已由 CI 取证确定为**事件类型决定**（非 flaky），并恢复 `✅ 引擎层测试全绿`；新增 **M8**（P1）与 **AC-08**、**子票 7**。定义结论本身不变，追加部分见 §9.6。
