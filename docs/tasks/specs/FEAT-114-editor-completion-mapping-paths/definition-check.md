# Definition Check · FEAT-114

| 项 | 值 |
|---|---|
| 任务名称 | 编辑器补齐完成映射声明与多层结果字段路径 |
| 拟确认基线版本 | V1 |
| 检查人（Agent） | requirements-analysis（2026-09-19 会话） |
| 检查时间 | 2026-09-19 |
| 未决产品事项数 | **0** |

## 9.1 目标与范围

- [x] 用户问题明确 — 自建工作流完成类型恒为空；多层结果路径在面板显示为未配置且编辑会静默覆盖手写路径
- [x] 需求目标明确 — 不打开 JSON 即可声明完成映射；多层路径如实回显与保真写回
- [x] 非目标明确 — §4 七条（含不做四段重排、不改看板呈现、不改运行时取值、不加 OBSERVING）
- [x] 修改范围明确 — §7 五项
- [x] 不修改范围明确 — §8 逐项
- [x] 修改前状态明确 — §5，含「编辑器内 `completionPath` 出现 0 次」这一手核实
- [x] 修改后状态明确 — §6

## 9.2 规则与边界

- [x] 主要业务规则明确 — §9 九条内核规则带出处，另加四条跨层约束（分层不混用、枚举真源为模板、内置只读、载荷闸门）
- [x] 主要用户路径明确 — §10 五步，含手写多层路径的第二条路径
- [x] 关键边界场景明确 — §11 七条（四种非法态、fanout、同字段冲突、单层↔多层互改、无候选值、内置只读）
- [x] 关键异常场景明确 — §11 + §17（载荷超限、内核分叉）
- [x] 对已有功能的影响明确 — 结果路由面板行为变更限多层路径场景；`CHORE-104` 载荷与同文件并行的影响写在 §17
- [x] 已知风险明确 — §17 四项带处置方式

## 9.3 决策完整性

- [x] 所有会影响产品结果的人工决策均已完成 — 发布门槛归属、面板分层形态（三层为定稿）、主源归属、DT-01 运行入口选 A、OBSERVING 取舍，共五项由产品 2026-09-19 选定
- [x] 不存在「施工时再决定」的产品问题 — 「新控件放哪一段」已由定稿 §4.2 定为「结果与去向」层；界面具体排布属施工选择
- [x] 未决产品事项数量为 0 — 载荷余量不足时「是否上调闸门」不属本票决定范围，已按 §17 明确回报为 `CHORE-104` 的阻塞项

## 9.4 任务组织

- [x] 已完成必要的功能切片拆分 — 本票 = 定稿 S-3；S-2（Preset 纠偏）与 S-4（看板字段）由主源另立，不混入本票；与 `CHORE-113` 分票
- [x] 本子任务可独立 UAT — UAT-01 不依赖 Preset 选择器或看板补齐即可验证完成类型呈现；UAT-02 独立验证路径保真
- [x] 前置依赖已明确填写「无」 — §14；`FEAT-115` 取消后本票不再有反向依赖，S-4 与本票互不阻塞（改不同区段）
- [x] 优先级已确定 — P1（发布后第一批）

## 9.5 验收

- [x] 每个主要功能有明确验收条件 — §15 七条对应 §7 五项范围
- [x] 已能形成主要 UAT 场景 — §16 四组，覆盖正常配置、路径保真、规则同口径、只读不变
- [x] 验收标准可以通过真实操作判断 — UAT-01/02 要求真实跑到收口并回看运行详情与 JSON

## 9.6 无人值守

- [x] 已明确「无人值守许可」= 允许
- [x] 允许无人值守时不存在必须在施工中由人选择的产品问题 — 规则真源与校验口径已在 §9 定稿；载荷超限的处理是「回报阻塞」而非请人选产品方案；真实验收关口按仓库双轨规则保留

## 结论

- [x] **全部通过** → 状态可改为「待确认」，呈递人工确认基线
- [ ] 未通过

未通过项：无。

核实说明：§9 与 §2 引用的行号（`validate-core.cjs:123/695/698/866/868/870/874/898/905/908`、`client.js:5552/5600-5618`、`build-bundle.mjs:276`、`templates/wf-diagnose.json` closeout）均在本轮直接读取当前 main 确认；「编辑器 `completionPath` 出现 0 次」由全文计数确认。
