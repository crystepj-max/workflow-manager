# Definition Check · CHORE-286 M8 收口归档完整性

## 检查元数据

| 字段 | 值 |
|---|---|
| 任务标识 | `CHORE-286` |
| 远端 issue | github#286 |
| 检查对象 | 使 `validate-workspace` 的 D-8 / D-9 / D-10 三项全绿（父票 AC-08） |
| 检查时间 | 2026-09-21 |
| 结论位置 | 本文末「结论」节 |

## 9.1 目标与范围

- [x] 目标限定为：D-8 / D-9 / D-10 三项全绿，达成父票 AC-08
- [x] 范围覆盖全部 67 条违例（D-8 33 + D-9 12 + D-10 22）
- [x] 明确不含：改动历史任务的业务内容、状态与合并记录
- [x] 明确不含：改写存根与既有摘要的既有事实文字（只新增字段）
- [x] 明确不含：修 `task-runs-cleanup.mjs` 的 `cleared_at` 缺口、清理 44 个孤儿目录、处理 22 条无法核验项

## 9.2 现状证据

- [x] 基线取自真实 CI：`main@1f9f09f9` 的 `validate` 作业日志，D-8 33 条 / D-9 12 条 / D-10 22 条，逐条明细已留存
- [x] 判定实现已读到行：D-8 `:294-326`、D-9 `:342-352`、D-10 `:355-368`
- [x] D-8 三类根因已分流并逐一核对材料可用性：15 条「摘要缺凭据」的存根**已载明** `remote_ref` 与合并信息；16 条「无归档目录」的**卡与规格终版均在库**；2 条「缺摘要」的卡与规格已在归档内
- [x] D-9 12 项已用工具**只读预演**取证：逐项目录、体积（合计约 8 MB，`loc-013-r1` 约 7.2 MB）
- [x] D-10 22 条已按分支真实存在性**三方分流**：真错误 1 条（`CHORE-104`）、仅本地存在 20 条、本地远端均在 1 条（`CHORE-111`）
- [x] D-10 的检查器环境敏感性已实证：核验 `refs/heads`（本地），CI 只检出单 ref → 标 `true` 必失败；本地实测违例 1 条，与 CI 的 22 条结论方向相反
- [x] 附带隐患已记录：`task-runs-cleanup.mjs` 对「明细目录已不存在」的到期项不补 `cleared_at`
- [x] 未采信父票的「D-8≈30 / D-9≈48 / D-10≈60」粗略估算，改为以 CI 实测为准并逐条列出

## 9.3 验收标准

- [x] AC-01 D-8 全绿，可机器验证
- [x] AC-02 D-9 全绿，可机器验证
- [x] AC-03 D-10 全绿，可机器验证
- [x] AC-04 本地与 CI 结论一致，可机器验证（新口径消除环境对立）
- [x] AC-05 未编造：回填字段均可溯源，且对短哈希与完整哈希做**前缀断言**
- [x] AC-06 未改写历史事实：摘要改动只新增字段；存根与源材料零改动，可用 `git diff` 核验
- [x] AC-07 父票 AC-08 达成
- [x] AC-08 D-9 删除内容有打包备份，路径记录在案
- [x] 标准不含模糊表述；「修绿」不得通过删检查、放宽判定或伪造字段值达成

## 9.4 依赖与前置

- [x] 无跨票代码依赖；M8 与 M1–M7 相互独立
- [x] 两项决策已由项目负责人裁定：D-10 改核验共享真值；D-9 授权清理 12 项
- [x] 工具依赖已确认：`gh` 已登录，发号得到 `github#286`
- [x] 施工隔离：本票在独立 worktree / 分支施工；主检出登记册被并行会话占用，故 D-9 的字段写入落在本票工作区

## 9.5 风险与残留

- [x] D-9 删除不可逆：已以打包备份兜底，且删除对象为过程产物、证据摘要仍在归档内
- [x] D-10 新口径依赖远端引用新鲜度：CI 由显式 fetch 步骤保证，失败会显式报错而非降级
- [x] `remote_ref` 未联网复核 CNB 侧：按存根 = 既有事实采信，符合 FIX-72 轻量归档语义，如实标注
- [x] 三项遗留已如实记录，不伪装为已解决：清理工具的 `cleared_at` 缺口、44 个孤儿目录、22 条无法核验项
- [x] 本票改动面含检查器与 CI 工作流各一处：均为「提供判定前提」，未放宽任何判定（`true`/`false` 两侧违例条件均保留）

## 结论

**通过。** 67 条违例已全部分流到四类根因，每条都有可溯源的修法与材料；D-10 的原描述已被实测更正（22 条中仅 1 条为真数据错误），并取得负责人对新口径的裁定；验收标准均可机器验证。具备开工条件。
