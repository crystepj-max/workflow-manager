# Definition Check · 片 2 证据呈递完整性（呈递保护 + 引擎时间序/conditional_pass 断言）

> 进入「待确认」前逐项勾选；全部通过后才可请求人工确认基线。人工确认后才落档为「已定义」。

## 检查元数据

| 项 | 值 |
|---|---|
| 任务名称 | 证据呈递完整性：成熟刷新呈递保护与引擎时间序/conditional_pass 断言 |
| 拟确认基线版本 | V1 |
| 检查人（Agent） | 独立开工会话（#123 余项） |
| 检查时间 | 2026-09-19 |
| 未决产品事项数 | **0** |

## 9.1 目标与范围

- [x] 用户问题明确——人工答的那份内容可被刷新替换
- [x] 需求目标明确——两条呈递保护 + 三条引擎新断言，绕过写入工具也躲不掉
- [x] 非目标明确——不动 schema、不接写入路径、不追溯历史、不重做已落地的 assembled 保护
- [x] 修改范围明确——`cwf-record.mjs` 两处 + `cwf-evidence-verify.mjs` 三处 + 用例 + 契约文字
- [x] 不修改范围明确——既有九项语义、回退/额度/attempt、归档链路

## 9.2 现状证据（本会话实测）

- [x] `isFinalized()` 行号与判定条件已核对（`cwf-record.mjs:227-240`），确认 design/baseline 两条缺口成立
- [x] 引擎⑥只做「chosen ∈ 自带候选集」已核对（`:139-149`），确认不进呈递历史比较
- [x] 已完成的一半已核对（`:139-146` + 用例 `:407`/`:427`）——避免重复施工
- [x] 时间序全仓零实现已用检索确认（schema/引擎/record/protocol 均无跨字段比较）
- [x] **纠正项**：conditional_pass 须附 feedback 在 schema 已强制，且 `cwf-validate.test.mjs:86` 有正例；缺的是引擎复核与负例——已如实写入规格 §5，不按「规则缺失」施工
- [x] **真实事故样本已独立复算**（2026-09-21）：`.agent-runs/cwf-74-r1/acceptance_package.a4.json` 记 `decided_at=09:30:00Z`、`created_at=15:09:17.151Z`、`verified_head=a30f5791`（提交 `2026-09-10T22:18:32+08:00` ＝ `14:18:32Z`）。本会话按日期解析复算得**两处倒挂**：对 `created_at` 早 5h39m17s、对提交时刻早 4h48m32s——转述成立，并**多发现一条不依赖 git 的探测器**，据此把时间序拆为 ⑩-a/⑩-b 两档
- [x] 与 GitHub #214（CHORE-75 收口护栏）边界已核对该票正文：其「回填」指**人工回填归档账目**、不含时间序断言；屏幕物证与归档 mtime 归该票，本票只做可机械断言部分（R-6 已写明边界）

## 9.3 验收标准

- [x] V-1~V-11 均可自动复现；每条保护要求「修复前先红」，且时间序一条直接用真实事故形状做用例
- [x] 明确要求「应放行」的正例（V-3），防止保护过头无人发现
- [x] 明确要求「无法评估 ≠ 通过」的断言（V-10），防止 git 不可达时静默放行

## 9.4 依赖与前置

- [x] 前置依赖：无（与片 1 代码面不重叠；片 3 单向依赖本片）
- [x] 既有通道确认存在：`deepEqual` 已由 `cwf-validate.mjs` 导出并在 assembled 保护中使用

## 9.5 风险与可逆性

- [x] 风险已列（保护过头、项数变化的连带文案、时钟偏差、与片 1 同文件冲突）
- [x] 可逆：纯代码与文档改动，可整片回退；不涉对外动作
- [x] 不可逆项：无

## 9.6 无人值守许可

- [x] 允许。两条保护是否要做、时间序落点、conditional_pass 处理深度三项产品口径已裁定；余下 E-2「补呈递放行」属不改产品结果的施工裁定，已在 §12 标注归属。

## 9.7 与既有契约一致性

- [x] §3.6「签收对象即呈递版本」——本片是该原则在另两类记录上的落实，不新增语义
- [x] §5.3 已决 package 必须保留裁决时呈递的 `decision_request`——本片把「保留」从状态自洽升级为与历史比对
- [x] §8.3 ②「conditional_pass 另须 feedback」——本片补引擎侧断言，条目文本不变
- [x] §8.5 保留纪律：新增判定不删除任何历史记录，R-1 明确跨 attempt 不受约束

## 9.8 未决事项

无未决产品事项。

> 非产品口径待办：引擎项数由九改为「九 + ⑩(a/b) + ⑪」时的文案联动（施工事项，规格 §17 已列）。正式号已落定为 GitHub #220（2026-09-21 经授权在 GitHub 补建）。
>
> 2026-09-21 修订说明：并入 cwf-74-r1 事故取证后，本检查表 §9.2 / §9.3 相应补项；**未决产品事项仍为 0**——权威时刻分层由松哥作为需求基线证据输入直接给出，未引入新的待决问题。

## 结论

- [x] **全部通过** → 状态可改为「待确认」，呈递人工确认基线
