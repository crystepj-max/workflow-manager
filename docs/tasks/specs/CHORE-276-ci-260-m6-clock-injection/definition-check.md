# Definition Check · CHORE-276 CI 可信度 M6 挂钟依赖

## 检查元数据

| 字段 | 值 |
|---|---|
| 任务标识 | `CHORE-276` |
| 远端 issue | github#276 |
| 检查对象 | 消除闸门内 4 处挂钟硬阈值：断言改为业务事实、阈值改为可注入 |
| 检查时间 | 2026-09-21 |
| 结论位置 | 本文末「结论」节 |

## 目标与范围

- [x] 目标限定为：目标两个测试文件默认全绿，且挂钟阈值可注入并**被证明生效**
- [x] 范围限定为 4 处已识别阈值 + 使其可断言的**最小**产品侧配合（新增一个输出字段）
- [x] 明确不含：删除或放宽任何检查意图
- [x] 明确不含：修改 dispatcher 的控制流与调度语义
- [x] 明确不含：父票其余子票范围（M5/M3/M2/M4 已分别由 `CHORE-262`/`CHORE-264`/`CHORE-268`/`CHORE-273` 收敛；M7 待决；第 8 类另立）

## 现状证据

- [x] 4 处阈值逐一取自父票规格 §M6 并**已核对当前代码仍在**：`ai-task-night-dispatch-m5.test.mjs:137` / `:67` / `:130`、`evaluation-baseline-runtime.test.mjs:48`
- [x] 关键发现已核实：`ai-task-dispatcher.mjs:490` 的 `entry.watchdogFired = true` 全仓**仅此一处赋值、零读取**（死代码）
- [x] 修复后本地实测：M5 调度 6/6、EB 运行时 9/9
- [x] 本文件挂钟断言已清零（`elapsed <` / `Date.now() - t0` 形态检索为空）
- [x] **注入生效已做对照实验**：`VWF_TEST_UNTIL_MS=1` → `until 超时：首段启动` 等出现；`VWF_TEST_TIMEOUT_MS=1` → 6 项全 fail
- [x] 产品侧 diff 复核：仅 +3/-1（新增 `watchdogFired` 集合与输出字段；移除死字段）

## 验收标准

- [x] AC-1 ~ AC-6 均可机器验证，判据为具体命令与通过/失败数
- [x] AC-4 用**注入极端值复现失败**来证明「可注入」不是摆设——静态改默认值无法通过该判据
- [x] AC-3 以形态检索兜底，防止挂钟断言以其他写法残留
- [x] AC-6 以「无新增分支或行为改变」约束，防止借机改动调度语义
- [x] 标准不含模糊表述

## 依赖与前置

- [x] 无跨票代码依赖
- [x] 施工隔离：本票在独立 worktree / 分支施工，不动主检出内并行会话的在制内容
- [x] 包测试前置已满足：`packages/dsh-visual-workflow` 已 `npm install`
- [x] 发号完成：`gh` 已登录 `crystepj-max`，发号得到 `github#276`
- [x] 定义落档：任务卡、本规格、本核对清单三者齐备且互相引用一致

## 风险与残留

- [x] 默认值上调（60s→120s、8s→30s）会延长**失败路径**的等待时间：仅在测试真挂死时触及，属可接受的兜底代价；正常路径耗时不变
- [x] `watchdogMinutes: 0.03` 保留：其数值不再承担断言职责，时序敏感性已由业务事实断言吸收
- [x] 产品侧仅新增输出字段，不改变任何控制流；死字段移除不构成行为变更（原本无读取方）

## 结论

**通过。** 4 处阈值已逐一识别并处置：核心那处（挂钟断言）换成了**更精确**的业务事实断言——这同时修复了产品侧一处「写了没人读」的死代码；阈值改为可注入并用极端值注入做了反向验证；产品侧改动仅 +3/-1 且不触碰控制流。三件套齐备，具备开工条件。
