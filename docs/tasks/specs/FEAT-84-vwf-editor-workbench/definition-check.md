# Definition Check · FEAT-84 编排台工作流模板编辑器落地

## 检查元数据

| 项 | 值 |
|---|---|
| 任务名称 | 编排台工作流模板编辑器落地（画布独立滚动 / 业务路由 / 撤销历史） |
| 任务标识 | `FEAT-84`（远端 cnb#84） |
| 拟确认基线版本 | V1 |
| 检查人（Agent） | 克里斯（WorkBuddy 会话） |
| 检查时间 | 2026-09-17T13:03:00Z |
| 未决产品事项数 | 0 |

---

## 9.1 目标与范围

- [x] 用户问题明确 —— 规格 §2：位置感丢失 / 看不懂在配什么 / 流程关系被压缩成二元
- [x] 需求目标明确 —— 规格 §3：两层表面、三区独立滚动、渐进披露、关系完整可见
- [x] 非目标明确 —— 规格 §4（6 条，含不改执行引擎、不给自定义模板加模型默认/覆盖层）
- [x] 修改范围明确 —— 规格 §7（11 项）
- [x] 不修改范围明确 —— 规格 §8（执行引擎 / 运行详情 / 角色 / 供应商后端 / host RPC）
- [x] 修改前状态明确 —— 规格 §5，含 `src/client.js` 约 152–190 行共享父级滚动这一根因
- [x] 修改后状态明确 —— 规格 §6

## 9.2 规则与边界

- [x] 主要业务规则明确 —— 规格 §9（8 条硬规则，含连接清单不漏边、技术词降层）
- [x] 主要用户路径明确 —— 规格 §10（9 步）
- [x] 关键边界场景明确 —— 规格 §11（8 类）
- [x] 关键异常场景明确 —— 规格 §11 含保存失败、模板定义与配置不一致、内置只读被尝试编辑
- [x] 对已有功能的影响明确 —— 规格 §8 明确不动执行引擎与模板语义；§9 明确内置模型范围保持
- [x] 已知风险明确 —— 规格 §17（5 项，含与 FEAT-86 的同文件合并冲突）

## 9.3 决策完整性

- [x] 所有会影响产品结果的人工决策均已完成 —— 规格 §12 六项均已有确认人与时间
- [x] 不存在「施工时再决定」的产品问题 —— 视觉方向、共享契约、模型范围边界、窄屏行为均已在规格内冻结
- [x] 未决产品事项数量为 0

## 9.4 任务组织

- [x] 如需求复杂，已完成必要的功能切片拆分 —— 规格 §13；H1/H2/H3 三切片，本切片为 H1 全量
- [x] 每个子任务均可独立 UAT —— 编辑器工作区不依赖运行详情与角色页改动的完成
- [x] 前置依赖已明确填写「无」或具体任务 —— 规格 §14「无」；共享契约不构成前置依赖
- [x] 优先级已确定 —— P1

## 9.5 验收

- [x] 每个主要功能有明确验收条件 —— 规格 §15 V-1～V-10
- [x] 已能形成主要 UAT 场景 —— 规格 §16 UAT-01～UAT-06
- [x] 验收标准可以通过真实操作判断 —— 全部为可观察行为，无「代码已改」式验收
- [x] 每条验收项均已声明执行时机 —— UAT-01～05 为「裁决前可观测」，UAT-06 为「收口后观测」
- [x] 「收口后观测」项已写明执行者与结果回填位置 —— UAT-06 已写明执行体与回填位置；失败处置为如实记录、判断是否本任务引入

## 9.6 无人值守

- [x] 已明确「无人值守许可」—— 允许
- [x] 若允许无人值守，不存在必须在施工过程中由人选择的产品问题 —— 全部取舍已在 §12 冻结；规格 §11 已给出各类边界场景的期望行为

---

## 结论

- [x] **全部通过** → 状态可改为「本地已定义」，呈递人工确认基线
- [ ] **未通过**

未通过项：

1. （无）

---

## 检查依据与限制说明

- 本检查基于 Codex 产出的三份独立施工 handoff 与仓库现状（`main` @ `bf33ad3`）。
- 定义阶段的判断依据为文档事实：`docs/tasks/handoffs/H1-editor-workbench.md`、`packages/dsh-visual-workflow/prototypes/ui-workbench/DESIGN.md`（A 编排台视觉与对比度原则）、`REVIEW-2.md`（本轮用户问题逐项回应）、`docs/tasks/LOC-014-builtin-model-override.md`（模型覆盖边界）。
- 规格中「修改前状态」的滚动容器根因来自源码阅读（`src/client.js` 约 152–190 行），**未在产品 DSH 中实测复现**；施工方须在开工时以真实界面确认。
- 本任务与 FEAT-85 / FEAT-86 可能在同一时间窗内并行施工（并发度由人工在启动批次时决定），三者共享 `src/client.js`。定义阶段的并发结论：**不设机器依赖**，共享契约以 `DESIGN.md` 冻结，收口按 FEAT-84 → FEAT-85 → FEAT-86 顺序合并。
