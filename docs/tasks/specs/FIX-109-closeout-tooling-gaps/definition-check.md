# Definition Check · FIX-109 收口脚本两处口径缺口

> 进入「待确认」前逐项勾选；全部通过后才可请求人工确认基线。人工确认后才可落档为「已定义」。任一未勾选或未决产品事项 > 0 → 禁止标为「已定义」。

---

## 检查元数据

| 项 | 值 |
|---|---|
| 任务名称 | 收口脚本两处口径缺口：merge_commit 回写时机致主检出残留 + 任务范围正则取空 |
| 拟确认基线版本 | V1 |
| 检查人（Agent） | WorkBuddy（克里斯） |
| 检查时间 | 2026-09-19 |
| 未决产品事项数 | **0** |

---

## 9.1 目标与范围

- [x] 用户问题明确——收口后主检出残留自我阻塞下一次收口；收口提交任务范围字段恒为废值
- [x] 需求目标明确——收口结束主检出干净；任务范围取到真实正文；两处有单测守住
- [x] 非目标明确——不动判定口径、不动引擎路线与适配层、不动验收包 schema、不追溯历史提交
- [x] 修改范围明确——`local-task-merge.mjs` 回写落账动作 + 范围抽取函数；`scripts/test/local-task-merge.test.mjs` 补测
- [x] 不修改范围明确——已列明 5 类不动项

## 9.2 现状证据

- [x] 修改前现状已实测并记录（代码行号、任务卡实际形态、收口提交实测值）
- [x] 存在可复现的历史现象（CHORE-106 收口后残留未提交；CHORE-37/38/106 任务范围字段同为废值）
- [x] 根因已定位到代码级（写入排在提交之后；正则假设标题后无空行）

## 9.3 验收标准

- [x] 每条验收标准可被自动或人工复现（主检出 `git status` 为空、`merge.commit` 为真实哈希、`extractRangeText` 可断言）
- [x] 验收标准不含模糊表述（「正常」「合理」等）

## 9.4 依赖与前置

- [x] 前置依赖为空
- [x] 依赖的既有通道已确认存在（收口脚本自身、既有测试文件）

## 9.5 风险与可逆性

- [x] 已列风险与处置（收口主路径回归、提交数变化、正则放宽误取、并行会话冲突）
- [x] 改动可逆（均为脚本内改动，回退即恢复；不涉及外部副作用与不可逆动作）

## 9.6 无人值守许可

- [x] 依据明确——两处均为脚本内部口径缺陷，修法与验收标准可由规格直接判定，无需要人取舍的产品问题

## 9.7 与既有契约一致性

- [x] 不改变收口契约语义：`merge.commit` 仍指收口合并提交，回写提交仅为账本落账
- [x] 不覆盖长期原则与版本目标规格

## 9.8 未决事项

- [x] 无未决事项。两处修法在规格 §7 已写明（回写落账改为脚本自动追加一次提交；范围抽取改为「取到下一个标题」并抽出可单测函数），无需人工裁决。

---

## 结论

定义门各项全部通过，未决产品事项为 0 → 本票可转入「**待确认**」，等待人工确认需求基线 V1 后落档为「本地已定义」。

> 当前状态：`定义中`（本次仅落档卡与规格，尚未请求基线确认）。
