# Definition Check · 片 3 验收包与收口写入路径自动整链校验

> 进入「待确认」前逐项勾选；全部通过后才可请求人工确认基线。人工确认后才落档为「已定义」。

## 检查元数据

| 项 | 值 |
|---|---|
| 任务名称 | 验收包与收口写入路径自动执行证据链整链校验（失败拒写） |
| 拟确认基线版本 | V1 |
| 检查人（Agent） | 独立开工会话（#123 余项） |
| 检查时间 | 2026-09-19 |
| 未决产品事项数 | **0** |

## 9.1 目标与范围

- [x] 用户问题明确——不跑命令也能在整链不成立时签收
- [x] 需求目标明确——两个时刻自动跑、失败拒写
- [x] 非目标明确——不加旁路开关、不全量触发、不改引擎语义、不做 CI
- [x] 修改范围明确——`cwf-record.mjs` 两处调用 + runbook 文案 + 用例
- [x] 不修改范围明确——引擎本体、其余五类记录、回退与额度语义

## 9.2 现状证据（本会话实测）

- [x] `cwf-record.mjs` 不引用 `verifyEvidenceChain` 已核对（import 仅 cwf-validate / cwf-run-init / workspace-paths）
- [x] 校验仍为人工步骤已核对：`dsh/skills/construction-bootstrap/runbook.md:143` 是引擎唯一调用处
- [x] 既有拒写先例已核对（schema 失败、proof 绑定不符均 exit 1）→ 拒写不是新行为范式
- [x] 范围归属如实标注：#123 原文为「仓库脚本**或** CI 检查」，仓库脚本已交付，本片属延伸而非补漏

## 9.3 验收标准

- [x] V-1~V-8 可复现；含「无记录文件、index.json 未更新」的落盘侧断言，不只是退出码
- [x] V-4 固定「awaiting_decision 不触发」，防止实现成全量触发

## 9.4 依赖与前置

- [x] 前置依赖：片 1、片 2，理由符合「未完成则无法正确开始」口径（自动化对象须是最终判定集）
- [x] 被依赖方接口确认存在：`verifyEvidenceChain` 已 `export`，CLI 入口有 `import.meta.url` 守卫，可安全 import

## 9.5 风险与可逆性

- [x] 🔴 主要设计点已前置披露：引擎从 `index.json` 读验收包，而待写记录尚未落盘（规格 §17），且明确禁止「先写后删」实现
- [x] 风险已列（签收变慢、同文件冲突、实况不可得时的语义）
- [x] 可逆：改动集中在两个写入分支，可整片回退

## 9.6 无人值守许可

- [x] 允许。触发时机与失败行为两项产品口径已裁定；`awaiting_decision` 不触发属不改产品结果的施工裁定，已在 §12 标注归属。剩余 §17 的设计点是实现方式选择，不改变对外行为。

## 9.7 与既有契约一致性

- [x] §8.3「呈递或签收前必须校验」——本片落实「签收前」一半，并在已知限制中显式声明「呈递前」仍为人工，不谎称全覆盖
- [x] §7.3 实况绑定语义沿用引擎既有回退，不另立口径
- [x] §8.5 保留纪律：拒写不落盘，不产生半成品记录

## 9.8 未决事项

无未决产品事项。

> 待前置：本片不得在片 1、片 2 合并前开工；正式号待授权补建。

## 结论

- [x] **全部通过** → 状态可改为「待确认」；但**开工资格**受片 1/片 2 前置约束
