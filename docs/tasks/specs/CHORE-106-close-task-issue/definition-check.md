# Definition Check · CHORE-106 收口 close-task 动作实现

> 进入「待确认」前逐项勾选；全部通过后才可请求人工确认基线。人工确认后才可落档为「已定义」。任一未勾选或未决产品事项 > 0 → 禁止标为「已定义」。

---

## 检查元数据

| 项 | 值 |
|---|---|
| 任务名称 | 收口交付动作 close-task 实现：合并后关闭远端 issue 并落账 |
| 拟确认基线版本 | V1 |
| 检查人（Agent） | WorkBuddy（克里斯） |
| 检查时间 | 2026-09-19 |
| 未决产品事项数 | **0**（§9.8 开放项已于 2026-09-19 经松哥裁定为方案 A，见 DT-01） |

---

## 9.1 目标与范围

- [x] 用户问题明确——17 个已合入 issue 未关闭，账实不符持续复发；收口提示词宣称了未实现的能力
- [x] 需求目标明确——让 close-task 可用、提示词与能力一致、失败不静默
- [x] 非目标明确——不动 GitHub 适配器现状、不碰 FEAT-84/85/86 UI、不做历史批量补关
- [x] 修改范围明确——`operations-host.mjs` 注册实现；`delivery-closeout-host.mjs` 接执行分支；提示词对齐；失败处理
- [x] 不修改范围明确——已列明 5 类不动项

## 9.2 现状证据

- [x] 修改前现状已实测并记录（常量/注释位置、provider 注册情况、PR body 为空、尾注格式）
- [x] 存在可复现的历史现象（18 个 issue 人工关闭记录）
- [x] 根因已定位到代码级（非推测）

## 9.3 验收标准

- [x] 每条验收标准可被自动或人工复现
- [x] 验收标准不含模糊表述（「正常」「合理」等）

## 9.4 依赖与前置

- [x] 前置依赖为空
- [x] 依赖的既有通道已确认存在（`remote-issue-sync.mjs` 的 `remoteAllocate` / `resolveRemoteSlug`）

## 9.5 风险与可逆性

- [x] 已列风险与处置（凭据、误关、本地路线未覆盖）
- [x] 关闭动作不可逆，已设计前置核查（WR-012 防重 + 合并确认成功后执行）

## 9.6 无人值守许可

- [x] 依据明确——补齐既有契约已声明的实现与提示词对齐，无需要人取舍的产品问题

## 9.7 与既有契约一致性

- [x] `dsh/roles/closeout.md` L22 契约本身正确（外部动作由适配器声明），本票补实现而非改契约
- [x] 不覆盖长期原则与版本目标规格

## 9.8 未决事项

- [x] **~~开放项~~ 已裁定**：本票实现范围是否覆盖**本地脚本路线**（`local-task-merge.mjs`）？
  - **裁定：方案 A**（松哥，2026-09-19）——只补引擎适配层实现；本地脚本路线不做自动关闭，但收口报告显式列出「待人工关闭 <issue 号>」；`local-task-merge.mjs` 不在本票改动范围。
  - 记录：`decision-tickets/DT-01-local-script-route.md`（已关闭）
  - 未决产品事项数：**0**

---

## 结论

定义门各项全部通过，未决产品事项为 0 → 本票可转入「**待确认**」，等待人工确认需求基线 V1 后落档为「本地已定义」。
