# CHORE-37 · 本地任务卡

## 任务基本信息

| 字段 | 值 |
|---|---|
| 任务标识 | `CHORE-37` |
| 远端 issue | cnb#37（编号由 CNB 远端发号；前身本机号 `LOC-035` 记于登记册 `legacy_id`） |
| 需求来源 | 会话录入 |
| 来源定位 | LOC-023 有条件通过优化意见②（2026-09-15 人工裁决）；分析复现见 `docs/tasks/specs/CHORE-37-closeout-force-wording/requirements-analysis.md` §3 |
| 任务名称 | 交付链口径残留收敛：closeout 角色的 `--force` 表述 |
| 任务类型 | 维护性（编号类型 CHORE） |
| 优先级 | P2 |
| 当前状态 | 已合并 |
| 需求基线版本 | V1 |
| 前置依赖 | 无 |
| 施工环境组 | CHORE-37 |
| 施工环境角色 | 独立 |
| 无人值守许可 | 允许 |
| 任务规格位置 | `docs/tasks/specs/CHORE-37-closeout-force-wording/task-spec-V1.md`（入库） |
| 定义时间 | 2026-09-16T16:09:57Z（基线 V1 经松哥确认） |
| GitHub 同步 | pending |

> 说明（不进机器解析字段）：
> - `GitHub 同步 = pending` 的含义：本任务属**本地轨道**——GitHub 凭据不可用；CNB 侧已有编号锚点（cnb#37）但**未被交付流程接入**，仅作需求源与讨论留痕，**不构成 tracker 侧的「已定义」**。本版本的开工资格来自本地任务卡与登记册。
> - `无人值守许可 = 允许` 的依据：纯文档改动 + 一次构建重建，删除行为的硬约束已由 R-4 固定，施工中无需要人取舍的产品问题。
> - Formula/取值约束：`无人值守许可` 只能写枚举原值，`GitHub 同步` 只能写 `pending` / `synced#N` / `not-applicable`，附加自然语言会通过实施前检查失败。

## 摘要（三要素速览）

### 任务目标

让收口说明书中「删不掉怎么办」这一条与已通过决策完全一致——**拒绝删除、登记遗留、不阻塞合并、任何情况下不使用强删参数**；改完后交付链上任何一处都不再出现「允许或建议使用强删」的表述。

### 涉及范围

- 做：
  - 唯一真源 `dsh/roles/closeout.md:24` 改写为禁止强删的口径（保留同条款里的归档核对与 prune 兜底两句）
  - 重建 `packages/dsh-visual-workflow/dist/roles/closeout.md`（由 `build-bundle.mjs` 从 `dsh/roles/*.md` 拷贝生成，**禁止手改**）
- 不做：
  - 不加机器闸门（松哥裁定选项 A，本次只收敛文本）
  - 不改 R-4 判定语义与四项前置条件
  - 不改 `scripts/local-task-merge.mjs` 的删除行为与其注释
  - 不改已正确的三处口径载体：`runbook.md` §7 收口第 5 步、`local-track-offline-mode.md` §7.4、约定 §3.4
  - 不动 `local-task-merge.test.mjs` 的反向断言（该断言正是守「不许用强删」的正向资产）
  - 不追溯回改历史归档报告；不改动本机安装态技能副本（分发同步另案）
  - 不处理本次识别到的**两处范围外残留**（见下方「范围外发现」）

### 验收标准

- [ ] V-1 真源阶段一删除条款已无「允许/建议使用强删」的表述，语义与规格 §6 目标文案一致
- [ ] V-2 全仓检索后交付链文档中 `--force` 只剩「禁止/不使用」类表述，允许类**计数为 0**
- [ ] V-3 四处口径载体语义一致（runbook / 离线轨道 / 约定 / 收口脚本实现）
- [ ] V-4 dist 副本与真源 `diff` 无差异
- [ ] V-5 `npm run validate:workspace` 主检出退出码 0
- [ ] V-6 `npm test` 不引入新的失败（`generate.test.mjs` S6 为既往红灯，须显式标注为非本票引入）

## 详细规格

完整需求以本地任务规格为准（见上表「任务规格位置」）。实质变更走 Vn→Vn+1 流程。

## 范围外发现（本票识别，未处理，供后续决策）

施工阶段全仓检索识别出两处同类风险，均**不在本票已确认范围内**，如实登记以免丢失：

| 位置 | 性质 | 为何未处理 | 建议 |
|---|---|---|---|
| `docs/runbooks/acceptance-construction-epic.md:21` | 验收演练手册的清理命令示例：直接 `git worktree remove --force <路径>` | 该处是**演练临时工作区**的拆除，不是收口删本任务工作区；且同一段还在使用已废弃的「工作区在仓库目录内」布局，修它等于同时改布局与后续所有路径示例，会把本票从 S 撑到 M | 若认定为同类漂移，建议单独立票，与布局迁移一并处理 |
| `scripts/workspace-isolation.mjs:935` | 脚本实际执行 `git worktree remove --force` | 本票明确不改脚本行为（决策 A 只改文案）；改它等于改删除行为，超出 R-4 的既有授权边界 | 建议单独立票评估：该脚本的强删是否在 R-4 覆盖范围内，是否需要加同等前置条件 |

两处均为**代码实现或历史文档**，与本票「收口角色说明书口径」不是同一对象；如需收敛，请给出明确指令另立任务。

## 变更记录

| 时间 | 状态 | 说明 |
|---|---|---|
| 2026-09-16T00:03:29.832Z | 定义中 | 由 LOC-023「有条件通过」优化意见②登记为后续候选任务（当时本机号 LOC-035），CNB 发号后为 CHORE-37 |
| 2026-09-16T16:00:00Z | 定义中 | 需求分析：定位唯一错口径位于 `dsh/roles/closeout.md:24`；确认 dist 副本为生成产物不可手改；判定体量 S |
| 2026-09-16T16:05:00Z | 待确认 | Definition Check 9.1～9.6 全部通过，未决产品事项 0；决策卡裁定为方案 A（只改文案，不加闸门） |
| 2026-09-16T16:09:57Z | 本地已定义 | 松哥确认基线 V1（含优先级 P2）；具备开工资格 |
| 2026-09-16T16:28:00Z | 等待验收 | 施工完成：真源第 24 行改写 + dist 重建；`npm test` 639/639 全绿；UAT-01～03 裁决前可观测项已自检通过，呈人工裁决 |
| 2026-09-17T15:25:00Z | 已合并 | 松哥裁决「验收通过」；收口合并 `2f2fc55`（tag `task/chore-37/v1`），归档三件套入库、工作区已删、分支 `dev-chore-37-r1` 保留待补 PR；收口后复核：`npm test` 679/679 全绿，治理闸门 4 项失败均为 LOC 系列既有红灯，本票不在其中 |
