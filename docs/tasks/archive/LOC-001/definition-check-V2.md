# Definition Check — LOC-001 基线 V2

> 进入「待确认」前逐项检查。全部通过后才可请求人工确认基线。

## 检查元数据

| 项 | 值 |
|---|---|
| 任务名称 | 编辑器边判断条件适配业务结果路由（V2：录入体验改造） |
| 拟确认基线版本 | V2 |
| 检查人（Agent） | DSH 会话 agent（deepseek-v4.1） |
| 检查时间 | 2026-09-10 |
| 未决产品事项数 | **0** |

## 9.1 目标与范围

- [x] 用户问题明确（第 2 节：不熟悉 DSL 的使用者完不成一条业务结果路由）
- [x] 需求目标明确（第 3 节：三步录入 + 往返不丢 + 缺口当场可见）
- [x] 非目标明确（第 4 节：不改契约、不改内核规则、不托管 boolean、不做 HD、不动画布）
- [x] 修改范围明确（第 7 节 F1–F8）
- [x] 不修改范围明确（第 8 节）
- [x] 修改前状态明确（第 5 节，逐条对应 V1 现状）
- [x] 修改后状态明确（第 6 节）

## 9.2 规则与边界

- [x] 主要业务规则明确（第 9 节 R1–R9：写入契约、参数名规则、删除保护、进档恢复、补边形状）
- [x] 主要用户路径明确（第 10 节：主路径 / 往返路径 / 边侧路径）
- [x] 关键边界场景明确（第 11 节 E1–E10）
- [x] 关键异常场景明确（E3 不可穷举、E5 未声明取值、E6 重复占用、E7 删除保护、E9 改名冲突）
- [x] 对已有功能的影响明确（fanout / boolean / 旧模式边 / 既有模板均不回归；V2 只改"离开该档清空 outcomePath"这一条 V1 行为）
- [x] 已知风险明确（第 17 节：双入口互覆盖、补边默认目标、会话记忆不跨模板）

## 9.3 决策完整性

- [x] 所有会影响产品结果的人工决策均已完成（D1 由用户选定交付路径时采纳推荐 A 方案；D4 来自用户第 2 条反馈；D2/D3 为 Agent 依内核契约给出的建议，随基线确认生效）
- [x] 不存在「施工时再决定」的产品问题
- [x] 未决产品事项数量为 0

## 9.4 任务组织

- [x] 如需求复杂，已完成必要的功能切片拆分（第 13 节：本切片即 V2 全量，不拆；理由：F6 与 F2/F3 不可分，F5 依赖取值声明）
- [x] 每个子任务均可独立 UAT（UAT-01 单场景即可判断能力存在）
- [x] 前置依赖已明确填写「无」
- [x] 优先级已确定（P1，沿用 V1）

## 9.5 验收

- [x] 每个主要功能有明确验收条件（第 15 节 9 条，逐条可勾）
- [x] 已能形成主要 UAT 场景（UAT-01 ~ UAT-04）
- [x] 验收标准可以通过真实操作判断（全部在编辑器界面上肉眼可判）

## 9.6 无人值守

- [x] 已明确「无人值守许可」：允许（沿用 V1）
- [x] 若允许无人值守，不存在必须在施工过程中由人选择的产品问题

## 结论

- [x] **全部通过** → 状态改为「待确认」，呈递人工确认基线
- [ ] 未通过

未通过项：无

## 附：本基线对 V1 验收面的影响（不得静默混用）

| V1 原文 | V2 处理 |
|---|---|
| 范围第 3 条「进入/离开该态**显式清理字段**」 | 改为：离开该档仍清空 `outcomePath`（DSL 不留休眠字段），但**切回时自动恢复**参数名与取值 → V1 该条被 R5+R6 取代 |
| 不做项「业务 outcome 枚举覆盖的**客户端实时检查**（由既有保存校验兜底）」 | 改为：新增**边存在性图标 + 反向告警**（客户端实时检查），该不做项撤销 |
| 范围第 3 条「outcomePath 输入（`$.field` 形式）」 | 改为：只填参数名 + 固定 `$.` 前缀（写入 `$.field` 不变） |
| 范围第 2 条「业务 outcome 的**名称输入**」 | 改为：源节点处于路由档时为**下拉**，非路由档保留文本输入 |

除此之外的 V1 验收项（边四态、画布标签与配色、往返不损坏、technical 边、i18n、测试与 dist）继续有效。
