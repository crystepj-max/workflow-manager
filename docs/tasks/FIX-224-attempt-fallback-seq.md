# FIX-224｜旧冻结脚本的执行证据兜底（node_attempt 序号）

## 任务基本信息

| 字段 | 值 |
|---|---|
| 任务标识 | FIX-224（远端锚点：GitHub Issue #224，2026-09-19 经用户授权发号） |
| 需求来源 | github-issue |
| 来源定位 | GitHub Issue #183 项 1（PR #177 Codex Review R2 ④，B 类遗留） |
| 任务名称 | 旧冻结脚本的执行证据兜底（node_attempt 序号） |
| 任务类型 | 完整功能开发 |
| 分类 | bug |
| 体量 | S |
| 优先级 | P2（随 V1 基线一并确认） |
| 当前状态 | 已定义 |
| 需求基线版本 | V1（已确认基线；Definition Check 通过，未决产品事项 0） |
| 前置依赖 | 无 |
| 施工环境组 | FIX-224 |
| 施工环境角色 | 独立 |
| 无人值守许可 | 允许（随 V1 基线一并确认） |
| 任务规格位置 | `docs/tasks/specs/FIX-224-attempt-fallback-seq/task-spec-V1.md` |
| 定义时间 | 2026-09-20T02:19:49Z |
| GitHub 同步 | synced#224 |

已登记进 `docs/tasks/registry.json`（`remote: GitHub #224`、`github_sync: #224`、`status: 待确认`），看板 `BOARD.md` 已重写。编号通路：用户 2026-09-19 授权改走 GitHub 建 issue，未使用 CNB 发号工具（CHORE-111 的发号源迁移仍待定）。

## 摘要（三要素）

**目标：** 旧运行续跑留下的执行记录不再冒充逐次证据——兜底条目带段内顺序号，并明确标注「段末扫描记录（不保证逐次）」。

**范围：** 做——宿主段末扫描入档补顺序标识与来源标记、运行详情对该来源的呈现、补齐回退路径用例。不做——重建历史逐次记录、改 `[vwf-attempt]` 协议、改 Store 提交键与 Proof 签发、批量改写历史落盘记录。

**验收标准：**

- [ ] AC-01：无事件行的冻结脚本跑完一段 → 本段新完成的每个节点各留一条带段内顺序号与 `evidence_source: 'segment_scan'` 的条目；同节点本段重入多次仍只有一条（不得凭空多出条数），该段整体显示「段末扫描记录（不保证逐次）」。
- [ ] AC-02：新脚本段行为逐字不变，`attempts-runtime.test.mjs` 现有 4 例仍全绿且不新增段末提交。
- [ ] AC-03：运行详情中该段与「逐次记录」肉眼可区分，无需了解实现即可判断。
- [ ] AC-04：`npm run generate && npm run validate` 不因本改动新增失败项。

## 成立性判定（本会话复核结论）

**部分成立。** 主路径（新运行逐次记录）已由 LOC-029 落地并有 4 例实测通过；残留仅在「Rev1 冻结脚本无 `[vwf-attempt]` 事件行」的旧运行续跑时出现——`recordNodeAttempts` 按结果键去重、条目无 `attempt_id`。原票描述准确，但「v0.1 引擎无逐次事件、需引擎可观察面增强」这一前提**已过时**：事件面已具备。

## 已拍板的产品口径

- ✅ **决策 1 = B**：兜底条目补段内顺序号 + 明确标注来源（不谎称证据完整，不作废历史运行）。确认人 松哥（crystepj-max），2026-09-19T13:16:16Z。
- ✅ **决策 1（附带）= 不提供补偿方案**：逐次次数物理不可复原，只做标注。同上确认。

## 详细规格

完整需求以本地任务规格为准（见上表「任务规格位置」）。实质变更走 Vn→Vn+1 流程。

## 变更记录

| 时间 | 状态 | 说明 |
|---|---|---|
| 2026-09-19T12:39:13Z | 定义中 | 从 #183 项 1 拆出，完成规格 V1 草稿与 Definition Check；未决产品事项 2 项，未达「待确认」 |
| 2026-09-19T13:16:16Z | 待确认 | 决策 1（含历史 Run 补偿附带项）落定为 B；规格 §3/§6/§12/§15/§16 与 AC-01 口径据此定稿，Definition Check 复检通过（未决 0） |
| 2026-09-19T13:32:21Z | 基线已确认 | 用户确认三片 V1 基线（批准对象见规格 §12 基线确认记录）。🟡 因决策 4 选择等 CHORE-111 发号，本卡未登记进登记册、未建远端 issue，故状态仍保持「待确认」而非「本地已定义」——发号后无需重议口径即可直接推进并开工 |
| 2026-09-19T13:40:00Z | 待发号→已发号 | 经用户授权改走 GitHub 通路发号：GitHub #224，登记册 `remote: GitHub #224`、看板已重写，卡片与规格目录由临时号改名为 `FIX-224-attempt-fallback-seq`。🟡 下一步只需按正式号重新确认基线 V1（内容未变），确认后转「已定义」即可开工；本卡与规格文件尚未提交入库，`validate:task-context` 会暂报 spec-untracked |
| 2026-09-20T02:19:49Z | 已定义 | V1 基线按正式号重新确认（内容与临时号版本一致，无口径变化）。远端 issue 正文状态同步为「已定义」，标签 `bug` + `ready-for-agent` + 体量标签已打。状态记「已定义」而非「本地已定义」：GitHub 可用且远端 issue 已承载任务基本信息，不属降级本地轨道 |
