# Issue #81 收口报告 — 正式内置角色体系（12 角色）与历史角色迁移

> *This was generated by AI during triage.*

- 收口日期：2026-08-30
- 收口分支：`dev-#81-Built-in-Roles`（PR #124）
- 实现交付：**已完成**（PR #124，4 个提交，+1196/−32，26 文件）
- 收口结论：**实现交付、验证挂起 —— 待 #104 完成后用建设流程 Dogfood 验证；验证不通过则重开**

---

## 1. 收口结论

本 Issue 的实现工作已经完成并通过全部可运行测试（169 项，0 失败），以 PR #124 交付。但按维护者确定的验证路径，**验证动作后置**：待 #104（External Profile）完成后，用建设流程 Dogfood 验证角色体系在真实流程中的表现。因此本次收口关闭 Issue 并打上 `deferred-verification` 标注，**验证不通过即重开**。

挂起**不是**否决：12 角色体系已真实落地，PR #124 保持开放供评审与合并；合并 PR 不等同于通过验证。

---

## 2. 交付清单

| 工单 | 内容 | 状态 |
|---|---|---|
| 01 | 内置角色集合单一来源化，测试基线去硬编码 | ✅ `540c515` |
| 02 | `dispatcher` 退出内置身份，迁为自定义角色 | ✅ `f7f012d` |
| 03–07 | 12 角色落地（新增 7 + 升级 5） | ✅ `dd780cc` |
| 08 | 收口验收与文档同步 | ✅ `202a7ce` |

**关键取舍**（详见 PR #124 描述）：

- `evaluator` 未复用 PR #70 素材 —— 那份是探索专用结论评估，与本 Issue 要求的「优化/探索共享基础能力」定位相反，已重写为共享能力，**角色文件内零评价枚举**（测试锁住）。
- 测试不复制角色清单 —— 内置角色清单本身即单一来源，原有 4 处「恰好 6 个」的硬编码断言改为行为与结构断言，另加下限护栏（`>= 12`）：扩充无需改测试，误删会被拦下。
- `dispatcher` 迁移零风险 —— 不动定义文件、只移出内置集合，4 个历史模板节点零改动，解析路径与引用保护全部照旧。
- 存量 5 个角色只做定向增补（身份定位能力导向化 + 新增「适用边界与可复用场景」），未整篇重写。

---

## 3. 验收核对（实现层）

机械核对 Issue 正文 10 条 + 补充 2 条：**PASS 11 / WARN 1 / FAIL 0**。

| 校验 | 结果 |
|---|---|
| 内置角色精确 12 个，id 与顺序符合规格 | ✅ |
| 内置只读拦截（update / remove 双重判定） | ✅ |
| 未新增按节点名命名的重复场景角色 | ✅ |
| `evaluator`/`review`/`test`/`accept` 定义含职责边界段落 | ✅ |
| `evaluator` 不含任何具体评价枚举 | ✅ |
| `dispatcher` 保留文件、不在内置集合、仍被 4 个模板节点引用 | ✅ |
| `dev`/`test`/`review`/`accept`/`closeout` 五个 id 不变 | ✅ |
| 现有模板引用的 profile 全部可解析到角色文件 | ✅ |
| 机器 ID 稳定英文、中文名符合规格 | ✅ |
| 12 个角色定义文件均存在且非空 | ✅ |
| 未创建永久专家角色（Expert A/B/C/D） | ✅ |
| 自定义角色修改不改变已启动 Run 的 Snapshot | ⚠️ **缺口，随 #79 回填** |

**测试**：`npm test`（插件包 5 文件）116 项全过；`node --test "scripts/test/*.test.mjs"` 85 项全过；`npm run validate` 全闸门通过；`npm run generate` 正常产出。

---

## 4. 待验证要素

**验证窗口**：#104（External Profile）完成并可用之后。

**验证方式**：用建设流程 Dogfood 跑真实任务，检验 12 角色在真实流程中的表现。重点核对：

1. 角色定义在真实任务中是否给出足够指引（`requirements` / `designer` / `diagnose` 为全新撰写，未经实战校准）；
2. `evaluator` 作为共享能力，在「优化」与「探索」两种节点契约下是否都能正确取用节点提供的评价取值；
3. `dispatcher` 以自定义身份参与历史流程时，行为是否与迁移前一致；
4. 探索三角色（`orchestrator` / `researcher` / `synthesizer`）复用 PR #70 素材校准后的实际效果。

**注意**：#104 完成 ≠ 验证必然通过。若建设流程跑起来后发现角色定义与真实流程需求有偏差，属正常验证结果，按重开标准处理。

---

## 5. 待重开要素（重开标准）

出现下列任一情形，即判定验证不通过，**重开本 Issue**：

1. 12 个角色的 id 或中文名与规格不符，或任一角色定义文件缺失/为空（文件缺失会**静默回退到占位正文**，不报错）；
2. `dispatcher` 迁移后，引用它的历史工作流节点无法解析角色或行为改变；
3. `evaluator` 无法被「优化」与「探索」共用，或评价枚举被写死进角色定义；
4. PR #124 合并后角色库行为回归：内置角色可被编辑/删除，或「基于此角色创建自定义角色」失效；
5. Dogfood 中发现角色职责边界问题（如 `review` 与 `evaluator` 混淆、`dev` 越界写测试、`diagnose` 跳过取证直接给结论）；
6. #82 实施四套模板时，发现现有 12 角色无法覆盖模板的节点绑定需求（绑定缺口）。

**已知不可交付项（不触发重开）**：Issue 验收第 8 条「自定义角色修改不改变已启动 Run 的 Snapshot」依赖 #79（Run Snapshot），本次未交付，随 #79 回填，不计入验证失败。

---

## 6. 交接产物

| 产物 | 位置 |
|---|---|
| 实现代码与测试 | PR #124（`dev-#81-Built-in-Roles`，4 提交） |
| 需求分析（三要素 + size 判定 + 依赖现状） | `specs/issue-81-built-in-roles/requirements-analysis.md` |
| 8 张工单记录（含验收证据） | `specs/issue-81-built-in-roles/issues/01–08` |
| 本收口报告 | `specs/issue-81-built-in-roles/CLOSEOUT.md` |
| 12 角色定义 | `dsh/roles/*.md`（12 内置 + `dispatcher` 自定义保留） |
| 角色权威规格 | `docs/design/workflow-manager-v0.1-final-product-spec.md` §8 |
| 能力边界原则 | `docs/design/workflow-design-principles.md` §1 / §10 / §13 |
| 探索三角色素材来源 | 分支 `feat/multi-perspective-exploration`（PR #70，仅素材） |

---

## 7. 关联

- 依赖 #58（已关闭）、遵循 #71（已关闭）
- #82 依赖本 Issue —— #82 已在 2026-08-30 完成需求分析定稿（体量 `sized-l`），因 5 项前置未开工而挂起，详见其 Issue 状态
- 验证载体：#104（External Profile，下一阶段）
