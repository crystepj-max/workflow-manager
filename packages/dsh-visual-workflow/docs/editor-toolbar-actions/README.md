# 编辑器操作区样式与撤销/重做 · 真实界面证据

> 本次改动：
> 1. 「新增节点 / 删除节点」改为图标 + 文案的成组操作按钮，删除操作全程保持红色；
> 2. 编辑器头部增加撤销/重做按钮（↶ ↷），支持画布编辑与 JSON 编辑的历史回退；
> 3. `vwf.workflows.list` 排序改为内置模板置顶，用户模板按字母序排在后面。

## 证据文件

| 文件 | 内容 |
| --- | --- |
| `editor-toolbar.png` | 真实 Chromium 打开编辑器后的操作区快照 |

## 程序化验证

- 插件测试：新增/删除成组样式、撤销/重做行为（含禁用态）、内置模板置顶排序均有用例覆盖；
- 历史回退覆盖画布编辑与 JSON 编辑产生的变更；
- 删除操作在禁用态仍为完整红色（不再因透明度混色发黑）。
