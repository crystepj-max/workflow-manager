# 相邻边直连修正 · 真实界面验收证据

> 本次修复防重叠误判：已经绕行的长边不再占用“直连标签中点”，避免「测试 → 审核」这类相邻 success 边被误绕到顶部。

## 证据文件

| 文件 | 内容 |
| --- | --- |
| `dev-workflow.png` | 真实 Chromium 加载「开发工作流 2.0」后的画布截图 |

## 程序化路径检查

修复后真实渲染结果：

- `测试 → 审核`：使用相邻节点的直接贝塞尔连接，不包含正交绕行折点；
- `调度 → 结束`、`开发 → 结束`、`分流 → 审核` 等确实跨越节点的边继续走上方外围车道；
- 回退/失败边的外围绕行规则保持不变。

该证据验证了防重叠规则不会再把已绕行长边的标签中点误占用为直连标签冲突。
