# 工作流边颜色区分 · 真实界面验收证据

> 本次优化调整边状态颜色：成功为固定蓝色 `#2563eb`，失败为红色，选中为黑色加粗；选中态在深色画布上保留浅色描边以保证可见。成功色不读取皮肤品牌变量，避免品牌色为黑色时成功边也被渲染成黑色。

## 证据文件

| 文件 | 内容 |
| --- | --- |
| `edge-colors.png` | 真实 Chromium 中的编辑器画布截图 |
| `capture-metrics.json` | 程序化颜色与字宽度量 |

度量结果：

- 未选中 success 边：`rgb(37, 99, 235)`，线宽 `2.2px`；
- 未选中 failure 边：`rgb(229, 72, 77)`，线宽 `2px`；
- 选中边：`rgb(17, 24, 39)`，线宽 `4.2px`；
- 选中边标签：黑色、`700` 字重，并带浅色描边。

`passed = true`。
